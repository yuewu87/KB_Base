# 问题记录

本文档记录 KN_Base 项目讨论过程中的**所有问题、结论与分歧**。

## 使用规则

- 每个问题下方直接写结论，**回答和方案写在问题下面**。
- 未得出结论的标记为 🔴 **待解决**，并写明卡在哪里。
- **项目要求：没有讨论出结果的问题，不先动手实现。**
- 结论变化时，修改此处并注明日期，不另开文档。
- **作废的条目在标题后标 `【作废 YYYY-MM-DD】` 并写明作废原因，内容保留不删**——「当初为什么那么想」比结论本身有用。**编号空缺不补，留着**，免得旧讨论、commit message、其它文档里的编号全部对不上。

> 🔴 **当前待解决的问题汇总在文末第十二节。**

---

# 一、项目初始化

### Q1. 这个项目要做什么？

**A：** 知识库项目。工程代码在 `E:\Study_Projects\KN_Base`，知识数据在 `E:\KB_Library`。两者物理分离，各自独立版本管理。

### Q2. Python 环境怎么建？

**A：** 用 **conda** 新建环境 `kn_base`，Python **3.11**（2026-09-15 确认）。

**环境内一律用 pip** 装依赖，不混用 `conda install`（避免依赖冲突）。

```bash
conda create -n kn_base python=3.11 -y
conda activate kn_base
pip install -r requirements.txt
```

### Q3. 代理 5408 怎么用？

**A：** 两者都要——写入 CLAUDE.md 作为约定，同时配置到 pip 和 git 全局。**已于 2026-09-15 完成**：
- pip → `C:\Users\wuyeu\AppData\Roaming\pip\pip.ini`
- git → `--global` 的 `http.proxy` / `https.proxy`

注意这是**全局**配置，影响本机所有项目。代理未启动时命令会挂起。

### Q4. 架构文档放哪？

**A：** 放 `README.md`（用户指定），不写进 CLAUDE.md。

---

# 二、知识库定位

### Q5. 知识库主要装什么内容？

**A：** 内容很杂，包括个人学习笔记、专业领域资料库、项目/研究沉淀、通用第二大脑。允许**多分级**。

### Q6. 知识库主要给人用还是给 AI 用？

**A：** **人机共用**（路线 B）。不是纯 AI 检索库。

### Q7. Obsidian 是必要的吗？

**A：** 分对象看：
- **对 AI 不必要**——AI 直接读文件系统就够了。
- **对人必要**——在「人机共用」定位下，Obsidian 承担人的阅读、编辑、反链、搜索界面。自建同等能力的 GUI 是几个月的活。

### Q8. 知识库的本体是什么？

**A：** 是 `E:\KB_Library` 里的 **markdown 文件 + 目录结构 + frontmatter 字段**，不是 Obsidian。Obsidian 只是这些文件的一个视图，可随时卸载，文件仍在。这也是选本地 markdown 而非数据库的原因：AI 友好、可读、无锁定。

---

# 三、内容结构

> **本节 Q9–Q14 于 2026-09-16 整块作废。** 原因：目录结构重订——新结构以**领域**为 vault 一级目录，`10_项目/`、`20_知识/`、`30_素材/` 全部取消。见 [Q67](#q67-目录结构重订)。**下面内容保留，供回看当初为什么那么想。**

### Q9. 分类用什么形态？ 【作废 2026-09-16】

> **作废原因：** 编号前缀那一整套顶层结构（`00_/10_/20_/30_/40_/90_`）取消，改由「领域」当一级目录。见 [Q67](#q67-目录结构重订)。

**A：** 人的大框架文件夹 + 项目子文件夹 + 独立知识区：

```
00_收件箱/          所有未定稿内容的统一入口
10_项目/
  <项目名>/
20_知识/            跨项目可复用知识（先建大分类，边用边长）
30_素材/            创作素材
40_索引/            MOC 主题地图 + Dataview 看板
90_附件/            图片等小文件
```

### Q10. 跨项目的「可复用知识」怎么处理？ 【作废 2026-09-16】

> **作废原因：** 「抽离」这个动作整个取消。投递时就直接分类进领域，不存在「先落在项目里、再抽出去」两步。见 [Q68](#q68-项目降为标签) 与 [Q71](#q71-抽离取消)。

**A：** **抽到独立知识区（`20_知识/`）**。项目文件夹只放项目专属档案（架构、决策、踩坑），从中提炼的通用知识进知识区，两边用双向链接互指。

理由：文件夹的物理位置只能有一个，但知识天然属于多个上下文。留在项目文件夹里会被项目边界锁死，下个项目启动时想不到去翻。

### Q11. 知识区内部怎么组织？ 【作废 2026-09-16】

> **作废原因：** 「知识区」这个容器没了，改为领域。但**「边用边长」的精神保留**，且这次真要实现了——领域内分类由 LLM 自己建（`新建分类需报告` 这条同时作废）。见 [Q69](#q69-领域内分类由-llm-自建)。

**A：** 先自动建几个大分类（后端/前端/工具链/方法论），之后**边用边长**。新建分类需报告，AI 不得自行发明分类。

### Q12. 课程知识归 `20_知识` 还是 `30_素材`？ 【作废 2026-09-16】

> **作废原因：** `20_知识` 和 `30_素材` 都不存在了。课程知识按内容归领域（编程课 → `计算机/`，绘画课 → `艺术/`）。**「创作素材」不再单独存放**——它归进领域自己的位置。

**A：** 归 `20_知识`（2026-09-15 确认）。课程学到的东西就是典型的可复用知识，应与项目经验一起被检索到，而不是和绘画参考图放一起。`30_素材` 只放创作素材。

### Q13. 项目内笔记文件名要不要带项目前缀？ 【作废 2026-09-16】

> **作废原因：** 没有项目文件夹了，就没有「项目内笔记」这个类别。**所有笔记文件名统一用内容标题**。

**A：** 要（2026-09-15 确认）。每个项目文件夹下都会有 `踩坑.md`，一旦出现同名文件，`[[踩坑]]` 双向链接就产生歧义。规则：`<项目名>-踩坑.md`。

### Q14. 「其他分类」是否改名为 `30_素材`？ 【作废 2026-09-16】

> **作废原因：** `30_素材` 取消。但**这条的教训要留着**：万能兜底分类注定变垃圾桶。**新增领域时同样适用**——不允许「其他」「杂项」这类领域。

**A：** 是（2026-09-15 确认）。原方案叫「其他分类」，注定变成垃圾桶——凡没明确归属的都往里塞。改为 `30_素材` 并限定语义为「创作素材」。

---

# 四、写入流程

### Q15. 「用大模型写入」具体指什么？

**A：** 分两层：
- **第一层**：在对话中总结（人的判断力介入，降噪）
- **第二层**：按规则写入库里（追求确定性、可重复、可校验）

### Q16. 两层之间怎么衔接？

**A：** 必须有一个**结构化接口**。第一层的产出不能是散文，得带固定字段（来源 / 一句话摘要 / 建议分类 / 关联笔记）。否则第二层没有判断依据，「按规则」无从谈起。

### Q17. 写入的节奏？

**A：** **随手记草稿 + 末尾转正**。会话中说「记一下」即落草稿，不打断思路；末尾统一转正。

### Q18. 要不要草稿区 / 收件箱？

**A：** 要。收件箱是**所有未定稿内容的统一入口**，agent 投递的和人手写的共用同一个 `00_收件箱/`，不分开——本质状态一致，分开只会让人每次纠结「该放哪」。收件箱**空的才是健康状态**。

### Q19. 转正前要不要逐条确认？

**A：** 不要。**报告是事后通知，不是事前审批单**。服务把活干完 → git commit → 给报告。用户扫一眼，不对再按 commit 回退。逐条拍板太麻烦。

### Q20. 归不了类的内容怎么办？

**A：** 不阻塞。**不进正式区**，留在 `00_收件箱/待归类/`，报告里列出。可当天处理，也可攒着集中看。AI 不得自行发明分类。

### Q21. 允许产生孤儿笔记吗？

**A：** 不允许。每条正式笔记至少链到一处已有笔记或 MOC 页。孤儿是检索不精准的根因。

### Q22. 投递后能不能反悔？

**A：** 能，且必须能。异步缓冲的意义就是留反悔窗口。收件箱条目在整理前必须**可编辑、可删除、可合并**。否则错的结论只是被延后固化。

---

# 五、字段与模板

### Q23. 「我的判断」字段是谁的判断？

**A：** **用户的判断**（字段名已由「我的判断」改为「用户的判断」）。内容须来自对话中用户说过的话。

**硬规则：用户未表达观点时该节留空，AI 不许代填。** 代填等于伪造用户观点，将来用户回看时分不清哪句是自己想的。

**怎么保证（2026-09-15 补充，Q58）：靠服务机械清空，不靠提示词。**

首次真实 LLM 运行就违反了这条：用户投递的只是一句事实陈述（「并发写入会锁表，用队列串行化解决」），模型却写了一整段「用户认为……」，还追加了两点用户没说过的补充。

加一条提示词硬规则后再试，**仍然被代填**——模型面对一个空标题的默认行为就是填满它，而「用户陈述的事实」与「用户的观点」这条界线对它太模糊。

最终定案：**服务在落盘前机械清空该节正文，只保留标题**（`planning.strip_user_judgment`），由用户自己在 Obsidian 里写。

- 机制上不可能伪造，不依赖模型自觉
- 代价：用户在对话里真说过的观点也不会被自动带过去——**刻意的取舍**，这一节的意义就在于它是用户亲手写的
- 提示词里仍保留一条（规则 8）说明「写了也会被丢掉」，避免模型浪费输出

### Q24. `类型` 字段的枚举？

**A：** `概念 | 踩坑 | 决策 | 经验 | 清单 | 日志 | 索引`。固定枚举，不许自由发挥——字段一旦失控，Dataview 查询和后续自动化全部失效。

其中 `日志`（Q50）和 `索引`（Q56）都属于**结构性文件**而非知识笔记；Dataview 查询知识时可按类型过滤掉。

- `日志` 于 2026-09-15 随 Q50 加入
- `索引` 于 2026-09-15 随 Q56 加入

后两者由**服务生成**，不由 LLM 产出——所以它们不在给模型的允许类型列表里。

### Q25. 检索主力是文件夹还是标签？

**A：** **`主题` 标签**。文件夹决定「物理放哪」，标签决定「属于哪些主题」。一篇笔记能挂多个标签，不能放多个文件夹。阶段一靠 Obsidian 搜索（`tag:#数据库`），阶段二作为 RAG 切片聚合依据。

> **2026-09-16 补注：结论不变，而且更重要了。** 新结构（[Q67](#q67-目录结构重订)）以领域为一级目录、领域内分类由 LLM 自建，**文件夹会自己长**——正因为如此，「检索不靠文件夹」这条成了容错的前提：文件夹放错了只影响人翻目录，不影响检索。
>
> **但有个连带问题没定：** `主题` 标签的词表现在**就是目录名**（`knowledge_topics` 读 `20_知识/` 下一层）。领域化之后标签从哪来——路径派生、LLM 另提、还是两者都要？**见第十二节待解决 #1。**

### Q26. 模板文件放哪？

**A：** 先放**服务代码仓库里**，但以 **`.md` 形式**存在，代码读取。便于修改。

---

# 六、系统架构

### Q27. 谁来整理知识库？

**A：** **独立服务 + 自带 LLM（API key）**，专职整理，不占用各会话的额度与上下文。各会话 agent 只负责投递，不懂整理规则。

关键转变：把「整理」从**会话行为**变成**服务行为**，规则收拢到服务一处维护。

### Q28. 接口形式？

**A：** 三层，共用同一份逻辑：

| 层 | 谁用 | 通用性 |
|---|---|---|
| **CLI** | 任何能跑 shell 的 harness | **最通用** |
| **HTTP REST** | 任何能发请求的工具 | 底层，另两层都调它 |
| **MCP server** | 支持 MCP 的工具（Claude Code、Codex CLI） | 体验最好，覆盖有限 |

目标：接入市面上大多数工具（Claude Code、DeepSeek harness、Codex 等）。
**CLI 才是最通用的一层**，因为「能跑 shell」几乎是所有 agent harness 的底线能力。

### Q29. 写入如何保证一致性？

**A：** 所有写入**必须经过服务进程**，CLI 和 MCP 都走 HTTP，不直接碰文件。服务没启动时 CLI 报错并提示，而不是自己偷偷写。这样写入天然串行化，不会出现两份逻辑打架。

### Q30. 各 harness 怎么知道何时投递？

**A：** 定案（2026-09-15）。这个问题实际包含**两半**，阻塞性不同，分开处理。

#### ① 接口侧 —— 阻塞最小闭环

**命令形态：**

```bash
kb push --content "并发写入会锁表，最后用队列串行化解决" \
        --project "电商后台" --source 会话

# 长内容走 stdin，避免转义麻烦
echo "..." | kb push --project "电商后台"
```

- **一次投递 = 一条草稿**，多条就调多次。CLI 越简单，agent 越不容易用错。
- 返回：`已收，id=20260915-a3f2`。异步立刻返回，不阻塞会话。

**投递内容格式：agent 只交正文，其余服务推断。**

- 必填：正文
- 可选：`--project`、`--source`
- **禁止让 agent 填 `类型` / `主题` / 建议分类** —— 那等于把整理判断泄漏到每个会话，与服务收拢规则的设计相悖。

**项目名映射规则：** 【前半段保留，后半段作废 2026-09-16】

- agent 报项目名，**默认取 git 仓库根的目录名**（`git rev-parse --show-toplevel` 的 basename），**不是 cwd 的 basename**——避免 agent 停在子目录（如 `src/kb/core/`）时取到 `core`。非 git 仓库时回退到 cwd basename。 ← **保留**
- **允许没有项目**：`--project` 可选。 ← **保留**（但「留空则走 `20_知识/`」作废，现在一律按内容归领域）
- **匹配前规范化**：统一转小写、`-` 与 `_` 视为等价，避免 `KN_Base` / `kn_base` / `KN-Base` 被当成三个项目。 ← **保留**（仍用于标签去重）
- ~~**服务按规范化名字在 `10_项目/` 下搜唯一匹配**~~ ← **作废 2026-09-16**。项目降为标签（[Q68](#q68-项目降为标签)），不再有项目目录，也就不存在「搜唯一匹配 → 挂到该项目」。`项目` 只是 frontmatter 里的一个字段和标签，**不影响笔记的物理位置**。
- ~~**从不自动新建项目文件夹**~~ ← **作废**。没有项目文件夹可建了。
- **`00_收件箱/待归类/` 的去向不变**，但路径改为 `_收件箱/待归类/`（[Q70](#q70-功能目录去编号)）。

#### ② 提示词侧 —— 不阻塞，且现在写太早

每个 harness 一份说明（Claude Code → skill；Codex → `AGENTS.md`；其他 → 通用说明），放 `docs/接入指南/`。

**但必须等接口稳定并跑通之后再写。** 接口没定稳就写说明书，必然是照着猜写的，接口一改全部过时。说明书应当是「描述既成事实」，不是「承诺未来行为」。

> 注：旧的「用户级 skill + 一行指针」方案是上一版架构（整理在会话内完成）的产物，**已作废**。新架构下 agent 只需会调 CLI。

#### 遗留 【作废 2026-09-16】

- ~~🔴 **项目目录改名后**（如 `KN_Base` → `KB_Engine`），vault 里已存在的项目文件夹是否跟改？~~ —— **问题本身消失了**。项目降为标签（[Q68](#q68-项目降为标签)），改名只需改 frontmatter 字段，不牵动任何路径和链接。

### Q31. 管理界面？

**A：** **本地 Web UI**，一个 `.bat` 启动，模块化设计。首批功能：配置页、工作记录。其他功能后续再说。

内容侧界面（阅读、反链、工作记录看板、待归类看板）**由 Obsidian + Dataview 承接**，不重复造。

### Q32. 密钥与配置放哪？

**A：** **用 `.env` 存在工程根目录（`KN_Base/.env`）**（2026-09-15 确认）。不拆到用户目录，保证配置集中在一个文件夹里处理。

- `.env` **不入库**——`.gitignore` 已含 `.env` / `.env.*` 规则
- **`.env.example` 必须入库**（`.gitignore` 中已用 `!.env.example` 放行），列出全部配置项。**密钥留空待填，其余给出可用默认值**——换机器或重装时照着 example 填一份就够，不用翻代码找配置项，也不用猜默认值是什么
- **绝不能进 vault**。`E:\KB_Library` 是 git 仓库，写进配置会被提交进历史，删掉也还在

**配置项（初稿）：**

| 键 | 必填 | 说明 |
|---|---|---|
| `KB_LLM_API_KEY` | 是 | LLM 的 API key |
| `KB_LLM_BASE_URL` | 是 | API 地址 |
| `KB_LLM_MODEL` | 是 | 模型名（deepseek-flash 起步；具体 id 待接通知时确认） |
| `KB_VAULT_PATH` | 否 | vault 路径，默认 `E:\KB_Library` |
| `KB_PORT` | 否 | 指定端口；留空则自动寻找 |

> 原方案的 `~/.kb/config.toml` **已作废**——它把配置拆到了用户目录，与「配置集中在一个文件夹」冲突。

### Q49. 自动寻找到的端口，CLI 怎么知道？

**A：** 定案（2026-09-15）。服务启动后在工程目录写一个**运行时文件**：

```
KN_Base/runtime/service.json     # { "port": 51723, "pid": 12345, "started": "..." }
```

CLI 的流程：读 `runtime/service.json` → 拿端口去探测 → 通就直接投递；不通（或文件不存在）就后台拉起服务、等服务写好文件、再投递。

这个文件**不入库**（加入 `.gitignore`）。放在工程目录而非用户目录，同样是为了「配置集中在一个文件夹」。

### Q50. 工作日志（整理日志）怎么办？

**A：** 定案（2026-09-15）。

**位置与粒度**：`_索引/整理日志/2026-09-15.md`，**按天聚合**——每次整理往当天那篇追加一节。

理由：调试期一天可能整理十几次，按次会产生一堆碎片文件；按天回看也顺。与「一次整理 = 一个 commit」不冲突，commit 里包含日志文件的那次追加。

**内容**（带 wiki 链接，Obsidian 里可点击跳转）：

```markdown
## 14:32 整理 3 条草稿

- 新建 [[并发写锁]]
- 合并入 [[XXX-踩坑]]
- 更新 [[40_索引/后端]]
- 待归类 1 条：……
```

**失败也记**。日志是审计记录，只记成功的话，回头看会不知道当时为什么有东西卡在收件箱。

**报告与工作日志同源**：服务产生一份「整理结果」结构，然后两个出口——渲染成文本给会话（报告），渲染成 markdown 进 vault（工作日志）。**不写两套逻辑**，否则会出现报告说「新建 3 篇」而日志写 4 篇这种对不上的情况。

**`类型` 字段取 `日志`**，因此枚举扩充为 6 个值（见 Q24）。

### Q51. 运行日志怎么办？

**A：** 定案（2026-09-15）。`KN_Base/logs/kb.log`，标准 Python logging，按大小轮转保留若干份，默认 INFO、可配 DEBUG。`logs/` 加入 `.gitignore`。

**核心约束：运行日志绝不能进 vault。** 一旦进去就成了笔记，会被检索、被 RAG 切片、被 AI 当作知识。技术日志是噪音，属于垃圾进垃圾出。

**两者不能混为一谈：**

| | **工作日志** | **运行日志** |
|---|---|---|
| 是什么 | 「什么时候整理了什么」，审计记录 | debug / info / error，技术排查用 |
| 谁看 | 用户 | 排查故障时 |
| 是不是知识 | 是，可被检索、阶段二可被 RAG 召回 | 不是 |
| 住哪 | vault 内（`_索引/整理日志/`） | vault 外（`KN_Base/logs/`） |
| 保留 | 长期 | 轮转，保留 N 份 |

> **2026-09-16 扩展：日志从两层变三层。** 新增**流程日志**（每一个流转：谁发了什么命令、分给谁、谁做了什么、怎么返回），落在服务侧存储，**同样不进 vault**。整理日志与流程日志在 Web UI 上同一页展示（卡片颜色区分），但**存储必须分开**。见 [`01_架构.md`](01_架构.md) 第十节。

### Q52. 测试策略？

**A：** **全 mock LLM**（2026-09-15 确认）。LLM 调用全部替换为返回固定变更计划的假实现，在此基础上对校验层、落盘层写真实测试。

- 快、免费、稳定，可随时跑
- 真实 LLM 的效果靠手动端到端验证

**理由：** 真实 API 慢、花钱、结果不稳定，不适合作为自动化测试的基础。而整理逻辑的正确性（校验、落盘、回滚）本就不该依赖具体模型的输出——这正是 Q33 把 `llm/` 做成抽象层的原因。

### Q53. vault 的 `.obsidian/` 要不要入库？

**A：** **部分入库**（2026-09-15 确认）。

- **入库**：`app.json`、`community-plugins.json`、插件配置等——换机器时设置基本能跟着走
- **忽略**：`workspace.json`、`cache` 等频繁变动的文件

**理由：** `workspace.json` 记录「哪个面板开在哪」，每次开关 Obsidian 都会变。全部入库会让 git 历史被这种噪音淹没，真正的内容变更反而难找。

### Q54. LLM 怎么调？

**A：** **`openai` SDK + 兼容端点**（2026-09-15 确认）。`base_url` 指向 DeepSeek 的 OpenAI 兼容端点。

**理由：** Q35 要求「后续兼容其他模型」。openai SDK 已是事实标准，市面上多数供应商都提供兼容端点——将来换供应商只改 `base_url` 和模型名，`llm/providers/` 的代码几乎不动。

### Q55. 草稿文件的格式与流转？

**A：** 定案（2026-09-15）。

**文件名 = id**：`00_收件箱/20260915-a3f2.md`
id 规则：`YYYYMMDD-` + 4 位随机十六进制。用 id 而非「日期+摘要」作文件名——id 是对外接口（`kb organize <id>`），文件名与之一致最不容易出错；草稿生命周期短，可读性由 Dataview 看板提供。

**内容 = frontmatter + 正文原样：**

```markdown
---
状态: 待整理
id: 20260915-a3f2
来源: 会话
项目: 电商后台
投递时间: 2026-09-15 14:32
---

（agent 投递的正文，原样保存）
```

**正文绝不改写** —— 改写属于整理阶段。投递阶段服务只负责落盘，保证投递是纯粹的、无判断的，否则投递行为就不可预测了。

字段中没有 `类型` / `主题` —— 那些是整理阶段才产生的（见 Q30：agent 不许填，服务在整理时推断）。

**两种「卡住」的流转：**

| 情况 | 原因 | 去向 |
|---|---|---|
| 整理失败 | LLM 报错 / 格式不合规 | 原样留在 `00_收件箱/`，下次重试 |
| 归不了类 | LLM 正常但判断不了归属 | 移到 `00_收件箱/待归类/` |

**`kb organize` 扫描 `00_收件箱/` 及其 `待归类/` 子目录，且每次重新尝试。** 理由：库会长大——三个月前归不了类的笔记，可能因后来新增了相关笔记而变得可归类。不重试就永远卡着。重试失败的继续留在 `待归类/`，代价只是一次 LLM 调用。

### Q56. 索引页（MOC）从哪来？

**A：** 定案（2026-09-15）。**服务在需要时自动创建** `_索引/<主题>.md`，内容是 Dataview 查询——零维护，永远自动列出该主题下的所有笔记，不需要人工往上加条目。

> **2026-09-16：机制保留，但「按什么出索引页」待定。** 领域化之后，索引页是按领域出（`_索引/计算机.md`）、还是按领域内子分类出？**见第十二节待解决 #4。**

**为什么必须自动建：** Q21 定了「不允许孤儿笔记」，但**空库第一天没有任何笔记可链**，第一条笔记必然违反该约束。索引页自动生成后，第一条笔记就有东西可链，约束从第一天起成立。

**幂等：** 已存在的索引页原样返回，**不覆盖**——用户可能自己改过。

**`类型` 字段取 `索引`**，枚举扩充为 7 个值（见 Q24）。

索引页由**服务生成，不由 LLM 产出**——它不在给模型的允许类型列表里。是否需要新建索引页，由服务在落盘时按「frontmatter 的 `主题` 是否对应既有主题目录」决定。

### Q57. 依赖版本怎么管？

**A：** 定案（2026-09-15）。**顶层依赖写下界 + 关键包上界。**

`requirements.txt`：

```
fastapi>=0.141
uvicorn>=0.53
jinja2>=3.1
python-dotenv>=1.0
openai>=3.14,<4
python-frontmatter>=1.3
httpx>=0.28,<1
pytest>=9,<10
```

**背景：** 原计划 Step 2 写「版本先不锁，装完后用 `pip freeze` 回填」，Step 12 却把 freeze 产物 `requirements.lock.txt` 加进了 `.gitignore`。两条合起来的效果是**顶层依赖永远不锁**，「回填」这个动作没有归属。这是计划自身的矛盾，在 Task 1 的代码质量审查中被发现。

**为什么只给 `openai` 加上界：** 大版本破坏性变更最容易发生在 `openai` 这类快速演进的 SDK 上（当前 3.14，跨到 4.x 是迟早的事）。其余几个 API 相对稳定，加下界防「装到过老的版本」即可，加上界反而会挡住安全补丁。

**锁文件：** `requirements.lock.txt`（`pip freeze` 产物）继续不入库——它含平台相关包，跨平台没有意义。

### Q33. 为什么 `core/` 不许依赖 web/api？

**A：** 硬约束。整理是这套系统最容易出错的部分，必须能脱离界面单独跑测试。`core/`（领域逻辑）不 import `web/` 或 `api/`，`llm/` 是抽象层，`api/` 和 `web/` 都是只做协议转换的薄层。

### Q48. vault 里要不要放 CLAUDE.md？

**A：** **不放**（2026-09-15 确认）。vault 保持**纯数据**——只有 markdown 文件、目录结构和 `.obsidian/` 配置，**不含任何规则或说明**。

理由：

- **服务不读它**。服务的规则在代码和 `templates/` 里（见 Q26），与 vault 中的文件无关。
- **不产生会话**。整理走 API key，人通过 Obsidian 读写，不在 vault 目录开 Claude Code 会话，没有会话需要加载它。
- **两份约定会漂移**。vault 一份、服务代码一份，改了一边忘了另一边，就会留下**一份没人读又会说错话的文档**——比没有更危险。

边界因此变得清晰：**数据在 vault，规则全在服务侧。** 换库、重建库都不牵动规则。

> 原设计（Q1 阶段）的「vault 内 CLAUDE.md」是上一版架构的产物——那时整理发生在 vault 会话里。架构改为独立服务后，它失去存在理由，**已作废**。

---

# 七、本轮新增决策

### Q34. 分期范围？

**A：** **先最小闭环**：vault 骨架 + 服务核心 + CLI 投递 + 异步整理。先把「投递 → 整理 → 落盘」跑通。Web UI、MCP、撤回界面排在其后。

理由：整理质量是成败点，得先能跑起来看到真实效果，再包界面。

### Q35. LLM 选型？

**A：** **deepseek-flash**，API key 由用户自己填。**后续要兼容其他模型**，因此 `llm/` 必须是抽象层，换供应商不改上层。

### Q36. 查重怎么判？

**A：** **两段式**：
1. **预筛（便宜）**：`主题` 标签重叠 + 标题关键词，从全库捞候选（5-10 篇）
2. **判定（LLM）**：只把候选笔记的「一句话结论」和新内容一起给模型，判：完全重复 / 相关 / 全新

优点：**便宜**（只喂候选，非全库）、**可解释**（能说出为何判重）、**可演进**（阶段二把预筛换成向量召回，判定层不动）。

### Q37. 整理由谁触发？

**A：** **只能由用户触发**（2026-09-15 确认）。用户在会话中明确说「这条可以整理了」，agent 才调用工具触发整理。

与异步机制咬合：投递时先搁置（留反悔窗口），用户确认后才整理。

**硬约束：agent 不得自行判断某条总结「已经稳定了」就触发整理。** 否则与「用户决定」矛盾，异步缓冲也就失去意义。

> **2026-09-16 修订：本条的结论已被推翻。** 现定为**整理触发权归会话 AI**——时机由会话 AI 判断（暂停项目时、半天未整理等），用户也可主动发起；**发请求前必须先确认没有需要推翻的结论**。理由：这条最初防的是「把还没想清楚的东西固化」，而那个风险现在由那道前置确认承担，交给会话 AI 判断更省事。**触发条件写进会话层接入说明（见 [Q62](#十一架构扩张决议2026-09-15-第二轮)）。** 原文保留，供回看当初的顾虑。

### Q38. 整理是单条还是批量？

**A：** 两者都提供（2026-09-15 确认）——`kb organize` 整理全部待整理，`kb organize <id>` 只整理某条。

### Q39. 服务是常驻还是按需启动？

**A：** **按需自动拉起**（2026-09-15 确认）。

CLI 检测到端口无响应时，自动后台启动服务进程再投递。用户无需关心服务开没开，也不用开机常驻。

这样解决了「按需启动」与「各会话 agent 随时能投递」的矛盾：对用户是按需的，对调用方是无感的。

### Q40. 端口号？

**A：** 自动寻找未占用的端口。

### Q41. Web UI 用静态页面还是模板引擎？

**A：** **模板引擎打底 + 少量 JS 增强**（如 Jinja2）。

理由：配置页是表单、工作记录是列表，都是「读数据展示」型，模板引擎写起来快得多；**不需要引入前端构建链**（无 npm/vite），服务端单进程，符合「管理简单」。只有需要实时刷新的地方（如整理进度）才加 JS 轮询。

### Q42. Python 环境搭了吗？

**A：** 还没搭，可以用。环境名与版本见 Q2（已定：`kn_base` / Python 3.11）。

---

# 八、延后项

> 注：Q45（失败兜底）、Q46（git 提交策略）原列于此，已于 2026-09-15 **定案**，见对应条目。

### Q43. 阶段二 RAG 的具体设计？

**A：** 延后。已约定分阶段（先人搜，后 RAG），细节到时再谈。

### Q44. 是否支持多个知识库？

**A：** 延后。目前只有一个库（`E:\KB_Library`），但配置放 vault 外暗示了多库的可能性。

### Q45. 整理失败的兜底策略？

**A：** 定案（2026-09-15）。**核心手段：把 LLM 调用和文件写入彻底分开。**

整理拆成三段，前两段一个字节都不写：

**① 规划（只读）**——调 LLM 产出完整**变更计划**：哪些笔记新建、哪些改写、哪些链接要加、哪些 MOC 要更新。不碰 vault 文件。

**② 校验（只读，纯静态检查）**——不靠 LLM 自我保证，机器校验：
- `类型` 是合法枚举值吗
- 引用的笔记、分类真实存在吗
- 目标路径合法、无冲突吗
- 计划自相矛盾吗（同一条草稿被规划进两处）

校验不过就打回重来，**vault 一个字节没动**。这一步拦掉绝大多数幻觉类错误。

**③ 落盘（写入，不调 LLM）**——按计划执行纯文件操作。因为不调模型，是确定性的、快的、失败率极低的。「LLM 调到一半留半成品」不会发生，因为规划阶段根本不写。

**失败分级：**

| 失败类型 | 处理 |
|---|---|
| LLM 超时/限流/网络 | 指数退避重试 3 次 |
| 返回格式不合规 | 把错误信息喂回去让它自己修，重试 2 次 |
| 仍失败 | **按条隔离**——该条留在收件箱原样不动，其余条正常整理 |

按条隔离的意义：10 条草稿里 1 条判不了，不该拖垮另外 9 条。失败条目**什么也不写**（保持原样），所以下次重试天然幂等。

**回滚必须精确，不能用 `git checkout .`** ——那会把用户手动编辑、尚未提交的改动一起还原掉。

> 服务自己维护一份「本次操作的文件清单」：修改过的 `git checkout -- <文件>` 还原，新建过的直接删除。用户的未提交改动不受影响。

**崩溃残留：** 服务启动时检查 vault 工作区，若发现本次操作清单残留（= 上次落盘没跑完），**报告给用户决定**回滚还是保留，不自动处理——因为存在用户有未提交改动的可能，自动处理有风险。

### Q46. vault 的 git 提交策略？

**A：** 定案（2026-09-15）。**一次整理 = 一个 commit。**

理由：这直接支撑「报告是事后通知」的前提——收到报告后不满意，回退这一个 commit，整次整理干净撤销。若一篇笔记一个 commit，一次整理 20 篇就是 20 个 commit，历史全是噪音，回退还得挑。

Commit message 用中文，列出本次动了什么：

```
整理草稿 3 条

- 新建 计算机/git/暂存区是全有或全无.md
- 新建 艺术/透视/一点透视.md
- 更新 _索引/计算机.md
```

> 路径示例已按新结构更新（2026-09-16，[Q67](#q67-目录结构重订)）。原文是 `20_知识/后端/…`、`10_项目/XXX/XXX-踩坑.md`、`40_索引/后端.md`。

**配套三条规则：**

**① 服务只 `git add` 自己动过的文件，禁止 `git add -A`。**
否则会把用户正在编辑、尚未提交的笔记一起裹进这次「AI 整理」的 commit，历史就骗人了。

**② 服务使用独立的 git author（`kb-service`）。**
`git log --author=kb-service` 能一眼分出「AI 写的」和「我写的」，回顾和排查都用得上。

**③ 落盘前记录 HEAD，失败时按文件清单精确回滚**（见 Q45），不回滚清单之外的东西。

### Q47. 附件与媒体策略？

**A：** 延后。用户提过「图片视频用链接和文件路径写入」，但手写路径会在移动文件时断链（Obsidian 的 `![[文件名]]` 不会）。具体规则未定。

> **2026-09-16 状态：已定案，不再是延后项。** 用户 2026-09-15 决定：**图片视频记绝对路径**，依赖「个人习惯不会轻易挪动文件」这个前提。附件目录从 `90_附件/` 改名为 `_附件/`（[Q70](#q70-功能目录去编号)）。

---

# 九、已定案汇总

> **这张表只记「已定案」。「待解决」在文末第十二节。**
> 原表（2026-09-15 版）里有几条已在 2026-09-16 作废：Q12（课程归属，`20_知识` 没了）、Q13（文件名前缀，项目文件夹没了）、Q14（`30_素材` 没了）、Q37（触发主体已修订）、Q47（已定案，不再是延后项）。

**基础设施与流程（不受结构重订影响）：**

| 编号 | 问题 | 结论 |
|---|---|---|
| Q2 | Python 环境 | `kn_base` / Python 3.11 |
| Q23 | `用户的判断` 不许代填 | 服务落盘前机械清空，不靠提示词 |
| Q27 | 谁来整理 | 独立服务 + 自带 LLM |
| Q28 | 接口形式 | CLI / HTTP / MCP 三层，共用一份逻辑 |
| Q29 | 写入一致性 | 一律经过服务进程 |
| Q38 | 整理粒度 | 单条 + 批量都提供 |
| Q39 | 服务启动方式 | 按需自动拉起 |
| Q45 | 整理失败兜底 | 规划 / 校验 / 落盘三段式，按条隔离 |
| Q46 | vault git 提交策略 | 一次整理 = 一个 commit |
| Q52 | 测试策略 | 全 mock LLM |

**架构扩张（2026-09-15 起）：**

| 编号 | 问题 | 结论 |
|---|---|---|
| Q57 | 第二层形态 | LLM 链式工作流，agent 之间无交流 |
| Q58 | 审核在哪 | 服务内，链上的一步 |
| Q59 | 修改请求怎么表达 | `--revise <文件名>` |
| Q60 | 失效内容怎么处理 | 保留 + 打失效标记 + 默认排除出检索 |
| Q61 | Web UI 时机 | 界面与阶段一并行，功能后置 |
| Q62 | 整理触发条件写在哪 | 会话层接入说明 |

---

# 十、下一步

**当前状态（2026-09-16）：目录结构正在重订。** 最小闭环（投递 → 整理 → 落盘）已实现并端到端验证通过（258 测试全绿），代码在分支 `feat/minimal-loop`。但**vault 里所有笔记都是测试数据，全部作废**，不做迁移。

**阶段划分已取消（2026-09-16）。** 原五阶段计划写在 `规划总纲.md`（该文件已删）。**改为一串按依赖顺序排的实现步骤**，见 [`01_架构.md`](01_架构.md) 第十三节。理由：阶段划分假设了「每阶段产出可用的东西」，而实际上这些改动互相咬合、拆成阶段反而多出人为边界。

**文档现状（2026-09-16 合并后）：**

| 文档 | 作用 |
|---|---|
| [`01_架构.md`](01_架构.md) | **已定案的目标架构**——只写「是什么」 |
| [`02_需求.md`](02_需求.md) | **要什么**——由原「需求调查」+「需求梳理」合并 |
| [`03_问题记录.md`](03_问题记录.md) | **本文件**——设计状态的唯一真源，只写「为什么」 |
| [`04_踩坑与经验.md`](04_踩坑与经验.md) | **踩过什么、学到了什么**——不记「做了什么」 |
| `README.md` | **待重写**——仍描述旧结构（`10_项目/`、编号目录、两层日志） |

> 未讨论出结果的问题，不先动手实现。

---

# 十一、架构扩张决议（2026-09-15 第二轮）

承接需求问卷与梳理，逐条定案。**需求见 [`02_需求.md`](02_需求.md)，架构见 [`01_架构.md`](01_架构.md)。**

| 编号 | 问题 | 结论 |
|---|---|---|
| Q57 | 第二层用多 agent 协作，还是别的形态？ | **LLM 链式工作流，agent 之间无交流。** 确定性流水线，LLM 只在固定调用点上工作（规划、审核判断、标题提炼）。**没有管理 agent、没有 agent 之间的对话** |
| Q58 | 审核跑在哪个进程？ | **服务内，链上的一步。** 规则只有一份，改一处全生效 |
| Q59 | 修改请求怎么表达？ | **`--revise <文件名>` 参数**，不写 frontmatter。显式指定比让 LLM 填判断字段可靠，也不违背 Q30「agent 只交正文」 |
| Q60 | 失效内容怎么处理？ | **不删，打失效标记保留，默认排除出检索**（点时间查仍可得历史）。**失效由会话层显式指定，不由服务靠相似度猜** |
| Q61 | Web UI 什么时候做？ | **界面与阶段一并行设计**，功能后置 |
| Q62 | 整理触发条件写在哪？ | **写进会话层接入说明**（即挂着的 Q30②）。「触发条件」= 什么时候该发整理请求 |
| ~~Q63~~ | 项目目录还分个人/工作吗？ | 【作废 2026-09-16】摊平成了 `10_项目/<项目名>/`，但**下一步 `10_项目/` 整个取消了**（[Q68](#q68-项目降为标签)）。这一层白改。 |
| ~~Q64~~ | 项目笔记的文件名由谁决定？ | 【作废 2026-09-16】没有项目文件夹就没有「项目笔记」这个类别，`<项目名>-<类型>.md` 失去意义。**文件名统一内容标题**（[Q72](#q72-文件名统一内容标题)）。**但挖出的那条判断标准保留，见下。** |
| ~~Q65~~ | 一条内容该进 `10_项目/` 还是 `20_知识/`？ | 【作废 2026-09-16】只剩一个去处（领域），不用判了。**但挖出的那条判断标准保留，见下。** |
| ~~Q66~~ | 抽离之后，项目笔记那一侧留什么？ | 【作废 2026-09-16】**抽离整个取消**（[Q71](#q71-抽离取消)），这个问题随之消失。 |

### 从 Q64/Q65 里保留下来的那条判断标准

> **机械约束（文件名格式对不对）→ 代码必须管；判断题（可不可复用、该不该代填）→ 代码管不了，只能写提示词。**

**这条跨越了具体规则的存废，是本轮最有价值的产出。** 项目里已经撞上三次：Q23（`用户的判断` 代填）、Q64（文件名格式）、Q65（可不可复用），每一次都是因为没分清这两类，把机械约束当成判断题交给了模型。

### Q60 的依据：Graphiti 的反面教材

调研查到 Graphiti（Zep）的双时态失效在生产上出过事故（[synapse 的事故报告](https://github.com/alexsh88/synapse/blob/main/docs/FINDING-silent-fact-loss.md)）：

> **被自动失效的事实里 70% 其实还是真的**（28/40，手工核对）。

根因是它的机制：挑「哪些旧事实该失效」时，候选集用**语义相似度**、**不要求候选与新事实共享同一个实体**，然后一次 LLM 调用返回一串**裸索引**（无理由、无置信度），系统照时间戳算术直接落。结果同项目内**只是用词相近**的两条互相误伤，常见形态是「更窄的新结论废掉更宽的真结论」。它还有个缺陷：失效判断**不留可复核的痕迹**。

**对我们的三条约束：**

1. 失效**显式指定**（`--revise`），不猜
2. 失效**留痕**（整理日志覆盖）
3. 链上每个 LLM 调用点都要有**确定性校验兜着** —— Q45 的三段式已经是这个形状，继续按它走

### Q64 的由来：提示词拦不住约束 【作废 2026-09-16】

> **规则本身作废了**（没项目文件夹），**但这条由来值得留着**——它是「机械约束 vs 判断题」那条标准的第一次成型。



**2026-09-16 首次端到端验证跑出来的。** 摊平改动做完后投了一条草稿，落盘结果是：

```
10_项目/KN_Base/KN_Base-项目目录摊平.md      ← 实际
10_项目/KN_Base/KN_Base-<类型>.md            ← Q13/README 定的
```

**根因：** 提示词规则 7 写的是「文件名必须**带**项目前缀，如 `项目名-踩坑.md`」，只要求「带前缀」。模型照做了——加了前缀，然后自己取了个内容标题。**代码里没有任何校验。**

**为什么这是致命的：** 这条规则的全部价值在于**路径可推导**（会话层说「改 KN_Base 的决策」不用先搜就能算出路径）。内容标题由模型定，推不出来——规则就白定了。

**处理：** 文件名改为由**代码强制**：

- `planning._project_note_name_error`：文件名必须是 `<项目名>-<类型>.md`，`rpartition("-")` 拆出项目名与后缀，两者都要对
- CREATE 分支再加一条：文件名后缀必须与 `类型` 字段一致（FOLD 不校验这条——fold 不写 frontmatter）
- 提示词规则 7 同步改写，并明说「不要拿内容标题做文件名」

**这是第三次撞上同一条：** Q23（`用户的判断` 不许代填）也是光靠提示词拦不住，最后改成落盘前机械清空。**凡是「必须如此」的约束，都要能落成代码检查**——提示词负责引导，机制负责保证。

**连带后果（已确认接受）：** 每个项目每种类型只能有一个文件。项目里第二条「决策」会走 fold 追加进 `KN_Base-决策.md`，越积越厚。

### Q65 的由来：Q10 一直没有落地规则 【作废 2026-09-16】

> **规则本身作废了**（只剩一个去处），**但这条由来值得留着**——它和 Q64 是一对反面：一个该归代码管，一个该归提示词管。



**Q10 早就定了「可复用知识抽到 `20_知识/`」，但提示词里没有对应的判断规则。** 9 条硬规则只说了「路径必须落在 `10_项目/` 或 `20_知识/` 下」，**没说什么时候用哪个**。模型只能靠常识猜。

**起因是有人问「能不能在正文里加一行标注，比如 `项目-xxx`、`课程-xxx`」。** 这个想法要解决的问题是真的，但做法有三个害处：

1. **重复来源** —— `--project KN_Base` 已经在传同一个信息，两处冲突以谁为准
2. **违反 Q30** —— 正文里写字面分类，等于「把整理判断泄漏到每个会话」
3. **要定解析格式** —— 代码解析还是模型读？格式、位置、多行怎么办

**真正的缺口是规则，不是输入格式。** 加一条判断规则即可：

> 换个项目还用得上吗？用得上 → `20_知识/<既有主题>/`；用不上 → `10_项目/<项目名>/`。

并明说**「草稿带了项目名不等于必须进 `10_项目/`」**——一个项目的会话里产出的东西，多数仍是可复用知识。早期实测中，模型把「pytest 长路径坑」正确归到了 `20_知识/工具链/`，说明它本来就会判；缺的是把判据写下来，让它不靠碰运气。

**这条只能写在提示词里，不能落成代码检查**——和 Q64 正好相反。「可不可复用」是判断题，代码判不了；「文件名格式对不对」是机械约束，代码必须管。**分清这两类，再决定约束放哪。**

**真实 LLM 复验：** dataclass 哈希坑 → `20_知识/工具链/`；KN_Base 索引页边界决定 → fold 进 `10_项目/KN_Base/KN_Base-决策.md`。两条都对。

### Q66 的由来：Q10 中间漏了一步 【作废 2026-09-16】

> **作废原因：抽离整个取消**（[Q71](#q71-抽离取消)）。这段记录的是「补 Q10 漏掉的那一步」，而 Q10 本身也作废了。
>
> **留着的原因是它示范了一件事：** 文档从「抽到」跳到「互指」，中间那一步没人写过，却没人发现——因为两边看起来都自洽。**这类「A 到 B 之间的动作没写」的漏洞，只能靠一次端到端实跑才暴露。**



**Q10 原文（本文件第 86 行）：**

> **A：** **抽到独立知识区（`20_知识/`）**。项目文件夹只放项目专属档案（架构、决策、踩坑），从中提炼的通用知识进知识区，**两边用双向链接互指**。

**问题在于它从「抽到」直接跳到了「互指」，中间那一步没写——项目那一侧留什么？** 要让「两边」互指，两边就得都有文件。文档写了「两边互指」，却从没说项目侧留的是什么。`精简` 这个词在全部文档里一次都没出现过。

**2026-09-16 补全（Q66）：** 项目笔记里**留精简版**，末尾用 `[[知识笔记名]]` 双链接指向被抽走的完整版，**像引用一样**。详细描述被精简掉，完整内容进 `20_知识/<既有主题>/`。

**为什么是精简而不是原样保留：** 详细版留在项目里，就又回到了 Q10 要解决的问题——被项目边界锁死。而完全不留在项目里，项目档案就断了线索，回头看不知道「当初这里是怎么做的」。**精简版 + 引用**两头都顾上了。

**落到当前结构上的形态**（受 Q64 影响）：Q64 之后每个项目每种类型只有一个文件，所以「留精简版」不是新开一个 md，而是在 `10_项目/<项目名>/<项目名>-<类型>.md` 里**留一节**。

#### 🔴 还没定：抽离由什么触发

**这是个真问题，不是细节。** 按 Q65 的路由规则，可复用内容投递时就直接进 `20_知识/` 了，**根本不会在项目里停留**——也就没什么可抽。抽离只剩下一种适用场景：**某条内容当初看着是项目专属，后来发现换个项目也能用。**

那它由谁在什么时候发现？三种可能，未定：

- 整理草稿时顺带看一眼已有项目笔记（但整理的对象是草稿，不是既有笔记，形状不搭）
- 定期巡检项目笔记（新机制，要定频率和触发）
- 用户或会话层明确说「这条现在通用了」（最省事，但要人记得）

**没讨论出结果之前不实现。**

### 重构需求：闭环重跑 【作废 2026-09-16】

> **作废原因：** 要重跑的那条路（进项目 → 抽离）不存在了。而且**库里所有笔记确认是测试数据，不做迁移、不做重跑，直接清空重建**。

~~**现状：库里 4 篇笔记全部直落 `20_知识/`，从没走过「进项目 → 抽离可复用知识」这条路（Q10）。** 也就是说 Q10 从未被验证。阶段一的验收标准就是补上这条路：拿一个真实项目走完整条链，重跑出来的库替换掉现有 4 篇。~~

~~原文在 vault 的 git 历史里，可恢复。~~

---

# 十二、结构重订（2026-09-16）

## 定案

### Q67. 目录结构重订

**A：** **以「领域」为 vault 一级目录**，取消全部编号前缀目录。

```
E:\KB_Library\
├── 计算机/          ← 领域（人建、固定）
├── 艺术/
├── 文学/
├── _收件箱/
│   └── 待归类/
├── _索引/
│   └── 整理日志/
└── _附件/
```

**领域是「知识」，`_` 开头的是「机器目录」**——两者混在同一层，靠下划线前缀区分，Obsidian 侧边栏里一眼分得开。

**领域怎么划：借现成分类体系的思路，用日常词命名。** 查过 DDC（杜威十进分类法，10 大类）与中图法（22 大类），以及个人知识库的实践——后者一致认为**不该照搬**（「个人知识量远不如图书馆藏书，过于细化会背离简易清晰」），且反复告诫**「避免概念模糊、包含量太大的词语」**。

于是：借 DDC 的划分思路（互斥、自成一圈、粒度均匀），**用日常词命名**，不起初照搬 22 类。起手三个：

| 领域 | 装什么 |
|---|---|
| `计算机` | 代码、工程实践、工具链，以及跨领域的方法论（对应 DDC 000「计算机、信息及**总论**」） |
| `艺术` | 绘画、设计 |
| `文学` | 小说、写作 |

### Q68. 项目降为标签

**A：** **`10_项目/` 取消。** `项目` 降为 frontmatter 字段 + 标签，**不影响笔记的物理位置**——全部按内容归领域。

理由：[Q71](#q71-抽离取消) 取消了抽离，项目笔记就永远出不来。那么继续分两处存，只是徒增「这条该放哪」的判断负担。降为标签后：

- 一篇笔记可以属于某个项目，也可以不属于
- 「看 KN_Base 的全部决策」靠 `项目: KN_Base` 筛选（Dataview）
- 项目改名不牵动任何路径和链接（原 Q30 遗留的问题随之消失）

**连带作废：** Q13、Q63、Q64，以及 2026-09-16 刚做完的「摊平目录」全部改动。

### Q69. 领域内分类由 LLM 自建

**A：** 领域**内**的分类由 LLM 自己建，**不再需要「AI 不得自行发明分类」这条禁令**——改用闸门：

| 层 | 谁管 | 怎么管 |
|---|---|---|
| **领域**（一级） | 人 | 新增要**上报确认** |
| **领域内分类** | LLM 自建 | 由**审核**管 |

**但放开时机：等审核搭好之后。** 审核是阶段二才有的，所以「放开自由分类」和「投递测试」都排在审核之后。**阶段一不放开**，领域下先平铺。

**连带作废：** Q11 的「新建分类需报告，AI 不得自行发明分类」。

### Q70. 功能目录去编号

**A：** 收件箱 / 索引 / 附件保留，但**去掉编号前缀，改用下划线**：`_收件箱/`、`_索引/`、`_附件/`。

理由：编号（`00_`/`40_`/`90_`）是为了在旧的六层结构里排序；领域成为一级目录之后，排序意义消失，剩下的只有一个需求——**把机器目录和知识目录分开**。下划线前缀做到这件事，且在文件列表里自动排到最前或最后。

### Q71. 抽离取消

**A：** **取消「从项目里抽离可复用知识到知识区」这个动作。**

理由：投递时就直接分类进领域了，内容**根本不会在项目里停留**，没有东西可抽。原有的设计（Q10、Q66）整条作废。

**代价（已知并接受）：** 当初看着是项目专属、后来发现换个项目也能用的内容，不会被自动提升。**没讨论出结果之前不实现任何补救机制。**

### Q72. 文件名统一内容标题

**A：** **所有笔记文件名 = 内容标题**，不再区分「可推导」与「不可推导」。

理由：`<项目名>-<类型>.md` 那套规则的前提是「项目文件夹存在」，这前提没了。现在路径一律不可推导，**找笔记统一靠检索**——这反而简化了命名规则（原先要分两类讲）。

### Q73. 现有笔记都是测试数据

**A：** **vault 里现有的全部笔记都是测试数据，全部作废，不做迁移。** 新结构落地时直接清空重建。

用户原话：「没有正式使用前都是测试」。

### Q74. 阶段划分重订

**A：** 重订。**凡是旧结构阻碍的一律当垃圾清理**——不为兼容旧结构做任何妥协。原来那套五阶段（写在 `规划总纲.md`，该文件已删）整体作废重写。

### Q75. 领域与领域内分类，谁管？

**A：** **领域新增由人处理；领域内子分类由 AI 处理。**（确认并细化 [Q69](#q69-领域内分类由-llm-自建)）

### Q76. `主题` 标签从哪来？

**A：** **两者都要**——路径派生保证基本一致，AI 再按规则另提。**具体规则由我先生成，用户过目。**

用户原话：「最好还是让 ai 自己按规则建，处理后面会说」。

### Q77. 分类重复怎么处理？

**A：** **归旧，不新建。** 已有分类能装下就用已有的。

### Q78. 审核的具体规则？

**A：** **重写**（旧的那套建立在旧结构上）。**分类的新建交给 AI 审核。**

### Q79. `_索引/` 索引页按什么出？

**A：** **按现有目录分**（一个目录一个索引页）。**知识库定时整理时顺手更新**（见 [Q81](#q81-知识库定时整理新功能)）。

### Q80. 日志分几层？

**A：** 按之前说的处理，**两个分类**：

| 分类 | 含 | 进 vault 吗 |
|---|---|---|
| **整理日志** | 整理了什么 + 汇报总结 | **进**（`_索引/整理日志/`）——它是知识，可被检索、阶段二可被 RAG 召回 |
| **技术日志** | 流程日志（每个流转）+ 运行日志（Python logging） | **不进**——技术日志是噪音，进去会被当成知识 |

> 分界线是「**是不是知识**」，不是「谁写的」。之前讨论时按三层（整理/流程/运行）讲，这里是同一个意思的另一种切法——**进 vault 的只有整理日志**。

### Q81. 知识库定时整理（新功能）

**A：** 新增一个功能：**每周定时整理一次**。

| | |
|---|---|
| **频率** | 七天一次 |
| **起算点** | 从添加第一份知识开始 |
| **用什么** | 独立 LLM（不占会话额度，与整理草稿同一套路） |
| **整理什么** | 标签重复性（近义标签合并）· 索引目录更新 · 其他需要整理的 |

**与「投递 → 整理」的区别：** 那条链处理的是一条**新草稿**；这条巡检的是**已有笔记**——它们不会自己变，但标签会长乱、索引会过期。**这是「边用边长」的另一个半边：长出来的东西要有人收拾。**

> 具体设计（触发方式、失败怎么办、与「一次整理 = 一个 commit」怎么咬合）未定，见下方待解决 #4。

### Q82. LLM 规则由谁写？

**A：** **每个环节的 LLM 规则由我先生成，用户只过目。** 用户提建议，不当场逐条讨论。

用户原话：「针对每个环节, llm 的规则都由你先生成, 我只过目, 建议」。

**含义：** 与其让用户从零描述期望，不如我先按理解写一版**具体的**规则给他改——改一版比写一版快。**但规则必须是可读的成品，不是提纲。**

### Q83. 「与用户对接的 AI」是什么？

**A：** **是另一个入口，不是一层，也不是管理 agent。**

它是**同一个服务能力的自然语言入口**，能力和「会话层 AI」完全相同，只是走 Web 不走 CLI。

**依据是用户自己的原话**：

> 「如果分发路由能做到处理命令、返回数据给会话层 ai，那其实 **web 的 agent 和会话层的 ai 没有区别**」

**看似矛盾的地方及化解：** 用户在问卷里提过要「管理层的 ai」，而 [Q57](#q57-第二层用多-agent-协作还是别的形态) 又定了「没有管理 agent」。两句话要同时成立，只有这一种读法——

**管理 agent 当初的活是「分发任务」，那个确实不需要**（链式工作流自己会走）。但「与用户对话」是另一回事：它是一个**入口**，不是一层调度。

### Q84. 对话逻辑放哪？

**A：** **放服务里，入口层只做适配。**

```
CLI    ┐
Web    ├─→ 知识库服务 ─→ 对话能力   ← 逻辑只有一份
qqbot  ┘    （以后）
```

**理由**：和 [Q27](#q27-谁来整理知识库)「规则收拢在服务一处」同一条。如果 Web 自己实现一套对话逻辑，以后接 qqbot 就是**第二套**——两边迟早漂移。

**所以「与用户对接的 AI」不是 Web 的功能，是服务的能力；Web 只是它目前的唯一界面。**

### Q85. 随手记有哪两种形态？

**A：** **两种都要。**

| | 怎么用 |
|---|---|
| **对话式** | 在 ai对话页说给 AI 听，它投递 |
| **模板式** | 侧栏一个「**＋ 记一条**」按钮 → 选模板 → 写 → 投递 |

**侧栏那个按钮是「动作」不是「页面」**，所以在视觉上和四个导航项分开（`ai对话 / 整理日志 / 工作日志 / 运行日志` 是页面，`＋ 记一条` 是动作）。

### Q86. 模板是什么型？

**A：** **脚手架型**——问几个大白话问题，帮人把话说清；**不逼人先想清楚这是什么类型**。

整理阶段负责重组（套骨架、取标题、打标签、决定放哪）。

**理由**：「随手记」的场景就是「**几句话的事**」，不该先让人做分类判断。

### Q87. Web 里投递时能填什么？

**A：** **只有正文。**

| 字段 | 怎么处理 |
|---|---|
| 正文 | 人写 |
| `来源` | 默认 `Web` |
| `项目` | **留空**——随手记一般没有项目可填 |

**标题、类型、标签、路径一概不填**——它们全部由整理阶段生成（Q30）。

> **顺带纠正一处**：2026-09-16 合并文档时，我把原问卷 Q19/Q20 关于「管理 AI」的答案当成「做法」删掉了，`02_需求.md` 里一度一个字都没有。**那是需求，不该删**——已补回 `02_需求.md` 第二节。

### Q88. Web 投递后要不要等整理？

**A：** **不等，直接走完。** 对话式和模板式两条路都一样。

这样两条投递路的区别就清楚了：

| 谁投的 | 要不要等 | 为什么 |
|---|---|---|
| **Web（你亲手写的）** | **不等** | 你写完就想看到结果，留着等没有意义 |
| **会话层（agent 干活时投的）** | 等 | 攒着，由会话 AI 判断时机（Q62） |

**代价（已知并接受）：** Web 投的没有「反悔窗口」了。安全网退到 [Q19](#q19-转正前要不要逐条确认) 那条——**报告是事后通知，不满意按 commit 回退**。

### Q89. 工作日志（流程日志）长什么样？

**A：** 两条规矩：

**① 用自然语言写，一条就是一个动作。** 不是结构化字段。

```
14:32  你在网页上投递了一条草稿
14:32  服务收下了，编号 20260917-40da
14:32  开始整理，共 1 条
14:33  模型决定放进「计算机/git」，类型是踩坑
14:33  校验通过
14:33  落盘：新建了 计算机/git/xxx.md
14:33  提交了一个 commit：「整理草稿 1 条」
```

**② 页面形态**：粉色卡片流 + 右侧栏。

**右侧栏发的是「一条流程链，走过的高亮」**——不是文字总结。

**竖着排**——侧栏是窄的，横排六步挤不下：

```
 ✓  投递
 │
 ✓  规划
 │
 ✓  校验
 │
 ○  审核
 │
 ○  落盘
 │
 ○  提交
```

（✓ = 走过，○ = 没走到。审核只在要新建分类时才跑，所以常常是「跳过」而不是「没走到」——具体怎么标实现时再定。）

**存放**：`data/logs/`，**不进 vault**——它是过程记录，不是知识（Q80 的分界）。

### Q90. 整理日志页的右侧栏？

**A：** 手机聊天式，**发日志总结的文本**（不是流程链）。

两页的右侧栏形态一致（都是手机聊天式），**发的东西不同**：整理日志发日志，工作日志发流程链。

---

## 🔴 待解决的问题

> **原 1–8 条已答（2026-09-16，见 Q74–Q82）。** 规则类已按 [Q82](#q82-llm-规则由谁写) 写成成品，落在 [`01_架构.md`](01_架构.md)，不再列为「待解决」。下面是剩下的。

### 真待解决（没有方案）

| # | 问题 | 缺什么 |
|---|---|---|
| 1 | **存量近义分类怎么收拾** | Q77 定的「归旧不新建」只管**新建时**。已经长出的近义分类（`git`/`Git`/`版本控制`）谁在什么时候合并？没方案 |
| 2 | **流程日志记到多细** | 「每一个流转」的字段清单没定 |
| 3 | **检索方案** | 怎么实现（FTS5 / 向量 / 别的），没选型 |

### 有意延后

| # | 问题 |
|---|---|
| 4 | Q30② 各 harness 的接入说明文档——等接口稳定跑通后再写 |
| 5 | Q43 阶段二 RAG 的具体设计 |
| 6 | Q44 是否支持多个知识库 |
| 7 | **定时整理**——功能本身先放着，不设计不实现（见 [Q81](#q81-知识库定时整理新功能)） |
| 8 | Web UI 的界面设计与实现时机 |
| 9 | 可移植打包 |

### C. 有意延后

| # | 问题 |
|---|---|
| 9 | Q30② 各 harness 的接入说明文档——等接口稳定跑通后再写 |
| 10 | Q43 阶段二 RAG 的具体设计 |
| 11 | Q44 是否支持多个知识库 |

### D. 需求侧问过、你还没答的

| # | 问题 |
|---|---|
| 11 | **成功判据**——三个月后从哪件具体的事上能看出「这套有用」 |
| 12 | **打包给谁用**——别人下载就能用，还是只为你自己换机器方便 |
| 13 | **QQ bot / 手机接入**——认真要做，还是方向上有这个可能 |
| 14 | **检索要不要提前**——阶段一里结构好了、分类还不能长，这段真空认不认 |
