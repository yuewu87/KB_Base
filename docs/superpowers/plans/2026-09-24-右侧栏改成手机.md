# 右侧栏改成手机 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把四个页面（整理日志 / 工作日志 / 运行日志 / ai对话）右侧那条各不相同的栏，统一换成一台「抽屉式手机」——平时收在右边只露一条把手，点开滑出，里面有时间、两个应用、一个 `home`。

**Architecture:** 纯前端改动，**不碰 Python 一行**。四个页面共用一个 `_phone.html` 片段（各页只管把 `phone_apps` / `phone_views` 两个变量填好再 include），交互全在新建的 `static/phone.js`，样式追加进 `style.css`。**唯一的后端联动是「手机滑出来时把桌宠顶开」**——靠一个自定义事件，不让两个 JS 文件互相依赖。

**Tech Stack:** Jinja2 / 单文件 `style.css` / 原生 JS（本项目**第二个**独立 `.js`）/ pytest（只做字符串与读文件断言）

**设计文档:** `docs/superpowers/specs/2026-09-24-右侧栏改成手机-design.md`。**两边冲突时以 spec 为准**，回头改计划。

---

## 开工前必做

- [ ] **⚠️ 停掉服务。** `src/` 一改，常驻进程仍跑旧逻辑，**而且看起来一切正常**——这个坑本项目反复踩（`CLAUDE.md` 平台陷阱）。

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop
```

- [ ] **⚠️ 退回点已经有了。** 动手前打了快照 tag：**`pre-phone-sidebar-20260924`**（指向 `2d5c51c`），本地和远端都有。要整体退回：`git checkout pre-phone-sidebar-20260924`
- [ ] **⚠️ 别改 `pet.js` 的 `placeBubble` / `showBubble` / `describe`。** 桌宠**自己**那个状态气泡（`.pet-bubble`）本次**一个字不动**——用户要改的「气泡」是**手机里的消息气泡**（`.bubble`）。这两个词指两个东西，第一版设计就栽在这上面
- [ ] **计划里所有 `pytest` / `ruff` 都用绝对路径的 python**（本机 `conda run` 会报内部错误，**不要用**）：

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **每次调 `kb.bat` 都要带 `PYTHONIOENCODING=utf-8`**，否则管道里读回来全是 GBK 乱码

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/kb/web/templates/_phone.html` | 手机的壳：把手 / 时间 / 主页应用格 / 应用内容区 / home。**四页共用这一份** | 新建 |
| `src/kb/web/templates/_tabs.html` | 竖排页签 —— 四个页面都不再用了 | **删除** |
| `journal.html` / `flow.html` / `runtime.html` / `chat.html` | 各自的 `<aside>` 换成填变量 + include `_phone.html` | 改 |
| `src/kb/web/static/phone.js` | 开合、主页 ↔ 应用、时间、把「手机左边缘」广播给桌宠 | 新建 |
| `src/kb/web/static/pet.js` | **只加**监听 `kb:phone` 把自己顶开那一段 | 改 |
| `src/kb/web/static/style.css` | 追加手机样式；改 `.chat-layout` 为单列；报告气泡换新样式；清掉页签那一族 | 改 |
| `src/kb/web/templates/base.html` | `<head>` 引 `phone.js`；**删掉 160–174 行那段竖排页签的切换脚本** | 改 |
| `tests/web/test_router.py` | 三条钉页签的用例改成钉手机 | 改 |
| `tests/web/test_setup.py` | 手机在四页都在、`phone.js` 能被取到、样式块还在、气泡的 `pre-wrap` 覆盖没丢 | 改 |

---

## Task 1: 手机的壳 —— 先只换整理日志

**为什么先只换一页：** 四个页面各换一遍之前，壳本身要先在一个页面上跑通。整理日志最简单（两个应用一对一），拿它当靶子。

**Files:**
- Create: `src/kb/web/templates/_phone.html`
- Create: `src/kb/web/static/phone.js`
- Modify: `src/kb/web/templates/journal.html:35-57`
- Modify: `src/kb/web/templates/base.html`（`<head>`；`159-174` 那段脚本）
- Modify: `src/kb/web/static/style.css`（`.chat-layout`；追加手机样式）
- Test: `tests/web/test_router.py:228`、`tests/web/test_setup.py`

---

- [ ] **Step 1: 先写失败测试**

**(a)** `tests/web/test_router.py:228` 那条整个替换掉（它钉的是马上要消失的竖排页签）：

```python
def test_journal_sidebar_is_the_phone(client):
    """整理日志的右栏是那台手机，主页上两个应用：「报告」「历史」。

    这条原来是 `test_journal_sidebar_has_two_tabs`——**改的不是删的**：
    它守的意图（这一页的右栏看得见那两个东西）一字没变，换的是结构。

    「默认开报告」那半没了：手机默认停在**主页**，两个应用一块儿摆着，
    没有「哪个默认打开」这回事。
    """
    body = client.get("/journal").text
    assert 'class="phone"' in body
    assert 'data-app="report"' in body
    assert 'data-app="history"' in body
    assert ">报告<" in body
    assert ">历史<" in body
```

**(b)** `tests/web/test_setup.py` **末尾**追加：

```python
# ---------- 右侧栏的手机 ----------

def test_journal_carries_the_phone_and_its_handle(client):
    """整理日志上有那台手机和它的把手。

    ⚠️ **这条这一版只看 `/journal`。** 另外三个页面要到 Task 2 才换上——
    现在就写「四个页面都要有」的话，这条在这一步**必红**，而 Task 1 的目标
    本来就是「先在一页上把壳跑通」。Task 2 会把它扩成四个页面。
    """
    html = client.get("/journal").text
    assert 'class="phone"' in html
    assert 'class="phone-handle"' in html


def test_phone_js_is_served(client):
    """`static/phone.js` 真能被取到——本项目**第二个**独立 `.js`。"""
    r = client.get("/static/phone.js")
    assert r.status_code == 200
    assert "getElementById('phone')" in r.text


def test_the_phone_style_block_is_really_there():
    """`.phone` 那段样式在 `style.css` 里，而且是「收在右边外面」的。

    **这条挡的是本项目唯一那种「页面看着正常、测试全绿、功能其实没了」**：
    把 `style.css` 里那段 `.phone` 删掉，上面两条照样过——`class="phone"`
    还在 HTML 里、`phone.js` 还在被引——但那个 `<aside>` 会退化成一个没有样式、
    **老老实实占着右侧一列**的方块，而抽屉、把手、滑出全部失效。
    **页面上不会报任何错。**

    读文件断言的写法照抄 `test_skins.py` 那批（它们也直接读 `style.css`）。
    """
    css = (_STATIC / "style.css").read_text(encoding="utf-8")
    start = css.index("\n.phone {")
    block = css[start:css.index("}", start)]
    assert "position: fixed" in block
    # 收着的时候在视口右边外面——没了这条就成了一条常驻栏
    assert "translateX(100%)" in block
    # 层级 40：`.sidebar` 20 / 手机与桌宠 40 / `.overlay` 50 / `.skin-menu` 60
    assert "z-index: 40" in block
```

- [ ] **Step 2: 跑，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py::test_journal_carries_the_phone_and_its_handle tests/web/test_setup.py::test_phone_js_is_served tests/web/test_setup.py::test_the_phone_style_block_is_really_there tests/web/test_router.py::test_journal_sidebar_is_the_phone -v
```

预期：四条全红。前两条与第四条报断言失败，`test_phone_js_is_served` 报 404，
`test_the_phone_style_block_is_really_there` 报 `ValueError: substring not found`。

- [ ] **Step 3: 建 `src/kb/web/templates/_phone.html`**

```jinja
{# 右侧栏的手机。**四个页面共用这一份**，各页只负责把两个变量填好再 include：

     phone_apps  = [{"id": "report", "name": "报告", "href": None}, ...]
                   `href` 不是 None 时那个应用是个**动作**（比如「新会话」），
                   点它是跳转、不展开内容。
     phone_views = {"report": "<已经渲染好的 HTML>", ...}
                   只给 `href` 为 None 的那些应用准备。

   时间由 `phone.js` 填（浏览器自己的钟）——那才是手机该显示的时间，
   也省得每一页的上下文里都塞一个 `now`。

   ⚠️ **标记形式是定死的**（`phone-app` / `phone-view` / `data-app`）：
   `tests/web/test_router.py::test_runtime_page_has_no_tabs` 专门断言
   「页面上没有 `data-pane`」，为的是防「运行日志里偶然出现某个词就无故变红」
   那类坑（那条 docstring 写着「断结构，别断内容」）。**别改回 `data-pane`。** #}

<aside class="phone" id="phone">
  <button type="button" class="phone-handle" id="phone-handle"
          aria-label="打开或收起手机" aria-expanded="false">‹</button>

  <div class="phone-time" id="phone-time"></div>

  <div class="phone-screen">
    <div class="phone-home" id="phone-home">
      {% for app in phone_apps %}
        {% if app.href %}
          <a class="phone-app" href="{{ app.href }}">{{ app.name }}</a>
        {% else %}
          <button type="button" class="phone-app" data-app="{{ app.id }}">{{ app.name }}</button>
        {% endif %}
      {% endfor %}
    </div>

    {% for app in phone_apps if not app.href %}
      <div class="phone-view" data-app="{{ app.id }}" hidden>
        <div class="phone-bar">
          <button type="button" class="phone-back" aria-label="回主页">‹</button>
          <b>{{ app.name }}</b>
        </div>
        {{ phone_views[app.id] | safe }}
      </div>
    {% endfor %}
  </div>

  <button type="button" class="phone-home-btn" id="phone-home-btn">home</button>
</aside>
```

- [ ] **Step 4: 建 `src/kb/web/static/phone.js`**

```javascript
// 右侧栏的手机：抽屉开合、主页 ↔ 应用、顶部时间。
//
// **本项目第二个独立 .js 文件**（第一个是 `pet.js`）。同样是**有意**单开的：
// 它自成一个东西——自己的开合状态、自己的面板切换、还要往外广播位置。
//
// ⚠️ **颜色一律走 style.css 里的变量**，这里不碰颜色。style.css 有「变量块
// 之外不许出现十六进制」的防回潮测试守着，手机的样式同样归它管。
(function () {
  var phone = document.getElementById('phone');
  if (!phone) return;                    // 页面里没有就安静退出

  var handle = document.getElementById('phone-handle');
  var homeBtn = document.getElementById('phone-home-btn');
  var timeBox = document.getElementById('phone-time');
  var homeScreen = document.getElementById('phone-home');
  var views = phone.querySelectorAll('.phone-view');

  // ---- 顶部时间 ----
  //
  // 用**浏览器自己的钟**：手机显示的就该是「现在几点」，而不是服务端渲染那一刻。
  // 每 30 秒对一次，跨分钟时不会一直停在旧时间上。
  function tick() {
    if (!timeBox) return;
    var d = new Date();
    timeBox.textContent =
      (d.getHours() < 10 ? '0' : '') + d.getHours() + ':' +
      (d.getMinutes() < 10 ? '0' : '') + d.getMinutes();
  }
  tick();
  setInterval(tick, 30000);

  // ---- 主页 ↔ 应用 ----
  function showHome() {
    homeScreen.hidden = false;
    views.forEach(function (v) { v.hidden = true; });
  }

  function showApp(id) {
    homeScreen.hidden = true;
    views.forEach(function (v) { v.hidden = v.dataset.app !== id; });
    phone.querySelector('.phone-screen').scrollTop = 0;
  }

  phone.querySelectorAll('.phone-app[data-app]').forEach(function (btn) {
    btn.addEventListener('click', function () { showApp(btn.dataset.app); });
  });
  phone.querySelectorAll('.phone-back').forEach(function (btn) {
    btn.addEventListener('click', showHome);
  });
  if (homeBtn) homeBtn.addEventListener('click', showHome);
  showHome();

  // ---- 抽屉开合 ----
  //
  // **手机左边缘要广播出去**：桌宠靠它算自己该被顶到哪儿（`pet.js` 里
  // `kb:phone` 那段）。**用事件而不是互相 import**——两个独立的文件之间
  // 只有这一个接口，谁也不用知道对方的内部。
  //
  // ⚠️ 报的是**展开后的**位置（`innerWidth - offsetWidth`），不是当前动画到哪，
  // 好让桌宠和手机同时开始动；不这样的话手机会先压上去、桌宠再弹开，看着像穿模。
  //
  // `offsetWidth` 是**布局宽**，不受 `transform` 影响，动画中途取也是 250。
  function setOpen(open) {
    phone.classList.toggle('open', open);
    if (handle) handle.setAttribute('aria-expanded', open ? 'true' : 'false');
    window.dispatchEvent(new CustomEvent('kb:phone', {
      detail: { open: open, left: window.innerWidth - phone.offsetWidth },
    }));
  }

  if (handle) {
    handle.addEventListener('click', function () {
      setOpen(!phone.classList.contains('open'));
    });
  }
})();
```

- [ ] **Step 5: 改 `journal.html` 的 `<aside>`**

把 `journal.html:35-57` 那一整段（从 `<aside class="chat-history tabbed">` 到 `</aside>`）换成：

```jinja
  {# 右侧栏那台手机。**先在这里把两个变量填好，再 include 那个共用片段**——
     手机本身不知道任何一页的内容。 #}
  {% set report_body %}
    <h2>最近一次整理</h2>
    {% if latest %}
      {% for r in latest %}
        <div class="bubble assistant">
          <span class="bubble-at">{{ r.at[-8:] }}</span>{{ r.text }}
        </div>
      {% endfor %}
    {% else %}
      <p class="muted">还没有整理过。</p>
    {% endif %}
  {% endset %}
  {% set history_body %}{% include "_boxes.html" %}{% endset %}
  {% set phone_apps = [
      {"id": "report", "name": "报告", "href": None},
      {"id": "history", "name": "历史", "href": None},
  ] %}
  {% set phone_views = {"report": report_body, "history": history_body} %}
  {% include "_phone.html" %}
```

- [ ] **Step 6: 改 `base.html` 两处**

**(a)** `<head>` 里、`pet.js` 那行下面（第 10 行之后）：

```html
  {# 右侧栏的手机：抽屉开合 + 面板切换。本项目**第二个**独立 .js。 #}
  <script src="/static/phone.js" defer></script>
```

**(b)** **删掉第 159–174 行**那段竖排页签的切换脚本（连同它上面那两行注释），也就是这整块：

```html
  <script>
    // 竖排页签：点哪个，就只显示它对应的面板。
    // 只有几行——为这个拖一个框架进来不划算（和侧栏折叠同一个路子）。
    document.querySelectorAll('.tab').forEach(function (tab) {
      tab.addEventListener('click', function () {
        const aside = tab.closest('.tabbed');
        aside.querySelectorAll('.tab').forEach(function (t) {
          t.classList.toggle('active', t === tab);
        });
        aside.querySelectorAll('.pane').forEach(function (p) {
          p.hidden = p.dataset.pane !== tab.dataset.panel;
        });
      });
    });
  </script>
```

⚠️ **只删这一块。** 它上面、下面都还有别的 `<script>`（设置窗那一段从 217 行跑到文件末尾），删多了整页就废了。

- [ ] **Step 7: 改 `style.css` 的 `.chat-layout`**

第 509 行那一条现在是两列：

```css
.chat-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 250px;
  gap: 30px;
  align-items: start;
  max-width: none;
}
```

改成单列——**第二列那条常驻栏没了，正文放开到满宽**（这是抽屉式最大的好处）：

```css
.chat-layout {
  display: grid;
  grid-template-columns: minmax(0, 1fr);
  gap: 30px;
  align-items: start;
  max-width: none;
}
```

- [ ] **Step 8: 追加手机样式到 `style.css` 末尾**

```css
/* ---- 右侧栏的手机 ----

   四个页面共用（`_phone.html`）。它是**抽屉**：平时收在视口右边外面，只留一条
   把手探出来；点把手滑出来，再点收回。用户选的这一版，代价写在设计文档里了
   ——切「报告/历史」从 1 次点击变成 3 次。

   **颜色一个都不写死**，全走变量：用户要的就是「跟着皮肤变」。

   **层级 40，和桌宠同一层。** 全站现成的层级：`.sidebar` 20 / 手机与桌宠 40 /
   `.overlay` 50 / `.skin-menu` 60。手机和桌宠不靠 z-index 分高下——手机滑出来时
   会把它**顶开**（见 `pet.js` 里 `kb:phone` 那段），两者根本不重叠。

   ⚠️ **别给 `.shell` / `body` / `html` 加 `transform` / `filter` / `will-change` /
   `contain`。** 桌宠那套坐标（`offsetLeft/offsetTop` 相对视口）靠的就是
   `offsetParent` 为 `null`；加了就整体偏掉，而且**页面不报任何错**。 */

.phone {
  position: fixed;
  top: 14px; bottom: 14px; right: 0;
  width: 250px;
  z-index: 40;
  padding: 12px 12px 10px;
  display: flex; flex-direction: column;
  border: 1px solid var(--line-strong);
  border-right: none;
  border-radius: 24px 0 0 24px;
  background: var(--paper);
  box-shadow: var(--shadow-lift);
  /* 收着的时候整台推到视口右边外面 */
  transform: translateX(100%);
  transition: transform .46s cubic-bezier(.22, 1, .36, 1);
}
.phone.open { transform: translateX(0); }

/* 把手：**钉在抽屉左边缘、跟着抽屉走**。
   收着时（`translateX(100%)`）抽屉左边缘正好落在视口右缘，这 28px 探在外面
   ——那是它唯一能被点到的位置。钉死在屏幕右边的话，抽屉一出来就把它盖住、
   点不着收不回去（原型上就是这么栽的）。 */
.phone-handle {
  position: absolute; left: -28px; top: 50%;
  transform: translateY(-50%);
  width: 28px; height: 66px;
  border: 1px solid var(--line-strong); border-right: none;
  border-radius: 11px 0 0 11px;
  background: var(--surface);
  color: var(--ink);
  font: inherit; font-size: 15px; cursor: pointer;
}
.phone-handle:hover { color: var(--accent-ink); }

.phone-time {
  text-align: center;
  font-size: 13px;
  color: var(--muted);
  padding-bottom: 6px;
}

.phone-screen { flex: 1; overflow-y: auto; padding-top: 12px; }

.phone-home {
  display: flex; flex-wrap: wrap; gap: 10px;
  align-content: flex-start;
}
.phone-app {
  width: 62px; height: 62px;
  border: 1px solid var(--line-strong);
  border-radius: 14px;
  background: var(--surface);
  color: var(--ink);
  font: inherit; font-size: 12px; line-height: 1.2;
  display: flex; align-items: center; justify-content: center;
  text-align: center; text-decoration: none;
  padding: 4px; cursor: pointer;
}
.phone-app:hover { border-color: var(--accent); }

.phone-bar {
  display: flex; align-items: center; gap: 6px;
  font-size: 12.5px; color: var(--muted);
  padding-bottom: 8px; margin-bottom: 10px;
  border-bottom: 1px solid var(--line);
}
.phone-bar b { color: var(--ink); }
.phone-back {
  border: none; background: none; cursor: pointer;
  font: inherit; font-size: 15px; color: var(--ink); padding: 0 4px;
}
.phone-back:hover { color: var(--accent-ink); }

.phone-home-btn {
  margin: 8px auto 0;
  width: 92px; height: 26px;
  border: 1px solid var(--line-strong);
  border-radius: 13px;
  background: var(--surface);
  color: var(--ink);
  font: inherit; font-size: 12.5px; cursor: pointer;
}
.phone-home-btn:hover { border-color: var(--accent); }

/* 应用内容里的小标题（「最近一次整理」「箱子」…）。
   **这条是从 `.chat-history h2` 搬过来的**——那些 `<h2>` 现在住在 `.phone-view`
   里，`.chat-history` 一不上身它们就掉回浏览器默认的大号粗体，
   一眼就看得出不对。原样保留字号/字距/颜色。 */
.phone-view h2 {
  margin: 0 0 10px;
  font-size: 12px;
  font-weight: 600;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: var(--faint);
}
```

- [ ] **Step 9: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py tests/web/test_router.py -v
```

预期：全绿。（`test_runtime_page_has_no_tabs` 也该绿——手机的标记里没有 `data-pane`。）

- [ ] **Step 10: 跑防回潮那条（新样式里不许写死颜色）**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_skins.py -v
```

预期：全绿。**若报「变量定义之外还有写死的色值」**，就是上面那段 CSS 里混进了 `#xxx` 或 `rgba(...)` 字面量——改成变量。

- [ ] **Step 11: 起服务，亲眼看一眼**

```bash
cd "E:/Study_Projects/KN_Base" && PYTHONIOENCODING=utf-8 ./kb.bat web
```

打开 `http://127.0.0.1:51823/journal`，按 F12 看 Console（**不该有红色报错**），然后：

1. 右边只有一条把手，**看不到手机**
2. 点把手 → 手机从右边滑出来，顶部有当前时间、两个应用、底下 `home`
3. 点「报告」→ 进去，上面一条细栏写着「报告」，`‹` 和 `home` 都能回主页
4. 再点把手 → 收起，把手还在
5. 正文比原先宽了（那条 250px 常驻栏没了）
6. **「最近一次整理」那行标题是小的、淡的、带字距的**——不是浏览器默认那种
   又大又黑的 `<h2>`。它原来靠 `.chat-history h2` 上样式，搬进手机之后
   换成 `.phone-view h2`（见 Step 8 末尾那条）。**掉了的话页面不报任何错，
   只是看着不对劲**

- [ ] **Step 12: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/templates/_phone.html src/kb/web/static/phone.js src/kb/web/templates/journal.html src/kb/web/templates/base.html src/kb/web/static/style.css tests/web/test_router.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 右侧栏的手机——壳 + 整理日志先换上

抽屉式：平时收在视口右边外面，只留一条把手；点开滑出来，里面有当前时间、
两个应用、底下 home。正文因此放开到满宽（原来那条 250px 常驻栏没了）。

四个页面共用一个 _phone.html，各页只填 phone_apps / phone_views 两个变量。

把手钉在抽屉左边缘、跟着抽屉走——原型里第一版钉死在屏幕右边，抽屉一出来
就把它盖住、点不着收不回去。那是原型的错，不能照搬。

手机左边缘用自定义事件广播出去，给后面「顶开桌宠」那步用；两个 js 文件
不互相 import。

base.html 里驱动竖排页签的那段脚本删掉了——页签没了，它就是死代码。

架构见 docs/superpowers/specs/2026-09-24-右侧栏改成手机-design.md

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 2: 另外三个页面 + 清掉竖排页签

**Files:**
- Modify: `flow.html:26-84`、`runtime.html:21-24`、`chat.html:88-104`
- Delete: `src/kb/web/templates/_tabs.html`
- Modify: `src/kb/web/static/style.css`（清掉 `.tabs` / `.tab` / `.pane` / `.chat-history.tabbed`）
- Test: `tests/web/test_router.py:408`、`test_setup.py`

---

- [ ] **Step 1: 先写失败测试**

`tests/web/test_router.py:408` 那条改成：

```python
def test_flow_page_has_the_phone_with_the_chain(client, vault):
    """工作日志那台手机主页上是「流程图 | 历史」，流程图里画最近一次那条链。

    链要真走过才有——先记一条，否则 `latest` 是空的，页面画的是空态。
    """
    from kb.core.flow import emit, set_run

    set_run("20260917-1400")
    emit("投递", "投了")

    r = client.get("/flow")
    assert 'class="phone"' in r.text
    assert ">流程图<" in r.text
    assert ">历史<" in r.text
    assert ">投递<" in r.text          # 流程链上的一步
```

`tests/web/test_setup.py`：**先把 Task 1 那条 `test_journal_carries_the_phone_and_its_handle`
整个替换成下面第一条**（从一页扩到四页），再接上第二条：

```python
def test_every_page_carries_the_phone(client):
    """四个页面都要有那台手机和它的把手。

    这是 Task 1 那条（当时只看 `/journal`）扩开之后的版本——每换一个页面就多
    一个要断的对象。漏一个页面的话，那一页的右侧栏就成了**空的**：`<aside>`
    没了、手机也没来，而页面上只会显得「右边本来就没什么」。
    """
    for path in ("/journal", "/flow", "/runtime", "/"):
        html = client.get(path).text
        assert 'class="phone"' in html, f"{path} 上没有手机"
        assert 'class="phone-handle"' in html, f"{path} 上没有把手"


def test_no_page_uses_the_vertical_tabs_anymore(client):
    """四个页面都不该再出现竖排页签的标记。

    **`_tabs.html` 删掉之后，这条守着它别从哪儿又长回来。**
    页签那一族类名（`.tabs` / `.tab` / `data-pane`）如果还有一处活着，
    说明有页面没换干净——而那种漏在页面上看着只是「右栏有点怪」。
    """
    for path in ("/journal", "/flow", "/runtime", "/"):
        html = client.get(path).text
        assert 'class="tabs"' not in html, f"{path} 还有页签容器"
        assert "data-pane=" not in html, f"{path} 还有旧的面板标记"
```

- [ ] **Step 2: 跑，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py::test_every_page_carries_the_phone tests/web/test_setup.py::test_no_page_uses_the_vertical_tabs_anymore tests/web/test_router.py::test_flow_page_has_the_phone_with_the_chain -v
```

预期：三条都红——
`test_every_page_carries_the_phone` 在 `/flow` 那条挂、
`test_no_page_uses_the_vertical_tabs_anymore` 在 `/flow` 那条挂、
`test_flow_page_has_the_phone_with_the_chain` 报没有 `class="phone"`。

- [ ] **Step 3: 改 `flow.html`**

`flow.html:26-84` 那一整段 `<aside class="chat-history tabbed">…</aside>` 换成：

```jinja
  {% set chart_body %}
    <h2>最近一次</h2>
    {% if chart %}
      {# **三种状态，不是两种。** 原先这里自己在模板里算 `reached`，
         没记录的一律画成「还没走到」——于是「审核」那种「没新建分类所以
         不跑」和真「卡在前一步」长得一模一样，看的人会以为出了问题。
         `group_flow` 早把 `skipped`（流程越过了它）与 `todo`（还没走到）
         分开算好了，`.chain-step.skipped` 的样式也早就备着。 #}
      <div class="chain">
        {% for step in steps %}
          {% set state = 'done' if step in chart.reached
                          else ('skipped' if step in chart.skipped else 'todo') %}
          <div class="chain-step {{ state }}">
            {% if state == 'done' %}
              <svg class="mark" viewBox="0 0 16 16" role="img">
                <title>{{ step }}：走过</title>
                <circle cx="8" cy="8" r="7" fill="currentColor"/>
                {# 勾是画在 `--accent` 实心圆上的，所以跟 `--on-accent` 走——
                   写死白色的话，④ 荧光绿上就是白勾压荧光绿，≈1.2:1，看不见。 #}
                <path d="M4.8 8.3l2.1 2.1 4.3-4.5" fill="none" stroke="var(--on-accent)"
                      stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
              <span>{{ step }}</span>
            {% elif state == 'skipped' %}
              {# **不写「（没跑）」**（用户 2026-09-23：「很丑」）。
                 「这一步没走」靠**它挂在主线的旁边**表达：那条链是一条不断的
                 直线，路径从它旁边绕过去；被跳过的这一步挂在右边、由一小段
                 横线接着。文案只留在 title 里（读屏读得到）。 #}
              <svg class="mark" viewBox="0 0 16 16" role="img">
                <title>{{ step }}：这一步没走，流程从旁边过去了</title>
                <circle cx="8" cy="8" r="5.4" fill="none" stroke="currentColor"
                        stroke-width="1.4"/>
              </svg>
              <span>{{ step }}</span>
            {% else %}
              <svg class="mark" viewBox="0 0 16 16" role="img">
                <title>{{ step }}：还没走到</title>
                <circle cx="8" cy="8" r="6.3" fill="none" stroke="currentColor" stroke-width="1.4"/>
              </svg>
              <span>{{ step }}</span>
            {% endif %}
          </div>
          {% if not loop.last %}<div class="chain-line"></div>{% endif %}
        {% endfor %}
      </div>
    {% else %}
      <p class="muted">还没有记录。</p>
    {% endif %}
  {% endset %}
  {% set history_body %}{% include "_boxes.html" %}{% endset %}
  {% set phone_apps = [
      {"id": "chart", "name": "流程图", "href": None},
      {"id": "history", "name": "历史", "href": None},
  ] %}
  {% set phone_views = {"chart": chart_body, "history": history_body} %}
  {% include "_phone.html" %}
```

- [ ] **Step 4: 改 `runtime.html`**

`runtime.html:21-24` 那条 `<aside>` 换成（**只有一个应用**——这个页面本来就只有箱子）：

```jinja
  {% set boxes_body %}{% include "_boxes.html" %}{% endset %}
  {% set phone_apps = [
      {"id": "boxes", "name": "箱子", "href": None},
  ] %}
  {% set phone_views = {"boxes": boxes_body} %}
  {% include "_phone.html" %}
```

- [ ] **Step 5: 改 `chat.html`**

`chat.html:88-104` 那条 `<aside class="chat-history">…</aside>` 换成：

```jinja
  {# ⚠️ 「新会话」是个**动作**，不是内容面板——它是 `<a href>` 跳转，
     靠 `href` 不为 None 表达（见 `_phone.html` 顶上那段说明）。
     这是四个页面里唯一一个「应用 = 动作」的地方，别当 bug 修。 #}
  {% set chats_body %}
    {% if not chats %}<p class="muted">还没有会话。</p>{% endif %}
    {% for c in chats %}
      <a class="chat-item {{ 'active' if c.id == cid }}" href="/?cid={{ c.id }}">
        <span class="chat-title">{{ c.title }}</span>
        <span class="mono">{{ c.updated }}</span>
      </a>
    {% endfor %}
  {% endset %}
  {% set phone_apps = [
      {"id": "chats", "name": "会话历史", "href": None},
      {"id": "new", "name": "新会话", "href": "/?new=1"},
  ] %}
  {% set phone_views = {"chats": chats_body} %}
  {% include "_phone.html" %}
```

- [ ] **Step 6: 删 `_tabs.html`**

```bash
git -C "E:/Study_Projects/KN_Base" rm src/kb/web/templates/_tabs.html
```

⚠️ 删之前先确认没有别处还在 include 它：

```bash
cd "E:/Study_Projects/KN_Base" && grep -rn "_tabs.html" src/ tests/
```

预期：**没有任何输出**。有的话先改掉那处再删。

- [ ] **Step 7: 清掉 `style.css` 里页签那一族**

删掉这些规则（四个页面都不再用 `.chat-history` 了，它们全是死的）：

| 行（约） | 选择器 |
|---|---|
| 517 | `.chat-history`（sticky 那条常驻栏本身） |
| 524 | `.chat-history h2` —— **它的样式已经在 Task 1 搬进 `.phone-view h2` 了**，这条可以放心删 |
| 547 / 562 / 567 | `.new-chat` / `.new-chat:hover` / `.new-chat .icon`——「开始新会话」现在是个手机应用格 |
| 713 | `.chat-history .thread` |
| 864 / 874 / 882 / 896 | `.chat-history.tabbed`（连同它上面讲「钉顶」的注释）、`.tabs`、`.tab`、`.tab.active` |
| 904 | `.pane`——手机里的内容区叫 `.phone-view` |

⚠️ **下面这些别删，它们还有用**：

- **`.chat-history .bubble` 那一族（`715` / `730` / `734` / `741`）**——Task 3 要把它们
  改写成 `.phone-view .bubble`，删早了会连带丢掉那条 `white-space: normal` 覆盖
- `.chat-item`（`614` / `623` / `625` / `638` / `639` / `909`）——`_boxes.html` 和
  会话列表还在用它，卡片搬进手机之后照旧生效
- `.chain` 那一族（`644`–`700`、`787`–`789`）——流程链照旧
- `.bubble-at`（`777`）——报告气泡里的时间戳照旧

- [ ] **Step 8: 跑全量测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
```

预期：全绿。**若哪条红了**，多半是有别的用例还在断旧标记——先把报错读完再动。

- [ ] **Step 9: 起服务，四个页面各点一遍**

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop && PYTHONIOENCODING=utf-8 ./kb.bat web
```

`/journal`、`/flow`、`/runtime`、`/` 四个页面各走一遍 Step 11 那五条。

⚠️ **`/` （ai对话）要单独看**：点「新会话」应该是**跳转**（开一个新会话），
不是展开一个面板。另外那一页的正文里还有一串对话气泡，**那串不该有任何变化**。

- [ ] **Step 10: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add -A src/kb/web/templates src/kb/web/static/style.css tests/web/test_router.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 另外三个页面也换上手机，竖排页签整个退休

工作日志（流程图 | 历史）、运行日志（只有箱子一个应用）、ai对话
（会话历史 + 新会话）三个页面各自填好变量 include 进来。

ai对话那个「新会话」是四个页面里唯一的「应用 = 动作」：它是 href 跳转，
不展开面板，靠 phone_apps 里的 href 不为 None 表达。

_tabs.html 删掉，连带清掉 style.css 里 .tabs/.tab/.pane/.chat-history.tabbed
那一族死样式。base.html 里那段切换脚本在上一提交已经删了。

新增一条测试守着页签标记别从哪儿长回来。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 3: 手机里的报告气泡换成「聊天气泡」那款

**⚠️ 改的是**手机里**整理报告的气泡**（`.phone-view .bubble`）。
`chat.html` **主区**那串对话气泡（`.bubble.user` / `.bubble.assistant`）**一个字不动**
——用户 2026-09-24：「只在手机里」。

**Files:**
- Modify: `src/kb/web/static/style.css`（`.chat-history .bubble` 那一段改写）
- Test: `tests/web/test_setup.py`

---

- [ ] **Step 1: 先写失败测试**

`tests/web/test_setup.py` 末尾追加：

```python
def test_the_phone_bubble_keeps_normal_whitespace():
    """手机里那个气泡**必须**保住 `white-space: normal`。

    **这条挡的是一条踩过的坑，而且丢了它页面不会报错。** 全局 `.bubble` 是
    `pre-wrap`（主区对话要靠它保住用户输入的换行）；而手机里那些气泡的内容
    来自模板，标签前后的换行与缩进会被原样画出来——**每个气泡凭空多一行空白**。
    这种毛病在截图里不一定看得出来。

    原来这条覆盖挂在 `.chat-history .bubble` 上，`.chat-history` 没了之后
    必须重新落到 `.phone-view .bubble` 上。丢了就是上面那个症状。
    """
    css = (_STATIC / "style.css").read_text(encoding="utf-8")
    start = css.index("\n.phone-view .bubble {")
    block = css[start:css.index("}", start)]
    assert "white-space: normal" in block


def test_the_phone_bubble_has_a_tail():
    """那个朝下的小尖得有规则画出来。

    **断的是「规则在不在」，不是「画得对不对」**——CSS 画出来什么样，
    本项目没有浏览器测试，只能人眼看（见计划里的人眼清单）。
    但「整块被删掉」这种漏，测试挡得住。
    """
    css = (_STATIC / "style.css").read_text(encoding="utf-8")
    assert ".phone-view .bubble::after" in css
```

- [ ] **Step 2: 跑，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py::test_the_phone_bubble_keeps_normal_whitespace tests/web/test_setup.py::test_the_phone_bubble_has_a_tail -v
```

预期：两条都红，报 `ValueError: substring not found`。

- [ ] **Step 3: 改写 `style.css` 里 `.chat-history .bubble` 那一段**

把 `style.css:715-741` 这一段（`.chat-history .bubble`、`.chat-history .bubble.assistant`、
`.chat-history .bubble.user`、`.chat-history .pane .bubble + .bubble`，含它们的注释）
换成下面这一整块：

```css
/* ---- 手机里的消息气泡 ----
 *
 * **只在手机里。** `chat.html` 主区那串对话气泡（`.bubble.user` /
 * `.bubble.assistant`）**一个字不动**——用户 2026-09-24：「只在手机里」。
 *
 * 样子照用户那张草图：近直角的矩形 + 左下角一个小尖。**那个尖是个样式，
 * 不是指示器**——它不指谁、不跟谁走（原话：「模仿聊天的气泡框」）。
 * 别去纠结它指着谁，也别为它加定位逻辑。
 *
 * ⚠️ **`white-space: normal` 这条覆盖不能丢。** 全局 `.bubble` 是 `pre-wrap`
 * （主区对话要靠它保住用户输入的换行），而这里的气泡内容来自模板，标签前后
 * 的换行与缩进会被原样画出来——**每个气泡凭空多一行空白**。
 * 这条毛病在截图里不一定看得出来，测试里有一条专门守着它。
 *
 * 相邻气泡的间距比原来大（8px → 14px）：加了尾巴之后挨太近，小尖会戳到
 * 下一条的框上。 */
.phone-view .bubble {
  position: relative;
  padding: 9px 11px;
  border-radius: 3px;
  font-size: 12.5px;
  line-height: 1.55;
  background: var(--surface);
  border: 1.5px solid var(--line-strong);
  color: var(--ink-soft);
  word-break: break-word;
  white-space: normal;               /* ← 别删，见上面那段 */
  margin-bottom: 14px;
}
.phone-view .bubble + .bubble { margin-bottom: 14px; }

/* 小尖：**两个三角叠出来**——外面那个用边框色当描边，里面那个用底色。
   里面那个要**比外面那个高一点**（上沿抬进气泡边框里 3px），把气泡自己那条
   底边在尾巴处**盖断**——草图上底线在尾巴处是断开的。不盖的话会有一条横线
   从尾巴上穿过去（原型上实测过，第一版就是这样）。 */
.phone-view .bubble::before,
.phone-view .bubble::after {
  content: "";
  position: absolute;
}
.phone-view .bubble::before {
  left: 10px; bottom: -10px;
  border-left: 8px solid transparent;
  border-right: 8px solid transparent;
  border-top: 10px solid var(--line-strong);
}
.phone-view .bubble::after {
  left: 11px; bottom: -9px;
  border-left: 7px solid transparent;
  border-right: 7px solid transparent;
  border-top: 12px solid var(--surface);
}
```

- [ ] **Step 4: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py tests/web/test_skins.py -v
```

预期：全绿（防回潮那条也会跑——上面用的全是变量，没有写死色值）。

- [ ] **Step 5: 起服务，亲眼看一眼**

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop && PYTHONIOENCODING=utf-8 ./kb.bat web
```

`/journal` → 点把手 → 点「报告」：

1. 气泡是**近直角**的（不是圆的），左下角有个**朝下的小尖**
2. 小尖那儿的底线是**断开**的，没有横线穿过去
3. 气泡里**没有多出来的空行**（一条报告就一行高）
4. 相邻两条气泡之间**没被小尖戳到**
5. 切皮肤（右上角皮肤浮层）→ 气泡的框和底**跟着变**

然后回 `/`（ai对话）：**主区那串对话气泡应该和以前一模一样**，没变。

- [ ] **Step 6: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/static/style.css tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 手机里的报告气泡换成聊天气泡那款

用户第二张草图：近直角的矩形 + 左下角一个小尖。那个尖是个**样式**,
不是指示器——不指谁、不跟谁走（原话：「模仿聊天的气泡框」）。

范围**只在手机里**（用户：「只在手机里」）。chat.html 主区那串对话气泡
一个字不动。

小尖靠两个三角叠出来：外面那个当描边，里面那个用底色、比外面高 3px，
把气泡自己那条底边在尾巴处盖断——草图里底线是断开的，不盖会有一条横线
从尾巴上穿过去（原型上实测过）。

顺手把相邻气泡的间距从 8px 放到 14px：加了尾巴之后挨太近会互相戳到。

⚠️ 原来挂在 .chat-history .bubble 上的 white-space: normal 覆盖改挂到
.phone-view .bubble 上了。那条不能丢——丢了每个气泡凭空多一行空白，
而且截图里不一定看得出来，所以加了一条测试守着。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 4: 手机滑出来时把桌宠顶开

**⚠️ 只加一段，别动 `pet.js` 里别的。** 它自己那个状态气泡（`.pet-bubble`）
本次**一个字不动**。

**Files:**
- Modify: `src/kb/web/static/pet.js`
- Modify: `src/kb/web/static/style.css`（`.pet.pushing` 一条）
- Test: 无（**这一条没有自动化测试**，见 Step 4）

---

- [ ] **Step 1: 在 `pet.js` 的常量组里加一个**

`var DRAG_SLOP = 5;` 那行下面加：

```javascript
  var PUSH_GAP = 14;                   // 被手机顶开后，离它左边缘留多少
```

- [ ] **Step 2: 在 `pet.js` 里加监听 + 一个收尾函数**

在 `window.addEventListener('resize', …)` 那一段**之前**插入：

```javascript
  // ---- 手机滑出来时把自己顶开 ----
  //
  // 用户原话：「手机弹出时桌宠会被弹开，也就是类似有碰撞体积，他们在同一层级」。
  // 所以**不是盖住、也不是穿过**——手机推开多少，桌宠就让开多少。
  //
  // **收回去的时候不动它**：留在被推到的地方。追着手机跑回去的话，
  // 一开一关它就在那儿来回抽。
  //
  // 手机那边只报「我开了没、我左边缘在哪」（`phone.js` 里的 `kb:phone`），
  // 剩下全归这里算——两个文件之间只有这一个接口，谁也不 import 谁。
  var pushTimer = null;

  window.addEventListener('kb:phone', function (e) {
    if (!e.detail.open) return;                       // 收回去不追
    var want = e.detail.left - PUSH_GAP - pet.offsetWidth;
    if (pet.offsetLeft <= want) return;               // 没挨上，别动它

    if (raf) { cancelAnimationFrame(raf); raf = null; }   // 别和弹射抢
    vx = vy = 0;

    pet.classList.add('pushing');                     // 那一下的过渡（见 style.css）
    clearTimeout(pushTimer);
    moveTo(clamp(want, 0, maxX()), pet.offsetTop);
    savePos();
    // 过一会儿把类摘掉：留着的话，用户拖它的时候会拖着一条延迟
    pushTimer = setTimeout(function () { pet.classList.remove('pushing'); }, 320);
  });
```

- [ ] **Step 3: 在 `pointerdown` 里接住「推开正走到一半」**

`pet.js` 的 `pointerdown` 里，`if (raf) { … }` 那两行**后面**加：

```javascript
    // **推开那一下可能正走到一半。** 先把当前的**实际**位置钉住、再摘掉过渡，
    // 否则摘 class 的瞬间它会跳到终点（`transition` 一没，元素直接落到
    // `left` 的目标值上）。`getBoundingClientRect` 给的是视口坐标，
    // 跟 `position: fixed` 元素上 `left` 的含义一致。
    if (pet.classList.contains('pushing')) {
      var at = pet.getBoundingClientRect();
      pet.classList.remove('pushing');
      pet.style.left = at.left + 'px';
      pet.style.top = at.top + 'px';
    }
    clearTimeout(pushTimer);
```

（`pushTimer` 在 `pointerdown` 里可见——它在同一个 IIFE 里、且在 `pointerdown`
之前声明。**别把它挪到这几个监听器后面去**，`var` 虽然提升，但赋值不会。）

- [ ] **Step 4: 在 `style.css` 末尾追加过渡**

```css
/* 手机把桌宠顶开那一下。**只在推开的时候挂上**（`pet.js` 加类、300ms 后摘掉），
   平时不能有——拖动要跟手、弹射是自己的物理循环，挂着 `transition` 两者都会拖泥带水。

   振幅带一点过冲：用户原话是「会被**弹**开」。 */
.pet.pushing { transition: left .3s cubic-bezier(.22, 1.4, .36, 1); }
```

- [ ] **Step 5: 跑全量 + 风格检查**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

预期：全绿。**JavaScript 的语法错误它测不出来**——所以下一步别跳。

```bash
node --check src/kb/web/static/pet.js && node --check src/kb/web/static/phone.js
```

预期：**没有任何输出**（exit 0）。有报错就照行号改。

- [ ] **Step 6: 起服务，亲眼看一眼（这一条没有自动化测试）**

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop && PYTHONIOENCODING=utf-8 ./kb.bat web
```

按 spec 第四节逐条对：

1. **桌宠停在右下角（默认位置）时点把手** → 手机滑出来的同时，桌宠**被顶到左边**，
   不是被盖住、也不是穿过去
2. **手机收回去** → 桌宠**留在被推到的地方**，不弹回右下角
3. **桌宠被推到很左边之后再点把手** → 它**不动**（已经没挨着了）
4. **桌宠正飞着（甩出去还没停）时点把手** → 弹射停下，改被顶开
5. **推开动画正走到一半时按住桌宠拖** → **不跳**，直接从当前位置跟手
6. **推开过一次之后，再拖它甩它** → 手感跟以前一样（没有粘滞）
7. **手机开着的时候刷新页面** → 手机是收着的（开合状态不记，设计里定的），
   桌宠回到它上次停的地方

哪条不对就说哪条。

- [ ] **Step 7: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/static/pet.js src/kb/web/static/style.css
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 手机滑出来时把桌宠顶开

用户原话：「手机弹出时桌宠会被弹开，也就是类似有碰撞体积，他们在同一层级」。
所以不是盖住、也不是穿过——手机推开多少，桌宠让开多少。

收回去时不动它：留在被推到的地方，追着跑回去的话一开一关就来回抽。

两边靠一个自定义事件（kb:phone）通话，谁也不 import 谁；手机只报「开了没、
左边缘在哪」，剩下全归 pet.js 算。手机上还留着它自己的物理（速度、摩擦、反弹）。

推开那一下挂一个临时 transition（300ms 后摘），平时不能有——拖动要跟手、
弹射是自己的物理循环，挂着会拖泥带水。指针按下去的瞬间如果正走到一半，
先读实际位置钉住再摘 class，否则会跳到终点。

**这半没有自动化测试**（本项目没有浏览器测试），靠人点一遍验的，清单见计划。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 5: 收尾

**Files:**
- Modify: `docs/01_架构.md`（工程结构树）
- Modify: `docs/03_问题记录.md`（新增一条 Q）

---

- [ ] **Step 1: `01_架构.md` 的工程结构树**

先找到那棵树（搜 `pyproject.toml · requirements.txt`）。`static/` 那一行现在写的是
`static/ ← style.css · pet.js`，改成：

```
    static/ ← style.css · pet.js · phone.js
```

并把 `pet.js` 旁边那条说明（「全仓唯一的独立 `.js`」）改掉——**现在有两个了**：

> `pet.js` 与 `phone.js` 是全仓**仅有的**两个独立 `.js`（其余 JS 都在 `base.html`
> 的内联 `<script>` 里）。两个都是自成一个东西：`pet.js` 有自己的物理循环、
> 持久化状态、状态气泡；`phone.js` 有自己的开合状态与面板切换。
> 而 `base.html` 那段内联已经 500 多行，再往里塞就更找不着了。
> 两者之间**只有 `kb:phone` 一个自定义事件**，谁也不 import 谁。

- [ ] **Step 2: `03_问题记录.md` 新增一条 Q**

按现有格式加（编号接在最后一个 Q 后面），记**结论与理由**：

- 四个页面那条各不相同的右侧栏**收成一台手机**（原话「全部替换」）
- **抽屉式而不是常驻**：这是取舍——正文放开到满宽，代价是切「报告/历史」
  从 1 次点击变成 3 次（`home` → 点应用）。用户看过原型后选的
- 两个应用的口径：**该页第一个面板 + 历史**；运行日志只有一个；
  ai对话的「新会话」是**动作**不是内容
- **手机顶开桌宠**而不是盖住/穿过（原话「类似有碰撞体积，他们在同一层级」）
- 报告气泡改成聊天气泡那款（`00.png`），**只在手机里**；
  小尖是**样式不是指示器**
- ⚠️ **记一笔术语坑**：这一轮设计我先把「气泡框」当成了桌宠的状态气泡
  （`.pet-bubble`），白画两屏。`.bubble` 与 `.pet-bubble` 是同一个词指的两个东西

同时把 **`.chat-history .bubble` 那条 `white-space: normal` 覆盖的迁移**记进去
——它是「丢了页面不报错、只是每个气泡多一行空白」的那类坑。

- [ ] **Step 3: 跑全量 + 风格检查**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **Step 4: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add docs/01_架构.md docs/03_问题记录.md
git -C "E:/Study_Projects/KN_Base" commit -m "docs: 右侧栏改成手机的收尾

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## 收尾检查

- [ ] `git log --oneline` 看一串提交是不是按 Task 1→5 排的，且**没有任何一条带了 `data/`、`.env` 或 `.superpowers/`**
- [ ] 手机那 5 条肉眼清单（Task 1 Step 11）与桌宠那 7 条（Task 4 Step 6）**逐条走过**
- [ ] 气泡那 5 条（Task 3 Step 5）走过，特别是**「主区对话气泡没变」**那条
- [ ] 六套皮肤各切一遍看手机（颜色跟不跟）
- [ ] **推之前扫一眼 diff**——仓库是 public 的
- [ ] 回滚点确认还在：`git tag -l "pre-phone-sidebar-*"`

---

## 不在本次范围（写了就别顺手做）

- 桌宠的「藏起来」开关
- 手机里显示别的东西（巡检状态、正在忙什么）
- 桌宠拖到屏幕最左边时把气泡小尖镜像到右下角
- 手机开合状态持久化（每次进页面都收着）
- 窄窗口的专门处理
- 真界面侧栏选中项 3.57:1 那条对比度（仍在 `docs/03_问题记录.md` 的延后表里）
