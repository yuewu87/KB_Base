# 对话统计 + 悬浮桌宠 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让对话答得出「库里有多少条」，并给界面加一个可拖可甩的 KB 桌宠（点击果冻 + 状态气泡）。

**Architecture:** 两半互不相干。**统计**是纯 core 改动——`vault.counts()` 做唯一判据，`/setup/state` 与对话的系统提示都调它。**桌宠**是纯前端——`base.html` 加一个 div 和一行 `<script src>`，逻辑全在新建的 `static/pet.js`，样式追加进 `style.css`。

**Tech Stack:** Python 3.11 / FastAPI / Jinja2 / 单文件 `style.css` / **本项目第一个独立 `.js` 文件** / pytest。

**设计文档:** `docs/superpowers/specs/2026-09-23-桌宠与统计-design.md`。**两边冲突时以 spec 为准**，回头改计划。

---

## 开工前必做

- [ ] **⚠️ 停掉服务。** `src/` 一改，常驻进程仍跑旧逻辑，**而且看起来一切正常**——这个坑本项目反复踩（`CLAUDE.md` 平台陷阱）。

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop
```

- [ ] **⚠️ 全仓有两个 `build_system_prompt`，别改错。** Task 3 要改的是
  **`src/kb/core/chat.py:37`** 那个（对话用的）；**`src/kb/core/planning.py:174`**
  那个同名的**是整理草稿用的，一个字都别动**。`tests/core/test_planning.py` 里
  十几处 `build_system_prompt()` 调的全是后者——改错了它们会一片红，那时候你
  会以为是自己手滑，其实是找错了文件。

- [ ] **⚠️ 本项目没有独立 `.js`，这是第一个。** `static/` 下现在只有
  `style.css`。`pet.js` 是**有意**单开的（理由见 spec 定案 #6），不是手滑建错地方。

- [ ] **计划里所有 `pytest` / `ruff` 都用绝对路径的 python**（本机 `conda run`
  会报内部错误，**不要用**）：

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **每次调 `kb.bat` 都要带 `PYTHONIOENCODING=utf-8`**，否则管道里读回来全是 GBK 乱码。

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/kb/core/vault.py` | 加 `counts(vault_root)`——**库的规模只有一个判据** | 改 |
| `src/kb/web/router.py` | `_counts` 改调它；import 跟着换 | 改 |
| `src/kb/core/chat.py` | `build_system_prompt(vault_root)` 加参数 + 「当前库」那一段 | 改 |
| `src/kb/web/templates/base.html` | 加 `.pet` 那个 div + 一行 `<script src>` | 改 |
| `src/kb/web/static/pet.js` | **新建**：拖动、弹射、反弹、果冻、气泡、位置持久化 | 新建 |
| `src/kb/web/static/style.css` | 追加 `.pet` / `.pet-bubble` 的样式（**只用变量**） | 改 |
| `tests/core/test_vault.py` | `counts` 的两条用例 | 改 |
| `tests/core/test_chat.py` | 系统提示里带着那两个数 | 改 |
| `tests/web/test_setup.py` | 桌宠的 div 与脚本引用 | 改 |

---

## Task 1: `vault.counts` —— 找一处地方数

**为什么先做这个：** 现在这个数只在 `router._counts` 里算过一次，而**对话在 core 层**，
够不着 web 层。「router 是薄层、判断下沉到 core」是本项目的硬规矩，所以先把它挪下去。

**Files:**
- Modify: `src/kb/core/vault.py`（加在 `list_notes` 后面）
- Test: `tests/core/test_vault.py`

---

- [ ] **Step 1: 先写失败测试**

`tests/core/test_vault.py` 顶部那段 `from kb.core.vault import (...)` 里加一行
`counts,`（按字母序插在 `clean_title` 后面即可）。然后在文件**末尾**追加：

```python
# ---------- 库的规模（对话统计与桌宠共用） ----------

def test_counts_counts_notes_and_drafts(tmp_path):
    """数两样：正式笔记、待整理草稿。**判据只此一份。**"""
    write_note(
        tmp_path / "计算机" / "甲.md", {"类型": "概念", "主题": ["计算机"]}, "x"
    )
    write_note(
        tmp_path / "计算机" / "乙.md", {"类型": "概念", "主题": ["计算机"]}, "y"
    )
    write_draft(tmp_path, _draft(id="20260915-aaaa"))

    assert counts(tmp_path) == {"notes": 2, "drafts": 1}


def test_counts_on_a_dir_that_is_not_a_vault(tmp_path):
    """目录都不存在时返回 0，**不抛**。

    它会被**每一条对话**和 `/setup/state` 调到——抛出去就是整块界面死掉
    （`/setup/state` 是每个页面首屏都要打的端点）。
    """
    assert counts(tmp_path / "还没有这个目录") == {"notes": 0, "drafts": 0}
```

- [ ] **Step 2: 跑一遍，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_vault.py -v
```

预期：`ImportError: cannot import name 'counts'`（整份文件收集失败，不是 2 条红）。

- [ ] **Step 3: 实现**

`src/kb/core/vault.py` 里，紧跟 `list_notes` 之后加：

```python
def counts(vault_root: Path) -> dict[str, int]:
    """库的规模：`{"notes": N, "drafts": M}`。

    **判据只此一份**——`/setup/state` 与对话的系统提示都调它。

    **为什么住在 `core/`**：对话那次调用在 core 层，够不着 `web/`；而
    「`router.py` 是薄层、判断下沉」也是本项目的硬规矩。

    **目录不存在时返回 0 不抛**：`list_notes` / `list_drafts` 对不存在的目录
    本来就返回 `[]`。这个函数会被每一条对话和 `/setup/state`（每个页面首屏）
    调到，抛出去就是整块界面死掉。
    """
    return {
        "notes": len(list_notes(vault_root)),
        "drafts": len(list_drafts(vault_root)),
    }
```

- [ ] **Step 4: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_vault.py -v
```

预期：全绿。

- [ ] **Step 5: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/core/vault.py tests/core/test_vault.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: vault.counts()——库的规模收成一处判据

现在这个数只在 router._counts 里算过一次，而对话在 core 层够不着 web 层。
先把它挪到 vault.py：/setup/state 与对话的系统提示都调它。

空目录返回 0 不抛——它会被每一条对话和每个页面首屏的 /setup/state 调到。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 2: `router._counts` 改调它

**Files:**
- Modify: `src/kb/web/router.py`（`_counts` 与文件头那行 import）

---

- [ ] **Step 1: 改 import**

第 43 行现在是：

```python
from kb.core.vault import list_drafts, list_notes
```

改成：

```python
from kb.core.vault import counts
```

⚠️ **这一行必须连着改。** 改完之后 `list_notes` / `list_drafts` 在这个文件里
**只剩 `_counts` 用过**（已经查过），留着就是 ruff F401。

- [ ] **Step 2: 改 `_counts` 里数数那两行**

把：

```python
        if vault_ready(vault_path):
            notes = len(list_notes(vault_path))
            drafts = len(list_drafts(vault_path))
```

改成：

```python
        if vault_ready(vault_path):
            counted = counts(vault_path)
            notes, drafts = counted["notes"], counted["drafts"]
```

**上面那段注释一个字不动**——它讲的是「为什么 `vault_ready` 为假时一个都不数」
（`.env` 填成一个文件时 `iterdir()` 会抛 `NotADirectoryError`），那条理由
对 `counts()` 同样成立。

- [ ] **Step 3: 跑跟 `/setup/state` 有关的测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py tests/web/test_router.py -v
```

预期：全绿。`counts` 的键名/取值与原来逐字一致，行为不该有任何变化。

- [ ] **Step 4: 风格检查（这一条专抓那行 import）**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

预期：`All checks passed!`。**若报 F401**，就是 Step 1 没做。

- [ ] **Step 5: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/router.py
git -C "E:/Study_Projects/KN_Base" commit -m "refactor: _counts 改调 vault.counts，判据只留一份

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 3: 系统提示里带上那两个数

**Files:**
- Modify: `src/kb/core/chat.py`（`build_system_prompt` 第 37 行；`run_turn` 第 118 行）
- Test: `tests/core/test_chat.py`

⚠️ **改的是 `chat.py` 那个 `build_system_prompt`。** `planning.py` 里有个同名的，
是整理草稿用的，**别动**（见「开工前必做」第二条）。

---

- [ ] **Step 1: 先写失败测试**

`tests/core/test_chat.py` 顶部 import 加 `build_system_prompt`：

```python
from kb.core.chat import MAX_ROUNDS, build_system_prompt, parse_reply, run_turn
```

末尾追加：

```python
# ---------- 系统提示里的库规模 ----------

def test_system_prompt_carries_the_library_size(tmp_path):
    """对话要答得出「库里有多少条」——它得**先知道**。

    这条是那个需求的全部实现：不新增动作，直接把两个数写进系统提示。
    """
    from kb.core.vault import write_note

    write_note(
        tmp_path / "计算机" / "甲.md", {"类型": "概念", "主题": ["计算机"]}, "x"
    )

    prompt = build_system_prompt(tmp_path)

    assert "1 篇笔记" in prompt
    assert "0 条待整理" in prompt


def test_system_prompt_says_so_when_there_is_no_vault():
    """没库时写「还没有知识库」，**不是「0 篇笔记」**。

    后者看着像库坏了；前者才是实话。
    """
    assert "还没有知识库" in build_system_prompt(None)


def test_run_turn_feeds_the_prompt_with_the_vault(tmp_path):
    """`run_turn` 要**把库传进去**——不传的话上面那段永远不出现。

    照着本文件已有的 `_Spy` 写法：截住 `system`，看它带没带那两个数。
    """
    from kb.core.vault import write_note

    write_note(
        tmp_path / "计算机" / "甲.md", {"类型": "概念", "主题": ["计算机"]}, "x"
    )
    seen: list[str] = []

    class _Spy:
        def __init__(self, replies):
            self._inner = FakeLLM(replies)

        def complete(self, system, user):
            seen.append(system)
            return self._inner.complete(system, user)

    run_turn(tmp_path, [], "库里有多少条", _Spy([_reply("1 篇")]))

    assert "1 篇笔记" in seen[0]
```

- [ ] **Step 2: 跑，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_chat.py -v
```

预期：三条全红，且报的是
`TypeError: build_system_prompt() takes 0 positional arguments but 1 was given`。

- [ ] **Step 3: 实现——签名加参数**

`src/kb/core/chat.py` 第 37 行：

```python
def build_system_prompt() -> str:
```

改成：

```python
def build_system_prompt(vault_root: Path | None) -> str:
```

（`Path` 这个文件里已经 import 过了。）

- [ ] **Step 4: 实现——提示里加一段**

在 `build_system_prompt` 那个 f-string 里，「## 你能做的动作」那一节**之前**
插入下面这段（注意它是 f-string 的一部分，`{...}` 里放的是表达式）：

```python
## 当前库

{_size_line(vault_root)}
```

然后在 `build_system_prompt` **之前**加这个小函数：

```python
def _size_line(vault_root: Path | None) -> str:
    """系统提示里那行「库有多大」。

    **没库时说实话**，不写「0 篇笔记」——那个看着像库坏了，而「还没有知识库」
    用户一眼就懂下一步该干什么。

    ⚠️ **每轮现算**，不缓存：一轮对话里用户可能刚投了一条，下一轮那个数就该变。
    代价是一次目录扫描（只走目录、不读正文），几十篇是瞬间的事。
    """
    if vault_root is None:
        return "还没有知识库"
    counted = counts(vault_root)
    return f"{counted['notes']} 篇笔记 · {counted['drafts']} 条待整理"
```

**文件头要新加一行 import。**

⚠️ **不是「往现有那行里塞一个名字」**——`chat.py` 现在**根本没有从
`kb.core.vault` import 过任何东西**（2026-09-24 核过），它现有的三行是：

```python
from kb.core.actions import describe_actions, run_action
from kb.core.chat_store import load_chat, new_chat_id, save_chat
from kb.llm.base import LLM, LLMError
```

所以加完是这样（按字母序，插在 `chat_store` 和 `llm.base` 之间）：

```python
from kb.core.actions import describe_actions, run_action
from kb.core.chat_store import load_chat, new_chat_id, save_chat
from kb.core.vault import counts
from kb.llm.base import LLM, LLMError
```

（不会成环：`kb.core.vault` 只 import `kb.proc` 和 `kb.core.models`，不碰 `chat`。）

- [ ] **Step 5: 实现——`run_turn` 传进去**

第 118 行：

```python
    system = build_system_prompt()
```

改成：

```python
    system = build_system_prompt(vault_root)
```

- [ ] **Step 6: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_chat.py -v
```

预期：全绿。

- [ ] **Step 7: 跑一遍对话那条链的所有测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_chat.py tests/api/test_http.py tests/web/test_router.py -v
```

预期：全绿。（对话那条链路上还有 `/chat`、网页对话表单两条入口，它们都走
`handle` → `run_turn`。）

- [ ] **Step 8: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/core/chat.py tests/core/test_chat.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 对话答得出「库里有多少条」

用户原话：「刚才问它一共有多少条内容了，他无法回答，待整理有多少条也无法
回答」——查下来是它手上压根没这个数：能做的动作只有 search/push/revise/
organize，没有「数一数」这一条。

按 spec 定案 #1（用户选的）：**不加动作，每轮直接把两个数写进系统提示**。
问就答、不额外花一轮；每轮现算所以永远新鲜。代价只是一次目录扫描。

没库时写「还没有知识库」而不是「0 篇笔记」——后者看着像库坏了。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 4: 桌宠——骨架、点击果冻、气泡

**Files:**
- Create: `src/kb/web/static/pet.js`
- Modify: `src/kb/web/templates/base.html`（`<head>` 里加 script；body 里加 div）
- Modify: `src/kb/web/static/style.css`（追加 `.pet` / `.pet-bubble`）
- Test: `tests/web/test_setup.py`

---

- [ ] **Step 1: 先写失败测试**

`tests/web/test_setup.py` 末尾追加：

```python
# ---------- 桌宠 ----------

def test_the_page_carries_the_pet(client):
    """每页都要有桌宠那个元素，而且要引到 `pet.js`。

    分成两条断言是因为**漏哪一样它都不动**：少了 div，脚本找不到东西挂；
    少了 script，div 就是个不会动的方块。而这两种漏法页面上都不会报错。
    """
    html = client.get("/").text
    assert 'id="pet"' in html
    assert '<script src="/static/pet.js" defer></script>' in html


def test_pet_js_is_served(client):
    """`static/pet.js` 真能被取到——**本项目第一个独立 `.js`**。

    它是新开的一个文件（见 spec 定案 #6），所以顺带钉一下它确实在那儿、
    而且被 StaticFiles 发出来了。
    """
    r = client.get("/static/pet.js")
    assert r.status_code == 200
    assert "getElementById('pet')" in r.text
```

- [ ] **Step 2: 跑，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py -v
```

预期：**两条**红——`test_the_page_carries_the_pet`（`id="pet"` 找不到）和
`test_pet_js_is_served`（`/static/pet.js` 是 404）。

- [ ] **Step 3: 建 `src/kb/web/static/pet.js`**

先建骨架（**只有点击那半**，拖动留给 Task 5）：

```javascript
// 悬浮桌宠：可点、可拖、可甩，在视口里反弹。
//
// **本项目第一个独立 .js 文件**（其余 JS 都在 base.html 的内联 <script> 里）。
// 单开是**有意**的，理由见 docs/superpowers/specs/2026-09-23-桌宠与统计-design.md
// 定案 #6：它自成一个东西——自己的物理循环、持久化状态、气泡。
//
// ⚠️ **颜色一律走 style.css 里的变量**，这里不碰颜色。style.css 有「变量块
// 之外不许出现十六进制」的防回潮测试守着，桌宠的样式同样归它管。
(function () {
  var pet = document.getElementById('pet');
  if (!pet) return;                    // 页面里没有就安静退出

  var BUBBLE_GAP = 8;                  // 气泡离桌宠多少
  var SIZE = 46;                       // 与 style.css 里的 .pet 尺寸一致
  var DRAG_SLOP = 5;                   // 位移小于它算「点击」
  var HIDE_AFTER = 4000;               // 气泡多久自己收（毫秒）

  var bubble = document.getElementById('pet-bubble');
  var hideTimer = null;

  function showBubble(text, bad) {
    bubble.textContent = text;
    bubble.classList.toggle('bad', !!bad);
    bubble.hidden = false;
    placeBubble();
    clearTimeout(hideTimer);
    hideTimer = setTimeout(hideBubble, HIDE_AFTER);
  }

  function hideBubble() {
    bubble.hidden = true;
    clearTimeout(hideTimer);
  }

  // **鼠标停在气泡上就不收**——正在看的时候被收走最烦人。
  bubble.addEventListener('mouseenter', function () { clearTimeout(hideTimer); });
  bubble.addEventListener('mouseleave', function () { restartHideTimer(); });

  function restartHideTimer() {
    clearTimeout(hideTimer);
    hideTimer = setTimeout(hideBubble, HIDE_AFTER);
  }

  function placeBubble() {
    var r = pet.getBoundingClientRect();
    var top = r.top - bubble.offsetHeight - BUBBLE_GAP;
    // **夹回可视区**：桌宠被甩到最上面时，气泡不能跑到屏幕外面去
    if (top < BUBBLE_GAP) top = r.bottom + BUBBLE_GAP;
    var left = Math.min(
      Math.max(r.left + r.width / 2 - bubble.offsetWidth / 2, BUBBLE_GAP),
      window.innerWidth - bubble.offsetWidth - BUBBLE_GAP
    );
    bubble.style.top = top + 'px';
    bubble.style.left = left + 'px';
  }

  function wobble() {
    pet.classList.remove('jelly');
    void pet.offsetWidth;              // 强制重排，动画才会重放
    pet.classList.add('jelly');
  }

  function describe(data) {
    if (!data.initialized) return '还没有知识库';
    return '库里 ' + data.counts.notes + ' 篇笔记 · 待整理 ' +
           data.counts.drafts + ' 条';
  }

  function onPetClick() {
    wobble();
    fetch('/setup/state')
      .then(function (r) { return r.json(); })
      .then(function (d) { showBubble(describe(d), false); })
      // **拉不到就如实说**，不装作 0 条——跟 /setup/remove 那条失败路径一个路子
      .catch(function (e) { showBubble('数不出来（' + e + '）', true); });
  }

  // **初始位置：右下角，离两边各 24px。**
  //
  // ⚠️ **`position: fixed` 的元素不给 left/top 会停在文档流里那个位置**——
  // 也就是它写成 HTML 的那个地方（几个 `.overlay` 后面），页面上会看着像
  // 飘在半空。所以这一句不能省。位置持久化在 Task 5 接上，那时它换成
  // 从 `localStorage` 读。
  pet.style.left = (window.innerWidth - SIZE - 24) + 'px';
  pet.style.top = (window.innerHeight - SIZE - 24) + 'px';

  // 拖动那半在 Task 5 接上；这一版先只做点击。
  pet.addEventListener('click', onPetClick);

  window.addEventListener('resize', function () {
    if (!bubble.hidden) placeBubble();
  });
})();
```

- [ ] **Step 4: 建 `base.html` 里的两处**

**(a)** `<head>` 里、样式表那行下面：

```html
  <link rel="stylesheet" href="/static/style.css">
  {# 桌宠：本项目第一个独立 .js。`defer` 保证它在 DOM 解析完之后才跑，
     而 `pet.js` 里第一句就是 `getElementById('pet')`。 #}
  <script src="/static/pet.js" defer></script>
```

**(b)** 三个 `.overlay` **全都结束之后**。

⚠️ **不要去找「最后一个 `<script>` 之前」**——`base.html` 里那个
`<script>`（设置窗那段）从第 209 行**一直跑到文件末尾**，按那句话找会站错
地方。认这两行锚点（第 205–209 行，已核过）：

```html
        <button type="button" class="secondary danger" id="remove-go" disabled
                onclick="confirmRemove()">移除</button>
      </div>
    </div>
  </div>

  <script>
```

**插在倒数第二行的 `</div>`（`#remove-overlay` 的收尾）和最后那行 `<script>`
之间**，也就是紧接着那个空行：

```html
  {# 悬浮桌宠。**摆在所有 `.overlay` 之后**是为了层级好调（见 style.css 里
     那条 z-index 注释）；它是 `position: fixed`，在文档里的位置不影响显示。 #}
  <div class="pet" id="pet" title="点一下看库的大小">KB</div>
  <div class="pet-bubble" id="pet-bubble" hidden></div>
```

- [ ] **Step 5: 追加样式到 `src/kb/web/static/style.css`**

追加到**文件末尾**（跟其余样式一样，在皮肤块之后）：

```css
/* ---- 悬浮桌宠 ----

   全站每一页都在，位置记在 localStorage。形象跟左上角那个 KB 标一致
   （圆角方块 + 强调色底），但它还要 fixed 定位、拖拽光标、果冻动画，
   所以不复用 `.brand-mark` 那个类，另写一套。

   **层级必须是 40。** 全站现成的层级（已核过 style.css）：`.sidebar` 20、
   `.overlay`（设置窗、移除确认、服务已停）50、`.skin-menu`（皮肤浮层）60。
   40 压着侧栏、被后面两个压着——桌宠飘在设置窗或皮肤浮层上面会挡事。 */

.pet {
  position: fixed;
  z-index: 40;
  width: 46px; height: 46px;
  border-radius: 13px;
  background: var(--accent);
  color: var(--on-accent);
  display: grid; place-items: center;
  font-size: 15px; font-weight: 700; letter-spacing: -.02em;
  cursor: grab;
  touch-action: none;                /* 触摸拖动时别让浏览器跟着滚 */
  user-select: none;
  box-shadow: var(--shadow-lift);
  transform-origin: 50% 50%;
}

.pet.dragging { cursor: grabbing; }

/* 果冻：点一下的反馈。振幅取的是原型里「弹」那一档（用户在原型里选的）。 */
.pet.jelly { animation: pet-jelly .7s cubic-bezier(.3, 1.9, .5, 1); }
@keyframes pet-jelly {
  0%   { transform: scale(1, 1); }
  20%  { transform: scale(1.38, .66); }
  42%  { transform: scale(.74, 1.30); }
  62%  { transform: scale(1.20, .84); }
  82%  { transform: scale(.94, 1.07); }
  100% { transform: scale(1, 1); }
}

.pet-bubble {
  position: fixed;
  z-index: 41;                        /* 压着桌宠，但仍在 .overlay 下面 */
  max-width: 230px;
  padding: 8px 11px;
  border: 1px solid var(--line);
  border-radius: 10px;
  background: var(--surface);
  color: var(--ink);
  font-size: 12.5px;
  line-height: 1.6;
  box-shadow: var(--shadow-lift);
  pointer-events: auto;               /* 悬停暂停计时靠它 */
}
.pet-bubble[hidden] { display: none; }
.pet-bubble.bad { color: var(--danger); }
```

> **`[hidden]` 那条为什么必须写**：`.pet-bubble` 是 `position: fixed`，
> 浏览器默认的 `[hidden] { display: none }` 会被类选择器里没写 `display` 这件事
> 放过——但 `position: fixed` 的元素**一旦有 display 值就会显示**。本项目已经
> 踩过这个（`.overlay[hidden]` 那条注释写着「两条 `.overlay` 一上来就把整页
> 盖死」），所以照抄那条做法显式写一遍。

- [ ] **Step 6: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py -v
```

预期：全绿。

- [ ] **Step 7: 跑一下防回潮那条（新样式里不许写死颜色）**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_skins.py -v
```

预期：全绿。**若报「变量定义之外还有写死的色值」**，就是上面那段 CSS 里混进了
`#xxx` 或 `rgba(...)` 字面量——改成变量。

- [ ] **Step 8: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/static/pet.js src/kb/web/static/style.css src/kb/web/templates/base.html tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 悬浮桌宠——骨架 + 点击果冻 + 状态气泡

形象用左上角那个 KB 标的样子（圆角方块 + 强调色），颜色全走变量，所以
换皮肤自动跟。位置记在 localStorage，全站每页都在。

果冻振幅取原型里「弹」那档（用户在原型里逐个试过之后选的）。
气泡点的时候拉一次 /setup/state，4 秒后自己收、鼠标停上去就不收；
拉不到如实说「数不出来」，不装作 0 条。

层级是 40：.overlay（设置窗那些）是 50，桌宠飘在上面会挡事。

pet.js 是**本项目第一个独立 .js**（其余 JS 都在 base.html 内联），有意单开。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 5: 桌宠——拖动、弹射、反弹、位置持久化

**这一条没有自动化测试**，本项目没有浏览器测试。所以 Step 5 那「亲眼看一眼」不是走过场，是它**唯一**的验证。

⚠️ **Step 1 和 Step 2 要连着做完再起服务。** Step 1 那段代码里已经调了
`savePos()`（`pointerup` 里两处、`fling` 停下时一处），而它要到 Step 2 才
定义。函数声明会提升，两步都做完就没问题；**只做 Step 1 就去开浏览器**会
在点第一下时报 `savePos is not defined`。

**Files:**
- Modify: `src/kb/web/static/pet.js`

---

- [ ] **Step 1: 常量 + pointer 事件（点击 vs 拖动靠位移分）**

**(a)** `pet.js` 顶部那组常量里，`SIZE` 那行下面加这几个：

```javascript
  var POS_KEY = 'kn.pet.pos';          // 位置存在 localStorage 的哪个键
  var EDGE = 24;                       // 初始位置离右下角多少
  var FLING_MIN = 2500;                // px/s，松手速度低于它就不飞
  var FRICTION = 0.985;                // 每帧衰减
  var BOUNCE = 0.62;                   // 撞边保留多少速度
  var STOP_AT = 0.02;                  // px/ms，低于它就停
```

**(b)** 把这两行：

```javascript
  // 拖动那半在 Task 5 接上；这一版先只做点击。
  pet.addEventListener('click', onPetClick);

  window.addEventListener('resize', function () {
    if (!bubble.hidden) placeBubble();
  });
```

换成下面这一整块——**`resize` 那条也一并换掉**（原来只管气泡，现在还要把桌宠
夹回可视区内）：

```javascript
  // ---- 拖动 / 弹射 / 反弹 ----
  //
  // **点击和拖动靠位移分**：`pointerdown` 到 `pointerup` 之间挪了不到
  // `DRAG_SLOP` 像素就算点击。不分的话，拖完松手会顺带弹一次气泡——
  // 那正是「拖开它别挡着我看东西」的时候最不想要的。
  var drag = null;                     // {sx, sy, ox, oy, px, py, last, moved}
  var vx = 0, vy = 0;                  // px/ms
  var raf = null;

  function maxX() { return window.innerWidth - SIZE; }
  function maxY() { return window.innerHeight - SIZE; }
  function clamp(v, lo, hi) { return Math.min(Math.max(v, lo), hi); }

  function moveTo(x, y) {
    pet.style.left = x + 'px';
    pet.style.top = y + 'px';
    if (!bubble.hidden) placeBubble();
  }

  function fling() {
    if (raf) return;
    raf = requestAnimationFrame(function step() {
      raf = null;
      var x = pet.offsetLeft + vx * 16;
      var y = pet.offsetTop + vy * 16;

      // 撞视口边就反弹，保留 BOUNCE 那部分速度
      if (x <= 0) { x = 0; vx = -vx * BOUNCE; }
      if (x >= maxX()) { x = maxX(); vx = -vx * BOUNCE; }
      if (y <= 0) { y = 0; vy = -vy * BOUNCE; }
      if (y >= maxY()) { y = maxY(); vy = -vy * BOUNCE; }

      vx *= FRICTION; vy *= FRICTION;
      moveTo(x, y);

      if (Math.abs(vx) < STOP_AT && Math.abs(vy) < STOP_AT) {
        vx = vy = 0;
        // **停在哪儿就记哪儿。** 少了这一句，记下的永远是「松手那一刻」的
        // 位置——飞出老远才停下的，刷新一下会跳回半路。
        savePos();
        return;
      }
      raf = requestAnimationFrame(step);
    });
  }

  pet.addEventListener('pointerdown', function (e) {
    if (raf) { cancelAnimationFrame(raf); raf = null; }
    hideBubble();
    pet.setPointerCapture(e.pointerId);
    pet.classList.add('dragging');
    vx = vy = 0;
    drag = {
      sx: e.clientX, sy: e.clientY, ox: pet.offsetLeft, oy: pet.offsetTop,
      px: e.clientX, py: e.clientY, last: performance.now(), moved: false,
    };
    e.preventDefault();
  });

  pet.addEventListener('pointermove', function (e) {
    if (!drag) return;
    var dx = e.clientX - drag.sx, dy = e.clientY - drag.sy;
    if (Math.abs(dx) > DRAG_SLOP || Math.abs(dy) > DRAG_SLOP) drag.moved = true;

    moveTo(clamp(drag.ox + dx, 0, maxX()), clamp(drag.oy + dy, 0, maxY()));

    // 速度按「上一次 move 到这一次」算，不是从按下的那点算起
    var now = performance.now();
    var dt = Math.max(now - drag.last, 1);
    vx = (e.clientX - drag.px) / dt;
    vy = (e.clientY - drag.py) / dt;
    drag.px = e.clientX; drag.py = e.clientY; drag.last = now;
  });

  pet.addEventListener('pointerup', function () {
    pet.classList.remove('dragging');
    var moved = drag && drag.moved;
    drag = null;

    if (!moved) { onPetClick(); savePos(); return; }

    var pxPerSec = Math.hypot(vx, vy) * 1000;
    if (pxPerSec >= FLING_MIN) { fling(); } else { vx = vy = 0; }
    savePos();
  });

  window.addEventListener('resize', function () {
    // ⚠️ 取 `offsetTop`，**不是 `offsetY`**：`offsetY` 在 `position: fixed`
    // 的元素上不是相对视口的距离，拿它算会跑到屏幕外面去。
    moveTo(clamp(pet.offsetLeft, 0, maxX()), clamp(pet.offsetTop, 0, maxY()));
  });
```

- [ ] **Step 2: 位置持久化 + 把初始位置接上它**

`localStorage` 三个函数（加在 `pet.js` 里 `fling()` 那段之后）：

```javascript
  function savePos() {
    try {
      localStorage.setItem(
        POS_KEY, JSON.stringify({ x: pet.offsetLeft, y: pet.offsetTop })
      );
    } catch (e) {
      // **隐私模式下 localStorage 会抛。** 位置记不住是小事，别让它把整个
      // 桌宠搞死——那不就成了「点了一下什么都没发生」。
    }
  }

  function loadPos() {
    try {
      var raw = localStorage.getItem(POS_KEY);
      if (!raw) return null;
      var p = JSON.parse(raw);
      return (typeof p.x === 'number' && typeof p.y === 'number') ? p : null;
    } catch (e) {
      return null;
    }
  }

  function startPos() {
    var saved = loadPos();
    if (saved) return { x: clamp(saved.x, 0, maxX()), y: clamp(saved.y, 0, maxY()) };
    return { x: maxX() - EDGE, y: maxY() - EDGE };   // 初始右下角
  }
```

最后，把 Task 4 里那两行**写死的**初始位置：

```javascript
  pet.style.left = (window.innerWidth - SIZE - 24) + 'px';
  pet.style.top = (window.innerHeight - SIZE - 24) + 'px';
```

换成从 `localStorage` 读：

```javascript
  var start = startPos();
  moveTo(start.x, start.y);
```

- [ ] **Step 3: 全量测试 + 风格检查**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

预期：全绿。**JavaScript 的语法错误它测不出来**——所以下一步别跳。

- [ ] **Step 4: 起服务，打开控制台看有没有报错**

```bash
cd "E:/Study_Projects/KN_Base" && PYTHONIOENCODING=utf-8 ./kb.bat web
```

打开页面，按 F12 看 Console：**不该有红色的报错**。有就是语法写错了，
照着报错的行号改。

- [ ] **Step 5: 亲眼看一眼（这一条是 Task 5 唯一的验证）**

按 spec 第四节逐条对：

1. **点一下** → 果冻 + 气泡，气泡里是「库里 N 篇笔记 · 待整理 M 条」，4 秒后自己淡出
2. **鼠标停在气泡上** → 不收；移开 → 重新计时（4 秒后再收）
3. **按住慢慢拖、松手** → 停在原地，**不飞**
4. **按住快速甩** → 飞出去、撞到边上反弹、慢慢停下
5. **拖完松手** → **不弹气泡**（拖了 5px 以上就不算点击）
6. **换个页面** → 它还在刚才那个位置
7. **刷新** → 还在
8. **打开设置窗** → **桌宠被压在遮罩下面**，不挡设置窗
9. **关掉浏览器再开** → 位置还记得
10. **把窗口拉小** → 它被夹回可视区内，没有跑出去
11. **⚠️ 把页面滚到中间再试一遍拖动和甩** → 位置跟不跟手、会不会跳

第 11 条是复查时点出来的、**没有浏览器就没法证伪**的一条：整套坐标都建立在
「`position: fixed` 元素的 `offsetLeft/offsetTop` 是相对视口的」之上。
`offsetParent` 为 null 时成立（`html` / `body` / `.shell` 目前都没沾
`transform` / `filter` / `will-change` / `contain`，已核过），滚动过之后
在真实浏览器里到底成不成立**只有手点能验**。这条要是坏了，症状是
「一拖就跳」，而**控制台不会有任何报错**。

哪条不对就说哪条。

- [ ] **Step 6: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/web/static/pet.js
git -C "E:/Study_Projects/KN_Base" commit -m "feat: 桌宠能拖能甩了——弹射阈值 2500 px/s

拖动 vs 点击靠位移分（<5px 算点击）：不分的话拖完松手会顺带弹一次气泡，
而那正是「拖开它别挡着我看东西」的时候最不想要的。

弹射阈值 2500 px/s 是用户在原型里拿滑杆调出来的（原型带「上一次松手：
XXX px/s」的实时读数，他甩了几下定的）——随手拖几百、刻意一甩两三千，
所以这个数意味着「得刻意快甩」。摩擦 0.985/帧、撞视口边保留 62%。

位置记在 localStorage（隐私模式下会抛，接住它——位置记不住是小事，
把桌宠搞死才是大事）。

**这半没有自动化测试**（本项目没有浏览器测试），靠人点一遍验的，
清单见计划 Task 5 Step 5。

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 6: 收尾

**Files:**
- Modify: `docs/01_架构.md`（工程结构树）
- Modify: `docs/03_问题记录.md`（新增一条 Q）

---

- [ ] **Step 1: `01_架构.md` 的工程结构树加 `pet.js`**

先找到那棵树（搜 `pyproject.toml · requirements.txt`），在 `static/` 那一行
补上 `pet.js`，并**说明它是全仓第一个独立 `.js`**。理由一句话：

> `pet.js` 是全仓**唯一**的独立 `.js`（其余 JS 都在 `base.html` 的内联
> `<script>` 里）。它自成一个东西——自己的物理循环、持久化状态、气泡——
> 而 `base.html` 那段内联已经 540 行，再往里塞就更找不着了。

- [ ] **Step 2: `03_问题记录.md` 新增一条 Q**

按现有格式加一节（编号接在最后一个 Q 后面），记**结论与理由**：两个需求、
以及那两条**用户在原型里试出来的数字**（果冻「弹」档、弹射 2500 px/s）——
数字本身是拍出来的，出处得留着，否则下一个人会以为是随手写的。

顺带记下**真界面侧栏选中项 3.57:1** 那条（`--accent` 压 `--accent-soft`，
低于 AA 4.5:1）：项目的可读性测试只覆盖「正文压底色」和「强调色当文字压底色」
两对，**「强调色压强调色底」这一对在盲区里**，所以它一直没被抓住。标 🔴。

- [ ] **Step 3: 跑全量 + 风格检查**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **Step 4: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add docs/01_架构.md docs/03_问题记录.md
git -C "E:/Study_Projects/KN_Base" commit -m "docs: 桌宠与统计的收尾——pet.js 的来由 + 选中项对比度那条

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## 收尾检查

- [ ] `git log --oneline` 看一串提交是不是按 Task 1→6 排的，且**没有任何一条带了 `data/` 或 `.superpowers/`**
- [ ] 桌宠那 10 条肉眼清单（Task 5 Step 5）逐条走过
- [ ] **推之前扫一眼 diff**——仓库是 public 的，而这轮改了一个前端文件和一个 core 函数，不该有别的
