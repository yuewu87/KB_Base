# 知识库生命周期（初始化 / 迁移 / 移除）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让别人的 KN_Base 能配起来、搬走、再干净地扔掉——把「有没有知识库」变成界面上看得见、端点上拦得住的显式状态。

**Architecture:** 「有没有库」不存标志位，由**磁盘 + `.env`** 推出来（`cfg.vault_path is None` 或路径下没有 `.git` = 未初始化）。判断逻辑全部下沉到 `core/`，`web/router.py` 只做协议转换——这条是项目的既有分层（`router.py` 开头那句「薄层——只做取数据 + 渲染」）。搬家与删除是纯 `core/` 操作，所以计划里新增一个 `core/lifecycle.py`；**spec 第八节的模块表没列它**，那是表列漏了，不是要破例（理由见下）。

**Tech Stack:** Python 3.11 / FastAPI / Jinja2 / 单文件 `style.css` / 内联 `<script>`（**项目没有独立 `.js` 文件**）/ pytest。

**设计文档:** `docs/superpowers/specs/2026-09-22-知识库生命周期-design.md`。本计划是它的施工图；**两边冲突时以 spec 为准**，回头改计划。

---

## 开工前必做

- [ ] **⚠️ `router.py` 与 `api/http.py` 的行号一律别信——按名字找。**

  这两份文件里引用到的行号，全是**写计划时**的位置。Task 5 落地后在 `router.py`
  里加了约 281 行、`api/http.py` 里加了约 26 行，**下面所有行号都偏了**（实测：
  `settings_context` 的 `return {"groups": groups}` 从 137 漂到 171）。追着行号
  找的结果是**改错函数**，而且改完测试还是绿的——最坏的那类错。

  **按函数名、变量名或那句代码本身定位。** 其余文件（`_settings.html`、
  `base.html`、`style.css`、`config.py`、`tests/`、`docs/`）Task 5 没碰，
  那边的行号是准的。

- [ ] **停掉服务。** `src/` 一改，常驻进程仍跑旧逻辑，**而且看起来一切正常**——这个坑本项目反复踩（`CLAUDE.md` 平台陷阱）。服务当前在 51823 / pid 18648 上跑着。

```bash
cd "E:/Study_Projects/KN_Base" && ./kb.bat stop
```

- [ ] **本计划里所有 `pytest` / `ruff` 都用绝对路径的 python**（本机 `conda run` 会报内部错误，**不要用**）：

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **每次调 `kb.bat` 都要带 `PYTHONIOENCODING=utf-8`**，否则管道里读回来全是 GBK 乱码。

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/kb/config.py` | 读 `.env` 造 `Config`；「没有库」时的统一文案与取路径的入口 | 改 |
| `src/kb/core/vault_setup.py` | **建 vault 骨架的唯一真源**：目录清单、`.gitignore`、`init_vault`、领域候选 | **新建** |
| `src/kb/core/lifecycle.py` | 库的四个状态、路径体检、搬家、移除、`Busy` 锁 | **新建** |
| `src/kb/core/vault.py` | vault 的读写；`SEED_DOMAINS` 搬走后只留路径常量 | 改 |
| `scripts/init_vault.py` | 薄壳：从 `kb.core.vault_setup` 导入，保留命令行入口 | 改 |
| `src/kb/core/settings.py` | 配置项清单；`KB_VAULT_PATH` 从只读改成可编辑 + 路径校验 | 改 |
| `src/kb/web/router.py` | 五个新端点；`settings_context` 加初始化状态；`build_router` 加参数 | 改 |
| `src/kb/api/http.py` | 造 `Busy` 传下去；投递/整理端点加拦截；`apply_settings` 旁加 `unbind_vault` | 改 |
| `src/kb/api/cli.py` | 没库时报明白话；4xx 的 `detail` 原样印出来，别甩 httpx 的英文 | 改 |
| `src/kb/web/templates/_settings.html` | 「知识库」分组的**一屏两态** | 改 |
| `src/kb/web/templates/base.html` | 一条龙的表单与 JS、移除确认模态、首屏引导卡 | 改 |
| `src/kb/web/static/style.css` | 复选框网格、危险按钮、引导卡 | 改 |

**为什么新增 `core/lifecycle.py`：** spec 第八节把端点写在 `router.py`（对），但没写这些端点**背后**的逻辑住哪。项目的硬规矩是「`core/` 不 import `web`/`api`」+「`router.py` 是薄层，判断全在 core」。`copytree` + 校验 + 改名删除这套判断放 web 层就破了那条规矩。所以：端点仍在 `router.py`，逻辑在 `lifecycle.py`。

---

## 实施勘误（2026-09-22 实跑后回填）

Task 1/2/3 在三个并行 worktree 里跑完了，**各抓到本计划一处「照抄跑不通」的错**。下面记的是实际落地的写法——**Task 4 及之后按原计划读，但涉及这几处的以本节为准**。

**勘误一（Task 1）：`/push` 漏了拦截，会返回 500 而不是 409。**

Step 8 的标题写「八个端点」，正文只列了六个（`/drop` `/inbox` `/search` `/organize` `/chat` `/sweep`）——`/push` 不在里面。不补的话 `require_vault` 抛的 `ConfigError` 没人接，客户端拿到的是 500。可 `vault_guard` 的 docstring 自己就点名了「`kb.bat push` 走 CLI、会话层 AI 走 `/push`，它们都绕过界面」——**漏的恰恰是最该拦的那个**。

`/push` 端点开头补一行：

```python
    @app.post("/push")
    def push(req: PushRequest) -> dict:
        vault()                     # 没库就 409，别等写了一半才发现
        ...
```

（`tests/web/test_setup.py::test_vault_endpoints_409_before_init` 那条参数化用例里已经含了 `/push`，所以补上之后是有测试守着的。）

**勘误二（Task 2）：`test_second_run_keeps_existing_notes` 照抄必红。**

那段是：

```python
    init_vault(tmp_path)
    note = tmp_path / "计算机" / "已有笔记.md"
    note.write_text("内容", encoding="utf-8")
```

`SEED_DOMAINS` 已经删了，`init_vault(root)` 不带 `domains` 就**只建机器目录、不建领域目录**——`计算机` 不存在，`write_text` 直接 `FileNotFoundError`。首调要带上领域：

```python
    init_vault(tmp_path, ["计算机"])      # 领域目录得先有，不然下面那行没地方写
```

**勘误三（Task 2）：Step 9 的「净 +4」是错的，实际净 +3。**

新增的 `tests/core/test_vault_setup.py` 是 **12** 条不是 13 条。629 − 9 + 12 = 632。删的 9 条与新增的 12 条逐条核对过，没有漏删漏建。

**勘误四（Task 3，这条最要紧）：`_vault_path_problem(where: Path)` 是个安全洞。**

**`Path("")` 就是 `"."`**——所以 `str(where).strip()` 得到 `"."`，空值分支永远进不去；而 `Path("") / ".git"` 会按**当前工作目录**去探，在仓库里跑测试时那儿真有 `.git`，于是**空值被放行**。「留空 = 解绑」这个本设计专门要堵的洞，会从后门重新打开。

改成**收原始字符串**、判完空值再建 `Path`：

```python
def _vault_path_problem(raw: str) -> str | None:
    """检查这个路径能不能当库用。返回一句人话，没问题返回 `None`。

    **收 `str` 不收 `Path`**：`Path("")` 就是 `"."`，空值会在建 `Path` 那一步
    被悄悄抹掉——`str(Path(""))` 是 `"."`，于是空值分支永远进不去，而
    `Path("") / ".git"` 还会按**当前工作目录**去探（在仓库里跑时那儿真有
    `.git`）。「留空 = 解绑」这个洞就是这么从后门重开的。
    """
    if not raw.strip():
        return "要解绑请用「移除知识库」，它会把痕迹一起清掉"
    where = Path(raw)
    if not (where / ".git").exists():
        return (
            f"{where} 那儿还没有知识库。想搬过去用「迁移到别处」，"
            "想从零建先「移除知识库」"
        )
    return None
```

**勘误五（Task 3）：路径分支的位置在「text 留空 = 报错」那条通用检查之后。**

计划让它在 `clean[key] = text` 之前，但那里已经在 `if spec.kind == "text" and not text` **之后**了——空值会被通用文案截走，专用分支成死代码，`test_vault_path_rejects_empty`（要求文案含「移除知识库」）必红。路径分支要挪到那条通用空值检查**之前**。

**勘误六（Task 3）：测试 helper `_vault_at` 的两处 `mkdir` 要 `exist_ok=True`。**

同一个 helper 在一个用例里被调两次，不加就是 `FileExistsError`。

**勘误七（Task 3 顶掉的两条既有用例，计划完全没提）：**

- `tests/web/test_router.py::test_save_settings_ignores_readonly` —— 原来拿 `KB_VAULT_PATH` 当只读样板，现在它可写了，改用 `KB_PORT`（仍然只读）。
- `tests/core/test_settings.py::test_validate_drops_readonly_and_unknown` —— 同上，拿 `KB_VAULT_PATH` 当只读样板的，换别的键。
- `tests/web/test_router.py::test_readonly_fields_show_the_effective_values` —— 它断言 vault 路径那栏显示**生效值**，而 `router._value()` 那条分支是按 `kind == "readonly"` 判的。改成 `text` 后按可改字段的规矩显示 `.env` 字面值，**这是新契约下的正确行为**，vault 那半断言删掉，端口那半保留。

  **留下的覆盖缺口由 Task 6 补**：Task 6 Step 1b 删掉 `_value` 里那段死代码，同时要加一条测试钉住设置窗真的渲染出了两态（见 Task 6 Step 7）。

**两处已知但当时打不到的雷，实现者按规矩没动，本计划在后续任务里补掉：**

- `src/kb/api/http.py` 的 `_chat_organize_fn` 里那个 `organize` 分支仍在传 `get_cfg().vault_path`——空库时会 `list_drafts(None)` 抛 `TypeError`。**当前打不到**（`/chat` 与 `chat_post` 都已加拦截），但这是「等哪天有人挪了那道拦截就炸」的雷。**Task 4 Step 5(h) 修掉。**
- `src/kb/web/router.py:94` 的 `settings_context._value` 对 `KB_VAULT_PATH` 仍返回 `str(cfg.vault_path)`，未初始化时会显示字面量 `None`。Task 3 把它改成 `text` 之后那条分支已是死代码，**Task 6 Step 1b 删掉。**

**勘误八（Task 1 审查查出，计划里没有这条）：`scripts/init_vault.py` 的回落到 Task 2 之后还活着。**

Task 1 拆掉了服务里的 `DEFAULT_VAULT_PATH`，`scripts/init_vault.py` 那个 `__main__` 却还留着
`Path(sys.argv[1]) if len(sys.argv) > 1 else Path(r"E:\KB_Library")`——Task 2 只把实现搬进
`kb.core.vault_setup`，把那个写死的默认值一并搬了过来。

它和服务里那条的区别是**更毒**：`NO_VAULT_MESSAGE` 恰恰让人「把 `.env` 的
KB_VAULT_PATH 填好、再跑 `python scripts/init_vault.py`」，而脚本**根本不读 `.env`**。
照做的人骨架建在作者那个路径上、服务的 `.env` 指着别处，落回同一个半截状态，
**而且看起来是成功的**。

已在 main 上修掉（`9a132c0`）：路径改必填、挡相对路径、`NO_VAULT_MESSAGE` 改成
「先建库、再把该路径填进 `.env`」。**Task 8 收尾时不用再管这一条。**

**勘误九（Task 4 实跑时实现者报的）：Step 6(e) 给 `new_note_post` 的片段漏了 `vault()`。**

那段是按「只包一层锁」的意思写的，却把仓库里本来就有的一行 `vault()` 漏掉了
（`router.py:316`，Task 3 Step 10(f) 特意加的「没库就 409，别等写了一半才发现」）。
照抄的后果：未初始化时 `/new` 走 `push_and_organize` → `require_vault` 抛
`ConfigError`，**没有任何 handler 接**，回 **500 而不是 409**。而 `/setup/*` 那几条
测试只覆盖 `/push` `/organize` `/inbox` `/search`，**不覆盖 `/new`**——漏了不会有
人告诉你。片段已修对并写了原因；实现者当时也自己看出来并保留了那一行。

同一次实跑还报了两处 **ruff F401**：

- 计划给 `lifecycle.py` 的模块头 import 了 `os` / `shutil` / `datetime` / `Path`，
  但 Task 4 只放 `Busy`，那四个当时都用不上。实现者只 import 了 `threading`。
  **Task 5 往里加 `check_path` / `migrate_vault` / `remove_vault` 时，记得把
  用得上的那几个补回去**（别以为「Task 4 已经有了」）。
- `tests/api/test_busy.py` 里的 `from pathlib import Path` 也用不上，已删。

---

## Task 1: 拆掉 `DEFAULT_VAULT_PATH`（地基一）

**这一步为什么排第一：** `config.py:27` 的 `DEFAULT_VAULT_PATH = Path(r"E:\KB_Library")` 配上 `config.py:117` 的回落，意味着**别人 `.env` 里留空就会写进作者本机的库**——而且不报错、看起来一切正常（`list_drafts` / `list_domains` 在目录不存在时都静默返回 `[]`）。「未初始化」这个状态要能被表示，跟这个回落直接冲突。碰到面最广，先做先暴露。

**Files:**
- Modify: `src/kb/config.py`
- Modify: `src/kb/api/http.py`
- Modify: `src/kb/api/cli.py`
- Modify: `src/kb/web/router.py`
- Test: `tests/test_config.py`

**好消息（摸底已确认）：** 所有测试构造 `Config` 时 `vault_path` 都是第 4 个位置参数（`Config("k", "u", "m", tmp_path, None)` 共 15 处）。字段**位置和名字都不动**，只把类型放宽成 `Path | None`，所以**这些测试一处都不用改**。只有 `tests/test_config.py` 那两条断言会红。

---

- [ ] **Step 1: 先写失败测试**

打开 `tests/test_config.py`，把第 6 行的 import 改成：

```python
from kb.config import (
    NO_VAULT_MESSAGE,
    Config,
    ConfigError,
    load_config,
    reload_config,
    require_vault,
)
```

把 `test_vault_path_defaults_when_absent`（第 48-50 行）整条替换成下面三条：

```python
def test_vault_path_is_none_when_absent(tmp_path):
    """**空值表示「没有」，不表示「回落到作者本机那个」。**

    这条是防雷测试。原先这里回落 `E:\\KB_Library`——别人的服务会安安静静地
    写进作者的库，而且看起来一切正常。
    """
    cfg = load_config(_write_env(tmp_path, VALID))
    assert cfg.vault_path is None


def test_no_hardcoded_vault_path_constant():
    """连名字都不许留——留着就会被下一个人接回去。"""
    import kb.config as config

    assert not hasattr(config, "DEFAULT_VAULT_PATH")


def test_require_vault_message_spells_out_both_steps():
    """没库时那句文案**必须说全两步**。

    `scripts/init_vault.py` 只建目录，**它不写 `.env`**（写配置一直是服务的活）。
    只说「跑这个脚本」会把人引到一个半截状态：库建好了、服务还是不知道该看哪儿。
    """
    with pytest.raises(ConfigError, match="初始化知识库"):
        require_vault(Config("k", "u", "m", None, None))
    assert "scripts/init_vault.py" in NO_VAULT_MESSAGE
```

- [ ] **Step 2: 跑一遍，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/test_config.py -v
```

预期：`ImportError: cannot import name 'NO_VAULT_MESSAGE'`（整份文件收集失败，不是 3 条红）。

- [ ] **Step 3: 改 `src/kb/config.py`**

**(a)** 删掉第 27 行整个常量和它下面一个空行：

```python
DEFAULT_VAULT_PATH = Path(r"E:\KB_Library")
```

**(b)** 第 40 行的字段。原来是：

```python
    vault_path: Path
```

改成：

```python
    # **`None` 表示「还没有知识库」**，不是「用默认的」。回落到一个写死的
    # 路径就是「别人的服务写进作者的库」，见 `require_vault`。
    vault_path: Path | None
```

**(c)** 在 `Config` 类之后（`_VALID_LOG_LEVELS` 之前）插入：

```python
# 没有知识库时，每条路都用这一句。**说全两步**——`scripts/init_vault.py`
# 只建目录，**它不写 `.env`**（写配置一直是服务的活）。只说「跑这个脚本」
# 会把人引到一个半截状态：库建好了、服务还是不知道该看哪儿。
# 文案必须 GBK 可编码：中文 Windows 的控制台与管道都是 GBK，编不出来的字符
# 会抛 UnicodeEncodeError（`tests/test_architecture.py` 守着这条）。
NO_VAULT_MESSAGE = (
    "还没有知识库。请在网页上点「初始化知识库」，"
    "或手工把 .env 的 KB_VAULT_PATH 填好、再跑 python scripts/init_vault.py"
)


def require_vault(cfg: Config) -> Path:
    """拿生效的 vault 路径。没配就抛 `ConfigError`。

    **空值表示「没有」，不表示「用作者本机那个」。** 原来这里回落到一个写死的
    `E:\\KB_Library`——别人的服务会安安静静地写进作者的库，而且看起来一切正常
    （`list_drafts` / `list_domains` 在目录不存在时都静默返回 `[]`）。
    这是设计里点名要先拆的雷，见 spec 第三节。
    """
    if cfg.vault_path is None:
        raise ConfigError(NO_VAULT_MESSAGE)
    return cfg.vault_path
```

**(d)** 第 117 行的回落：

```python
        vault_path=Path(vault_raw) if vault_raw else DEFAULT_VAULT_PATH,
```

改成：

```python
        vault_path=Path(vault_raw) if vault_raw else None,
```

- [ ] **Step 4: 跑 `tests/test_config.py`，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/test_config.py -v
```

预期：全绿。

- [ ] **Step 5: 改 `src/kb/api/http.py`——加 `vault_guard` 工厂**

第 27 行的 import 改成：

```python
from kb.config import (
    DATA_DIR,
    PROJECT_ROOT,
    Config,
    ConfigError,
    load_config,
    reload_config,
    require_vault,
)
```

在 `build_llm`（第 70 行）之前插入：

```python
def vault_guard(get_cfg: Callable[[], Config]) -> Callable[[], Path]:
    """造一个「取 vault 路径；没配就 409」的函数。

    **端点必须自己拦，不能只靠界面置灰**：`kb.bat push` 走 CLI、接入指南里的
    会话层 AI 走 `/push`，它们都绕过界面。

    用 409 不用 400：请求本身没写错，是**服务端现在没有可写的东西**——
    跟「资源冲突」是同一类，什么时候能重试取决于用户去建库。
    """
    def guard() -> Path:
        try:
            return require_vault(get_cfg())
        except ConfigError as exc:
            raise HTTPException(status_code=409, detail=str(exc)) from exc
    return guard
```

- [ ] **Step 6: 改 `src/kb/api/http.py`——把 `cfg.vault_path` 换成 `require_vault(cfg)`**

`push_draft` 第 119 行：

```python
            write_draft(cfg.vault_path, draft)
```

改成：

```python
            write_draft(require_vault(cfg), draft)
```

`push_and_organize`：在函数体开头（`run = flow.new_run_id()` 之前）加一行，并换掉两处用法。改完这一段长这样：

```python
    vault = require_vault(cfg)      # 没配就当场抛，别等写了一半才发现
    run = flow.new_run_id()
    flow.set_run(run)

    text, draft_id = push_draft(
        cfg, content, source=source, revise_target=revise_target
    )
    if not draft_id:
        return text

    results = organize.organize_selected(
        vault,
        [p for p in [find_draft(vault, draft_id)] if p],
        llm,
        run_id=run,
    )
```

- [ ] **Step 7: 改 `src/kb/api/http.py`——`create_app` 里造 guard**

在 `get_cfg` 定义之后、`get_llm` 之前插入：

```python
    vault = vault_guard(get_cfg)
```

- [ ] **Step 8: 改 `src/kb/api/http.py`——八个端点换成 `vault()`**

逐个改（行号是改动前的）：

`/drop`（约 445-458）：

```python
        dropped: list[str] = []
        missing: list[str] = []
        vault_path = vault()            # 没库就 409，别在循环里才发现
        for draft_id in ids:
            path = find_draft(vault_path, draft_id)
            if path is None:
                missing.append(draft_id)
                continue
            remove_draft(path)
            dropped.append(draft_id)
        return {
            "dropped": dropped,
            "missing": missing,
            "remaining": len(list_drafts(vault_path)),
        }
```

`/inbox`（约 460-463）开头：

```python
    @app.get("/inbox")
    def inbox() -> dict:
        vault_path = vault()
        items = []
        for path in list_drafts(vault_path):
```

`/search`（约 482-488）开头：

```python
    @app.get("/search")
    def search(q: str = "") -> dict:
        vault_path = vault()
        hits = search_notes(vault_path, q)
        items = []
        for hit in hits:
            items.append({
                "path": hit.path.relative_to(vault_path).as_posix(),
```

`/organize`（约 497-514）开头：

```python
    @app.post("/organize")
    def run_organize(req: OrganizeRequest) -> dict:
        """整理草稿。不传 draft_id 就整理全部（Q38）。"""
        vault_path = vault()
        if req.draft_id:
            path = find_draft(vault_path, req.draft_id)
            if path is None:
                raise HTTPException(
                    status_code=404, detail=f"找不到草稿 {req.draft_id}"
                )
            paths = [path]
        else:
            paths = list_drafts(vault_path)
```

同函数末尾那行：

```python
        results = organize.organize_selected(vault_path, paths, get_llm())
```

`/chat`（约 529-538）：

```python
        vault_path = vault()
        chat_id, reply = handle(
            data_dir,
            vault_path,
            req.chat_id,
            req.message,
            get_llm(),
            organize_fn=_chat_organize_fn,
        )
```

`/sweep`（约 558）：

```python
            return run_sweep(vault(), data_dir, get_llm())
```

- [ ] **Step 9: 改 `src/kb/api/http.py` 的 `main()`——自动巡检要挡一道**

第 619 行原来是：

```python
    if sweep_state.due(DATA_DIR, interval_days=cfg.sweep_interval_days):
```

改成：

```python
    # **没有知识库就没有可巡检的东西。** 不挡的话 `run_sweep(None, ...)`
    # 会当场炸在后台线程里——那儿的异常没人接，只会静默消失。
    if cfg.vault_path and sweep_state.due(
        DATA_DIR, interval_days=cfg.sweep_interval_days
    ):
```

下面 `args=(cfg.vault_path, DATA_DIR, build_llm(cfg))` 不动——上面那个 `if` 已经保证了它不是 `None`。

- [ ] **Step 10: 改 `src/kb/web/router.py`**

**(a)** import 加上 `vault_guard` 与 `NO_VAULT_MESSAGE`：

```python
from kb.api.http import push_and_organize, run_sweep, vault_guard
from kb.config import NO_VAULT_MESSAGE, Config
```

**(b)** `build_router` 里、`_ctx` 之前加：

```python
    vault = vault_guard(get_cfg)
```

**(c)** `_ctx` 第一行改成（未初始化时侧栏那行不能显示字面量 `None`）：

```python
    def _ctx(name: str, **extra) -> dict:
        # `get_cfg()` 每请求现读，所以换皮肤**下一个请求就生效**，不用重启。
        # **皮肤只在这一处注入**：六个页面 + 设置片段都走这个函数，
        # 每个端点各传一次迟早漏一个——而漏掉的那页正是「切了皮肤没反应」
        # 的那一页，而且多半是你没点开的那页。
        vault_path = get_cfg().vault_path
        return {
            "active": name,
            # 未初始化时是空串，不是 `None`——侧栏要显示的是「没有」，
            # 不是一个 Python 字面量。
            "vault": str(vault_path) if vault_path else "",
            "skin": get_cfg().skin,
            "skins": skins.options(),
            **extra,
        }
```

**(d)** `chat_post`（约 256-266）：`get_cfg().vault_path` → `vault()`。

**(e)** `journal`（约 287-301）整段换成：

```python
    @router.get("/journal", response_class=HTMLResponse)
    def journal(request: Request, d: str = ""):
        # **未初始化时这一页照样打得开**，只是空的。首屏那张引导卡得有个
        # 落脚的地方——页面上甩一坨 409 JSON 不成样子。
        vault_path = get_cfg().vault_path
        days = journal_days(vault_path) if vault_path else []
        day = _pick_day(d, days)
        return templates.TemplateResponse(
            request,
            "journal.html",
            _ctx(
                "journal",
                days=days,
                day=day,
                sections=(
                    read_journal(vault_path, day)
                    if (vault_path and day) else []
                ),
                latest=latest_run_rows(data_dir),
            ),
        )
```

**(f)** `new_note_post`（约 279-285）：在 `push_and_organize` 之前加一行。

```python
    @router.post("/new")
    def new_note_post(content: str = Form(...)):
        """只填正文（Q87）——`source` 记成 Web，项目留空，其余归整理。"""
        if not content.strip():
            raise HTTPException(status_code=400, detail="正文不能为空")
        vault()                     # 没库就 409，别等写了一半才发现
        push_and_organize(get_cfg(), content, get_llm(), source="Web")
        return RedirectResponse("/journal", status_code=303)
```

**(g)** `_run_sweep_or_report`（约 320-337）：**这里不用 `vault()`**——它是个页面表单，回 409 JSON 很难看。改成落一份「没跑成」的报告，跟下面那条失败路径一个语义：

```python
    def _run_sweep_or_report() -> RedirectResponse:
        """跑一次巡检，失败就落一份没读的报告——**人在这儿等着，不能甩 500**。

        写成报告的语义和后台那条失败路径一致（`_sweep_in_background` 也这么落），
        巡检页上看得见原因。**没有知识库也走这条路**：`/sweep` 是个页面表单，
        回 409 JSON 很难看，而且用户看不出下一步该干什么。
        """
        vault_path = get_cfg().vault_path
        if vault_path is None:
            sweep_state.save_report(
                data_dir,
                {
                    "summary": f"这次巡检没跑成：{NO_VAULT_MESSAGE}",
                    "tag_merges": [],
                    "dir_merges": [],
                },
            )
            return RedirectResponse("/sweep", status_code=303)

        try:
            run_sweep(vault_path, data_dir, get_llm())
        except sweep.SweepError as exc:
            sweep_state.save_report(
                data_dir,
                {
                    "summary": f"这次巡检没跑成：{exc}",
                    "tag_merges": [],
                    "dir_merges": [],
                },
            )
        return RedirectResponse("/sweep", status_code=303)
```

`/settings/test`（约 462-468）里的 `vault_path=cfg.vault_path` **保持不动**——`Config` 收 `Path | None`，把这个值原样传下去就行。

- [ ] **Step 11: 改 `src/kb/api/cli.py`**

**(a)** `cmd_status` 第 198 行：

```python
    print(f"vault: {cfg.vault_path or '（还没有知识库，去网页上点「初始化知识库」）'}")
```

**(b)** `main()`（第 271-284 行）整个换成：

```python
def main(argv: list[str] | None = None) -> int:
    args = build_parser().parse_args(argv)
    try:
        return args.func(args)
    except ConfigError as exc:
        print(f"配置错误：{exc}", file=sys.stderr)
        return 2
    except httpx.HTTPStatusError as exc:
        # 4xx 的 `detail` 就是给人看的那句话（409 时是「还没有知识库。……」）。
        #
        # **必须排在 `httpx.HTTPError` 前面**——它是前者的子类，放后面永远
        # 轮不到，用户看到的会是 `Client error '409 Conflict' for url ...`，
        # 一句英文，完全看不出该干什么。
        try:
            detail = exc.response.json().get("detail")
        except ValueError:
            detail = None
        print(detail or f"服务返回 {exc.response.status_code}", file=sys.stderr)
        return 1
    except httpx.HTTPError as exc:
        print(f"与服务通信失败：{exc}", file=sys.stderr)
        return 1
```

- [ ] **Step 12: 跑全量测试（地基一步，必须全量）**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
```

预期：全绿，数量与改动前一致（**没有新增用例之外的红**）。若 `tests/test_architecture.py` 报「GBK 编不出来」，检查 `NO_VAULT_MESSAGE` 里有没有 emoji 或生僻符号——中文引号「」和破折号都是 GBK 内的，安全。

- [ ] **Step 13: 风格检查**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **Step 14: 提交**

```bash
git -C "E:/Study_Projects/KN_Base" add src/kb/config.py src/kb/api/http.py src/kb/api/cli.py src/kb/web/router.py tests/test_config.py
git -C "E:/Study_Projects/KN_Base" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t1.txt"
```

提交信息写进那个临时文件（长中文用 `-F`，避免 shell 引号问题），内容：

```
refactor: 拆掉 DEFAULT_VAULT_PATH——空值表示「没有」，不是「用作者本机那个」

给别人用的第一步。config.py:117 原先在 KB_VAULT_PATH 留空时回落到
Path(r"E:\KB_Library")——作者本机的路径。别人的服务会安安静静写进作者的库，
而且看起来一切正常（list_drafts / list_domains 在目录不存在时都返回 []）。

而「未初始化」这个状态要能被表示，跟这个回落直接冲突。所以：

- 删掉 DEFAULT_VAULT_PATH，Config.vault_path 放宽成 Path | None
- 新增 require_vault(cfg)：没配就抛 ConfigError，文案说全两步
  （init_vault.py 只建目录，它不写 .env）
- 新增 vault_guard(get_cfg)：端点拿到就 409，不是 400——请求没写错，
  是服务端现在没有可写的东西
- CLI 加 HTTPStatusError 分支，4xx 的 detail 原样印出来；
  它必须排在 httpx.HTTPError 前面，否则是子类、永远轮不到
- main() 的自动巡检加 vault 判断，否则 run_sweep(None, ...) 炸在后台线程里

所有测试构造 Config 时 vault_path 都是第 4 个位置参数，字段位置没动，
所以 15 处构造点一处没改。新增三条防雷用例，其中一条断言
kb.config 里不再有 DEFAULT_VAULT_PATH 这个名字。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 2: `init_vault` 下沉到 `core`（地基二）

**为什么搬：** `scripts/` 与 `src/kb/` 之间**现在没有桥**——`src/` 下没有任何文件 import `scripts`，`pyproject.toml` 的 `pythonpath` 只有 `src`。服务进程**恰好**能 import 它，是因为 `spawn_service` 用 `-m kb.api.http` 且 `cwd=PROJECT_ROOT`，`python -m` 把 cwd 放进了 `sys.path[0]`。**那是巧合不是设计**：换个人从别的 cwd 起服务就断。而且「vault 结构的唯一真源」放在服务读不到的地方，本来就不该。

**Files:**
- Create: `src/kb/core/vault_setup.py`
- Modify: `src/kb/core/vault.py`
- Modify: `scripts/init_vault.py`
- Modify: `tests/test_init_vault.py`
- Test: `tests/core/test_vault_setup.py`（新建）

---

- [ ] **Step 1: 写失败测试**

新建 `tests/core/test_vault_setup.py`：

```python
"""建 vault 骨架。这个模块是**知识库结构的唯一真源**。"""

import subprocess

from kb.core.vault import ATTACHMENTS, INBOX, INDEX, JOURNAL_DIR, PENDING
from kb.core.vault_setup import (
    DEFAULT_DOMAINS,
    DOMAIN_CANDIDATES,
    init_vault,
    vault_dirs,
)


def test_creates_machine_dirs_without_domains(tmp_path):
    """不给领域就只建机器目录——领域是可选的，不是骨架的必需品。"""
    init_vault(tmp_path)
    for rel in (INBOX, f"{INBOX}/{PENDING}", INDEX, f"{INDEX}/{JOURNAL_DIR}",
                ATTACHMENTS):
        assert (tmp_path / rel).is_dir(), f"缺少目录 {rel}"


def test_creates_only_the_domains_it_is_given(tmp_path):
    """勾了哪几个就建哪几个，**不多建**。

    多建的那天，「领域名让用户选」就名存实亡了。
    """
    init_vault(tmp_path, ["计算机", "健康"])
    assert (tmp_path / "计算机").is_dir()
    assert (tmp_path / "健康").is_dir()
    assert not (tmp_path / "艺术").exists()


def test_default_domains_are_a_subset_of_candidates():
    """两边会漂移——加候选时忘了默认、或反过来，界面上就是空勾或勾错。"""
    assert set(DEFAULT_DOMAINS) <= set(DOMAIN_CANDIDATES)
    assert DEFAULT_DOMAINS, "至少得默认勾一个，否则用户一进来就得自己找"


def test_domain_candidates_are_unique():
    """重复的名字在建目录时不报错（`exist_ok=True`），但界面上会出现两个
    一模一样的复选框——勾一个，另一个看起来没勾。"""
    assert len(DOMAIN_CANDIDATES) == len(set(DOMAIN_CANDIDATES))


def test_writes_gitignore_ignoring_workspace(tmp_path):
    """Q53：workspace.json 每次开关 Obsidian 都变，必须忽略。"""
    init_vault(tmp_path)
    text = (tmp_path / ".gitignore").read_text(encoding="utf-8")
    assert ".obsidian/workspace.json" in text


def test_initializes_git_repo_and_commits(tmp_path):
    """骨架要有首次提交，否则 .gitignore 一直悬着未跟踪。"""
    init_vault(tmp_path)
    assert (tmp_path / ".git").is_dir()
    out = subprocess.run(
        ["git", "log", "--oneline"],
        cwd=tmp_path,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
    )
    assert out.returncode == 0
    assert "初始化 vault 骨架" in out.stdout


def test_does_not_touch_existing_gitignore(tmp_path):
    """幂等：已存在的 .gitignore 不能被覆盖——用户可能自己加过规则。"""
    (tmp_path / ".gitignore").write_text("# 我自己加的\n", encoding="utf-8")
    init_vault(tmp_path)
    assert (tmp_path / ".gitignore").read_text(encoding="utf-8") == "# 我自己加的\n"


def test_is_idempotent(tmp_path):
    """重复执行不报错，且第二次无事可做。**界面上那句「幂等」的底气。**"""
    init_vault(tmp_path, ["计算机"])
    assert init_vault(tmp_path, ["计算机"]) == []


def test_second_run_keeps_existing_notes(tmp_path):
    """幂等：不能删掉用户已经写进去的内容。"""
    init_vault(tmp_path)
    note = tmp_path / "计算机" / "已有笔记.md"
    note.write_text("内容", encoding="utf-8")
    init_vault(tmp_path)
    assert note.read_text(encoding="utf-8") == "内容"


def test_reports_what_it_did(tmp_path):
    """返回动作清单，便于调用方展示——空清单表示什么都没做。"""
    actions = init_vault(tmp_path)
    assert actions
    assert any("git" in a for a in actions)


def test_vault_dirs_are_absolute_under_root(tmp_path):
    for d in vault_dirs(tmp_path, ["计算机"]):
        assert d.is_relative_to(tmp_path)


def test_script_is_a_thin_shell():
    """`scripts/init_vault.py` 必须是**同一份实现**，不是抄了一份。

    抄一份的那天开始两边就会漂移——改了一个忘了另一个，而 README 还指着
    脚本说「这是唯一真源」。
    """
    from kb.core import vault_setup
    from scripts import init_vault as script

    assert script.init_vault is vault_setup.init_vault
```

- [ ] **Step 2: 跑一遍，确认它红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_vault_setup.py -v
```

预期：`ModuleNotFoundError: No module named 'kb.core.vault_setup'`。

- [ ] **Step 3: 新建 `src/kb/core/vault_setup.py`**

把 `scripts/init_vault.py` 的逻辑整个搬过来，加 `domains` 参数和新常量：

```python
"""建 vault 骨架。幂等——可重复执行，不破坏已有内容。

**这个模块是知识库结构的唯一真源**：实际目录是它的产物。
想调结构就改这里重跑，不要手工建目录。

**为什么住在 `core/` 而不是 `scripts/`**：服务要调它（设置窗那条龙）。
`src/` 与 `scripts/` 之间没有桥——`pyproject.toml` 的 `pythonpath` 只有 `src`，
`src/` 下也没有任何文件 import `scripts`。服务进程「恰好」能 import 是因为
`spawn_service` 用 `-m kb.api.http` 且 `cwd=PROJECT_ROOT`，`python -m` 把 cwd
放进了 `sys.path[0]`——**那是巧合不是设计**，换个人从别的 cwd 起服务就断。
"""

from __future__ import annotations

import subprocess
from pathlib import Path

from kb.core.vault import ATTACHMENTS, INBOX, INDEX, JOURNAL_DIR, PENDING

# 开库时可勾的领域清单。**九个候选**——界面把它们画成复选框。
#
# **清单不进 `.env`，设置里也没有这一栏。** 领域的唯一真源始终是磁盘
# （`vault.list_domains` 直接读目录），勾选的作用只是建出对应的目录，
# 建完就不再被读第二次。要加领域 = 建文件夹，不走配置。
DOMAIN_CANDIDATES = [
    "计算机", "科学", "艺术", "文学", "历史", "哲学", "生活", "工作", "健康",
]

# 界面上默认勾上的那几个。
DEFAULT_DOMAINS = ["计算机"]

VAULT_GITIGNORE = """\
# Obsidian 工作区状态——每次开关都变，入库会淹没历史（Q53）
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache

# 系统文件
.DS_Store
Thumbs.db
"""


def vault_dirs(vault_root: Path, domains: list[str] | None = None) -> list[Path]:
    """列出骨架包含的全部目录（顺序即创建顺序）。

    `domains` 不给就只建机器目录——领域是**可选的**，不是骨架的必需品。
    """
    dirs = [
        vault_root / INBOX,
        vault_root / INBOX / PENDING,
        vault_root / INDEX,
        vault_root / INDEX / JOURNAL_DIR,
        vault_root / ATTACHMENTS,
    ]
    dirs += [vault_root / d for d in (domains or [])]
    return dirs


def _git(vault_root: Path, *args: str) -> subprocess.CompletedProcess:
    """调用 git。

    **必须显式指定 utf-8 与 errors="replace"。** Windows 下 `text=True` 会按
    本地编码（GBK）解码，而 git 输出（中文 commit message、路径）是 UTF-8——
    解码异常抛在子进程的读取线程里，主流程只看到 `stdout=None`，极难排查。
    """
    return subprocess.run(
        ["git", *args],
        cwd=vault_root,
        capture_output=True,
        text=True,
        encoding="utf-8",
        errors="replace",
    )


def _has_commits(vault_root: Path) -> bool:
    return _git(vault_root, "rev-parse", "HEAD").returncode == 0


def init_vault(vault_root: Path, domains: list[str] | None = None) -> list[str]:
    """建目录、写 .gitignore、初始化 git。返回本次实际执行的动作。

    只做缺失的部分，已有的东西一律不碰——包括用户自己改过的 `.gitignore`。
    **幂等**：重复点不会毁掉已有的东西，这是界面上那句提示的底气。

    `domains` 是这次要建的领域目录。不给就只建机器目录——命令行那条
    （`python scripts/init_vault.py <路径>` 不带领域）行为不变。
    """
    actions: list[str] = []

    for d in vault_dirs(vault_root, domains):
        if not d.exists():
            d.mkdir(parents=True, exist_ok=True)
            actions.append(f"建目录 {d.relative_to(vault_root)}")

    gitignore = vault_root / ".gitignore"
    if not gitignore.exists():
        gitignore.write_text(VAULT_GITIGNORE, encoding="utf-8")
        actions.append("写 .gitignore")

    if not (vault_root / ".git").exists():
        _git(vault_root, "init", "-b", "main")
        actions.append("初始化 git 仓库")

    if not _has_commits(vault_root) and gitignore.exists():
        _git(vault_root, "add", ".gitignore")
        _git(vault_root, "commit", "-m", "chore: 初始化 vault 骨架")
        actions.append("首次提交")

    return actions
```

- [ ] **Step 4: 改 `src/kb/core/vault.py`——搬走 `SEED_DOMAINS`**

删掉第 40-41 行：

```python
# 内置领域——`init_vault.py` 用它建目录。运行时的领域清单以磁盘为准。
SEED_DOMAINS = ["计算机", "艺术", "文学"]
```

**注意：** 全仓库只有 `scripts/init_vault.py` 和 `tests/test_init_vault.py` 引用它，两个文件下一步都会改。`docs/03_问题记录.md:962` 里那句「`SEED_DOMAINS`，新增要改脚本或手工建」是文档，Task 7 一起改。

- [ ] **Step 5: 改 `scripts/init_vault.py` 成薄壳**

整个文件替换成：

```python
"""建 vault 骨架。**薄壳**——实现住在 `kb.core.vault_setup`。

搬家的理由：服务要调它（设置窗那条龙），而 `src/` 与 `scripts/` 之间没有桥
（`pyproject.toml` 的 `pythonpath` 只有 `src`）。服务进程「恰好」能 import 它
是巧合不是设计——见 `kb/core/vault_setup.py` 开头那段。

直接运行：

    python scripts/init_vault.py [vault 路径]
"""

from __future__ import annotations

import sys
from pathlib import Path

# 让脚本能直接跑：pytest 走 pyproject 里的 pythonpath，直接运行则没有，
# 于是 `from kb...` 会 ModuleNotFoundError。
sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "src"))

from kb.core.vault_setup import (  # noqa: E402 —— 必须在 sys.path 自举之后
    DEFAULT_DOMAINS,
    DOMAIN_CANDIDATES,
    VAULT_GITIGNORE,
    init_vault,
    vault_dirs,
)

__all__ = [
    "DEFAULT_DOMAINS",
    "DOMAIN_CANDIDATES",
    "VAULT_GITIGNORE",
    "init_vault",
    "vault_dirs",
]


if __name__ == "__main__":
    root = Path(sys.argv[1]) if len(sys.argv) > 1 else Path(r"E:\KB_Library")
    done = init_vault(root, DEFAULT_DOMAINS)
    if done:
        print(f"初始化 {root}：")
        for line in done:
            print(f"  {line}")
    else:
        print(f"{root} 已是完整骨架，无需改动。")
```

- [ ] **Step 6: 删掉老的 `tests/test_init_vault.py`**

那一份的用例已经全部搬进 `tests/core/test_vault_setup.py`，而且搬过去的那份更全（多了领域相关三条）。**直接删文件，不要留两份**：

```bash
git -C "E:/Study_Projects/KN_Base" rm tests/test_init_vault.py
```

- [ ] **Step 7: 跑测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_vault_setup.py tests/test_config.py -v
```

预期：全绿。

- [ ] **Step 8: 跑全量（地基二，必须全量）**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
```

预期：全绿。总数 = 改动前 − 9（删掉的旧用例）+ 13（新增的）= 净 +4。

- [ ] **Step 9: ruff + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
git -C "E:/Study_Projects/KN_Base" add -A src/kb/core/vault_setup.py src/kb/core/vault.py scripts/init_vault.py tests/core/test_vault_setup.py tests/test_init_vault.py
git -C "E:/Study_Projects/KN_Base" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t2.txt"
```

```
refactor: init_vault 下沉到 kb.core.vault_setup，scripts/ 变薄壳

服务要调它（设置窗那条龙），而 src/ 与 scripts/ 之间没有桥：pythonpath
只有 src，src/ 下也没有任何文件 import scripts。服务进程「恰好」能 import
是因为 spawn_service 用 -m kb.api.http 且 cwd=PROJECT_ROOT，python -m 把
cwd 放进了 sys.path[0]——巧合不是设计，换个人从别的 cwd 起服务就断。
「vault 结构的唯一真源」本来也不该放在服务读不到的地方。

- 新建 kb/core/vault_setup.py：vault_dirs / VAULT_GITIGNORE / init_vault
  / DOMAIN_CANDIDATES / DEFAULT_DOMAINS
- init_vault 加 domains 参数；不给就只建机器目录，命令行那条行为不变
- SEED_DOMAINS 从 vault.py 删除（它是「内置领域」，与「候选清单」是两回事）
- scripts/init_vault.py 只剩 sys.path 自举 + 命令行入口 + 再导出
- 测试搬到 tests/core/test_vault_setup.py 并补齐：只建给定的领域、
  DEFAULT ⊆ CANDIDATES、候选不重复、脚本是同一份实现（不是抄的）

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 3: 放开 `KB_VAULT_PATH` + 路径校验

**这一步为什么必须和 Task 1 一起看：** 只读字段后端也不信前端（`settings.validate` 会把只读的从提交里丢掉）。放开它之后，「改路径」这个动作就真的能生效了——所以校验必须同一批做，否则用户可以凭空把服务指向一个空目录，把库换废。

**Files:**
- Modify: `src/kb/core/settings.py`
- Test: `tests/core/test_settings.py`

---

- [ ] **Step 1: 先改既有测试，让它反映新契约**

`tests/core/test_settings.py` 的 `test_manifest_snapshot`（第 141-143 行）里那段：

```python
        ("知识库", [
            ("KB_VAULT_PATH", "readonly", "", ()),
        ]),
```

改成：

```python
        ("知识库", [
            ("KB_VAULT_PATH", "text", "", ()),
        ]),
```

`test_readonly_fields_are_not_editable`（第 157-162 行）整条换成：

```python
def test_readonly_fields_are_not_editable():
    """端口改了就换地址、书签作废，仍然只读。

    **vault 路径不再只读**（2026-09-22）。当初把它设成只读是本机被改废过的
    教训，现在敢放开，靠的是两条机械约束：改路径时目标必须是个已初始化的库
    （`validate` 里那条 `_vault_path_problem`），以及「搬家」这个显式动作。
    放开的是输入框，不是判断。
    """
    keys = editable_keys()
    assert "KB_VAULT_PATH" in keys
    assert "KB_PORT" not in keys
    assert "KB_LLM_MODEL" in keys
```

- [ ] **Step 2: 加新用例**

在同一个文件末尾追加：

```python
# ---------- 改路径（spec 9.2） ----------

def _vault_at(tmp_path, *, with_git: bool) -> str:
    """造一个（或不是）已初始化的库，返回它的路径字符串。"""
    path = tmp_path / "库"
    path.mkdir()
    if with_git:
        (path / ".git").mkdir()
    return str(path)


def test_vault_path_accepts_an_initialized_vault(tmp_path):
    """**这是「换机器后路径变了」那条路**——库还在，只是位置不同。"""
    clean = validate({"KB_VAULT_PATH": _vault_at(tmp_path, with_git=True)})
    assert clean["KB_VAULT_PATH"] == _vault_at(tmp_path, with_git=True)


def test_vault_path_rejects_a_plain_directory(tmp_path):
    """指到一个没有 `.git` 的目录 = 把服务换废：收件箱建起来、领域是空的、
    每条都掉进待归类。文案要告诉用户两条正路。"""
    with pytest.raises(SettingsError) as exc:
        validate({"KB_VAULT_PATH": _vault_at(tmp_path, with_git=False)})
    assert "迁移到别处" in str(exc.value)
    assert "移除知识库" in str(exc.value)


def test_vault_path_rejects_a_missing_path(tmp_path):
    with pytest.raises(SettingsError, match="还没有知识库"):
        validate({"KB_VAULT_PATH": str(tmp_path / "根本没有这个目录")})


def test_vault_path_rejects_empty(tmp_path):
    """**不给「留空 = 解绑」。**

    那会留下笔记、会话、日志在原地，而界面上显示成「还没有知识库」——
    痕迹还在、界面说没了，正是本设计要消灭的那种状态。解绑走「移除知识库」。
    """
    with pytest.raises(SettingsError, match="移除知识库"):
        validate({"KB_VAULT_PATH": ""})


def test_missing_vault_key_is_not_an_error():
    """`.env` 里没这一行 ≠ 用户想把路径改成空。

    `validate` 只处理**提交上来的**键；设置窗没提交它，就不该报错。
    这条和上一条是一对，别把空的当成一个错误。
    """
    assert validate({"KB_LLM_MODEL": "m"}) == {"KB_LLM_MODEL": "m"}
```

- [ ] **Step 3: 跑，确认红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_settings.py -v
```

预期：`test_manifest_snapshot`、`test_readonly_fields_are_not_editable`、以及 5 条新用例全红。

- [ ] **Step 4: 改 `src/kb/core/settings.py` 的字段定义**

第 58-61 行：

```python
    Group("知识库", (
        Field("KB_VAULT_PATH", "vault 路径", "readonly",
              help="换库不是常事，换错了整个服务当场废。要改去改 .env"),
    )),
```

改成：

```python
    Group("知识库", (
        # 2026-09-22 从 `readonly` 放开。当初设成只读是本机被改废过的教训，
        # 现在敢放开，靠的是 `validate` 里那条路径检查（目标必须是个已初始化
        # 的库）+「迁移」这个显式动作——**放开的是输入框，不是判断**。
        Field("KB_VAULT_PATH", "vault 路径", "text",
              help="笔记本体存的地方，是个独立的 git 仓库。"
                   "要搬去别处用下面的「迁移到别处」，不要在这儿直接改"),
    )),
```

- [ ] **Step 5: 改 `src/kb/core/settings.py` 的 `validate`**

**(a)** 文件头那段 docstring 里的四条硬规矩，第一条后面追加一句：

```python
**只读的字段，后端也不信前端。** vault 路径换错了整个服务当场废、端口改了
就换地址（书签作废，见 `04_踩坑与经验.md` 第 20 条）——前端不给改，
后端**也**要把它们从提交里丢掉，不能只靠界面。
（vault 路径 2026-09-22 已移出这一条，改由下面那条单独的路径检查守着。）

**路径不是随便哪个目录都行。** `KB_VAULT_PATH` 提交上来时必须是**一个已初始化
的库**（含 `.git`）。详见 `_vault_path_problem`——空值、不存在的路径、没 `.git`
的目录都拒，各有各的出口文案。
```

**(b)** 在 `validate` 函数**之前**插入：

```python
# ------------------------------------------------------------ vault 路径

def _vault_path_problem(where: Path) -> str | None:
    """检查这个路径能不能当库用。返回一句人话，没问题返回 `None`。

    **只认「已初始化的库」= 含 `.git`。** 为什么用 `.git` 而不是「目录存在」：
    `init_vault` 必然建 `.git`，而用户随便填一个空目录不会有。用「目录存在」
    判，用户填了个现成的空文件夹就会被当成「已初始化」，然后投递进去——
    `atomic_write` 会自己 `mkdir(parents=True)`，于是**在一个不是库的地方长出
    半个库**：收件箱有草稿、领域是空的、每条都掉进待归类。那正是要防的
    「看起来一切正常」。

    **三种失败各有各的出口**，因为用户下一步该干的事不一样：
    空值 → 用「移除知识库」；路径不存在 / 没 `.git` → 要么迁移过去、要么先移除
    再重建。
    """
    if not str(where).strip():
        return "要解绑请用「移除知识库」，它会把痕迹一起清掉"
    if not (where / ".git").exists():
        return (
            f"{where} 那儿还没有知识库。想搬过去用「迁移到别处」，"
            "想从零建先「移除知识库」"
        )
    return None
```

**(c)** 在 `validate` 的 `for` 循环里、`clean[key] = text`（第 219 行）**之前**插入路径分支：

```python
        if spec.key == "KB_VAULT_PATH":
            problem = _vault_path_problem(Path(text))
            if problem:
                problems.append(problem)
                continue
            clean[key] = text
            continue
```

**注意缩进层次**：它和 `if spec.kind == "secret"` 那几条同级，都在 `for` 循环体内、`clean[key] = text` 上方。

- [ ] **Step 6: 跑 `tests/core/test_settings.py`**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_settings.py -v
```

预期：全绿。

- [ ] **Step 7: 跑目标测试连带的那几个**

`/settings` 保存这条路会被 `tests/web/test_router.py` 覆盖，而那里的 `vault_path` 是临时目录、**没有 `.git`**——改完校验后那些「保存设置」的用例可能红。

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_router.py -v
```

**红的话**：在 `tests/web/test_router.py` 的 `vault` 夹具（第 36-50 行）末尾加一行，让那个手搓的半骨架也有 `.git`：

```python
    # `settings.validate` 现在要求 KB_VAULT_PATH 指向一个已初始化的库
    # （含 `.git`）。这个夹具原来是手搓的半骨架，补一个空 `.git` 即可——
    # 走真 `init_vault` 的是 `tests/web/test_setup.py` 的夹具。
    (tmp_path / ".git").mkdir()
    return tmp_path
```

同样看一眼 `tests/web/test_router.py` 里那几个直接写 `.env` 再 `POST /settings` 的用例（约 481、512、551、576、638、669 行），它们的 `Config` 里 `vault_path` 都是 `tmp_path`——同一个夹具路径，加一次 `.git` 就都覆盖到了。

- [ ] **Step 8: ruff + 全量 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
git -C "E:/Study_Projects/KN_Base" add src/kb/core/settings.py tests/core/test_settings.py tests/web/test_router.py
git -C "E:/Study_Projects/KN_Base" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t3.txt"
```

```
feat: 放开 KB_VAULT_PATH，改路径时校验目标是个已初始化的库

需求第十条「知识库路径要能填」。02_需求.md:118 那条「端口与 vault 路径
只读」要改——当初不是拍脑袋，是本机被改废过的教训。现在敢放开，靠的是
两条机械约束：改路径时目标必须含 .git，以及「迁移」这个显式动作。
放开的是输入框，不是判断。

三种失败各有各的出口文案，因为用户下一步该干的事不一样：
- 空值 → 「要解绑请用『移除知识库』」。不给「留空 = 解绑」：那会留下笔记、
  会话、日志在原地而界面显示成「还没有知识库」——痕迹还在、界面说没了
- 路径不存在 / 没 .git → 「想搬过去用『迁移到别处』，想从零建先『移除知识库』」

用 .git 而不是「目录存在」当判据：init_vault 必然建 .git，随便一个空目录
没有。用「目录存在」判，用户填个现成空文件夹就被当成已初始化，投递进去后
atomic_write 自己 mkdir(parents=True)，在一个不是库的地方长出半个库——
收件箱有草稿、领域是空的、每条都掉进待归类，正是要防的「看起来一切正常」。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 4: `Busy` 锁 + 入口拦截

**为什么必须有：** 一轮整理会**多次**读 vault（每条草稿规划时 `list_domains` + `find_candidates`，落盘时 `write_note`）。迁移插在中间就把一次整理劈成两半——前一半落旧库、后一半落新库，而最后那个 `commit_changes` 只在新库里提交，**旧库那半永远不进 git 却躺在磁盘上**，看起来一切正常。

**Files:**
- Create: `src/kb/core/lifecycle.py`（本任务只放 `Busy`，后面两个任务往里加）
- Modify: `src/kb/api/http.py`
- Modify: `src/kb/web/router.py`
- Test: `tests/core/test_lifecycle.py`（新建）

---

- [ ] **Step 1: 先写失败测试**

新建 `tests/core/test_lifecycle.py`：

```python
"""库的生命周期：状态推断、路径体检、搬家、移除。"""

from kb.core.lifecycle import Busy


def test_acquire_succeeds_when_idle():
    busy = Busy()
    assert busy.what is None
    assert busy.acquire("organize") is True
    assert busy.what == "organize"


def test_acquire_fails_while_held():
    """**不等待，直接返回 False。** 排队等只会把一个「正在整理」变成
    一串排队请求，而调用方要的是「现在不行，等会儿再来」。"""
    busy = Busy()
    busy.acquire("organize")
    assert busy.acquire("migrate") is False
    assert busy.what == "organize"       # 先来的那个不被顶掉


def test_release_frees_it():
    busy = Busy()
    busy.acquire("organize")
    busy.release()
    assert busy.what is None
    assert busy.acquire("migrate") is True


def test_label_is_chinese():
    """409 的文案要给人看，不能写 `正在migrate`。"""
    busy = Busy()
    busy.acquire("migrate")
    assert busy.label == "迁移"
```

- [ ] **Step 2: 跑，确认红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_lifecycle.py -v
```

预期：`ModuleNotFoundError: No module named 'kb.core.lifecycle'`。

- [ ] **Step 3: 新建 `src/kb/core/lifecycle.py`**

```python
"""知识库的生命周期：有没有库、能不能搬、怎么扔干净。

**只碰磁盘与 `.env`，不含 HTTP 与模板**——端点与界面在 `web/router.py`。
分层规矩见 `docs/01_架构.md`：`core/` 不 import `web`/`api`。
"""

from __future__ import annotations

import os
import shutil
import threading
from datetime import datetime
from pathlib import Path

_BUSY_LABELS = {"organize": "整理", "migrate": "迁移", "remove": "移除"}


class Busy:
    """谁在动 vault。**同一时刻只允许一件事。**

    挡的是「半路迁移」那类事故：一轮整理会多次读库（每条草稿规划时
    `list_domains` + `find_candidates`，落盘时 `write_note`），迁移插在中间
    就把一次整理劈成两半——前一半落旧库、后一半落新库，而最后那个
    `commit_changes` 只在新库里提交，**旧库那半永远不进 git 却躺在磁盘上**，
    看起来一切正常。见 spec 5.3。

    `acquire` **不等待**：拿不到返回 `False`，由调用方回 409。
    排队等只会把一个「正在整理」变成一串排队请求。
    """

    def __init__(self) -> None:
        self._lock = threading.Lock()
        self._what: str | None = None

    def acquire(self, what: str) -> bool:
        with self._lock:
            if self._what is not None:
                return False
            self._what = what
            return True

    def release(self) -> None:
        with self._lock:
            self._what = None

    @property
    def what(self) -> str | None:
        with self._lock:
            return self._what

    @property
    def label(self) -> str:
        """409 文案里那个动词。写英文会变成「正在migrate，等它跑完再试」。"""
        what = self.what
        return _BUSY_LABELS.get(what, what or "")
```

- [ ] **Step 4: 跑，确认绿**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_lifecycle.py -v
```

- [ ] **Step 5: `src/kb/api/http.py`——造 `Busy` 并传下去**

**(a)** import 加一行（放在 `from kb.core import flow, organize, settings, sweep, sweep_state` 下面）：

```python
from kb.core.lifecycle import Busy
```

**(b)** `create_app` 里、`state` / `cache` 那两个盒子旁边加：

```python
    # **谁在动 vault。** 服务是单进程多线程（uvicorn），整理与迁移可能同时
    # 进来——那个交错会把一次整理劈成两半，见 `Busy` 的 docstring。
    busy = Busy()
```

**(c)** `include_router` 那段（约 377 行）改成：

```python
    app.include_router(build_router(
        get_cfg, data_dir, _chat_organize_fn, get_llm,
        apply_settings, quit_fn or _default_quit, env_path, make_llm,
        busy=busy,
    ))
```

**本步只加 `busy` 一个参数**，`unbind_vault` 是 Task 5 才加的第二个（它属于「移除」那条路，此刻还不存在）。一步一个能跑的提交，别提前把参数位占上。

**(d)** 在 `create_app` 里、`apply_settings` 之后定义整理的锁：

```python
    @contextmanager
    def organizing():
        """整理期间占住 `busy` 锁；拿不到就 409。

        **不是 `with busy` 那种阻塞式**——拿不到锁要立刻回 409，
        而不是让请求排在这儿等一个可能几分钟的整理跑完。
        """
        if not busy.acquire("organize"):
            raise HTTPException(
                status_code=409, detail=f"正在{busy.label}，等它跑完再试"
            )
        try:
            yield
        finally:
            busy.release()
```

**(e)** import 区加 `from contextlib import contextmanager`。

**(f)** `/organize` 端点的整理那几行包进锁里——把

```python
        log = logging.getLogger("kb.organize")
        log.info("开始整理 %d 条草稿", len(paths))
        results = organize.organize_selected(vault_path, paths, get_llm())
```

改成：

```python
        log = logging.getLogger("kb.organize")
        log.info("开始整理 %d 条草稿", len(paths))
        with organizing():
            results = organize.organize_selected(vault_path, paths, get_llm())
```

**(g)** `/chat` 端点——对话层能触发整理（`_chat_organize_fn` 里那个 `organize` 动作），所以整轮对话都要占锁：

```python
        vault_path = vault()
        with organizing():
            chat_id, reply = handle(
                data_dir,
                vault_path,
                req.chat_id,
                req.message,
                get_llm(),
                organize_fn=_chat_organize_fn,
            )
```

**(h) 顺手拆掉 `_chat_organize_fn` 里那颗雷**（勘误里记的）。

`create_app` 里那个闭包目前长这样：

```python
    def _chat_organize_fn(kind: str, content: str, target: str | None) -> str:
        ...
        if kind == "organize":
            results = organize.organize_selected(
                get_cfg().vault_path,                     # ← 空库时是 None
                list_drafts(get_cfg().vault_path),        # ← 同一颗雷，且这行先炸
                get_llm(),
            )
```

没库时 `get_cfg().vault_path` 是 `None`，`list_drafts(None)` 会 `TypeError`——它连 `organize_selected` 都进不去。

**当前打不到**（`/chat` 与 `chat_post` 都加了拦截，无库时根本到不了 `handle`）——但这是「等哪天有人挪了那道拦截就炸」的雷，而且炸在对话线程里、症状是「聊着聊着 500」。既然本步正在改这个文件，顺手换掉：

```python
        if kind == "organize":
            vault_path = require_vault(get_cfg())    # 没库就当场抛，别漏到 list_drafts
            results = organize.organize_selected(
                vault_path, list_drafts(vault_path), get_llm()
            )
```

`require_vault` 是 Task 1 加进 `config.py` 的，`http.py` 第 34 行已经 import 了，不用再补。

**别把它包进 `organizing()`**——`/chat` 端点那一层已经占了锁，再占一次是同一个线程对自己 `acquire`，`Busy` 不重入，会**当场 409 掉自己**。要改的就是「路径从哪儿取」，不是锁。

- [ ] **Step 6: `src/kb/web/router.py`——接住 `busy` 并拦两个入口**

**(a)** import 加：

```python
from kb.core.lifecycle import Busy
```

**(b)** `build_router` 的签名（第 186-195 行）末尾加一个**关键字参数**：

```python
def build_router(
    get_cfg: Callable[[], Config],
    data_dir: Path,
    organize_fn: Callable[[str, str, str | None], str],
    get_llm: Callable[[], LLM],
    apply_settings: Callable[[dict[str, str]], dict[str, str]],
    quit_fn: Callable[[], None],
    env_path: Path,
    build_llm_fn: Callable[[Config], LLM],
    *,
    busy: Busy,
) -> APIRouter:
```

docstring 里补一句：

```python
    `busy` 由 `create_app` 造了传进来——**锁必须是同一把**。各造一把的话，
    网页这边「正在整理」挡住了投递，服务端 `/organize` 却照样跑，
    等于没锁。
```

**(c)** `build_router` 体内、`vault = vault_guard(get_cfg)` 下面加：

```python
    @contextmanager
    def organizing():
        """整理期间占住 `busy` 锁；拿不到就 409。"""
        if not busy.acquire("organize"):
            raise HTTPException(
                status_code=409, detail=f"正在{busy.label}，等它跑完再试"
            )
        try:
            yield
        finally:
            busy.release()
```

**(d)** `from contextlib import contextmanager` 加进 import 区。

**(e)** `POST /`（chat_post）和 `POST /new`（new_note_post）各包一层：

```python
    @router.post("/")
    def chat_post(message: str = Form(...), cid: str = Form("")):
        with organizing():
            chat_id, _ = handle(
                data_dir,
                vault(),
                cid or None,
                message,
                get_llm(),
                organize_fn=organize_fn,
            )
        return RedirectResponse(f"/?cid={chat_id}", status_code=303)
```

```python
    @router.post("/new")
    def new_note_post(content: str = Form(...)):
        """只填正文（Q87）——`source` 记成 Web，项目留空，其余归整理。"""
        if not content.strip():
            raise HTTPException(status_code=400, detail="正文不能为空")
        vault()                     # ← 仓库里本来就有这行（Task 3 加的），**别漏**
        with organizing():
            push_and_organize(get_cfg(), content, get_llm(), source="Web")
        return RedirectResponse("/journal", status_code=303)
```

**`vault()` 那行不能少。** 上面这段原来是照「只包一层锁」的意思写的，漏了它：
`push_and_organize` 内部虽然也调 `require_vault`，但那抛的是 `ConfigError`，
**没有 handler 接**，未初始化时 `/new` 会回 500 而不是 409。而 `/setup/*` 那几条
测试只覆盖 `/push` `/organize` `/inbox` `/search`，**不覆盖 `/new`**，所以这个
回归测试抓不到——漏了不会有人告诉你。`vault()` 是 `vault_guard` 那一路，回 409。

- [ ] **Step 7: 把 `Busy` 挂到 app 上，加一条端点用例钉住 409**

**(a)** `create_app` 里那句 `busy = Busy()` 挪到 `app = FastAPI(...)`（约 369 行）**之后**，紧跟着挂上去：

```python
    # **挂到 app 上给测试用。** 不然「正在忙」只能靠一个真跑几分钟的整理去
    # 制造——那是测不动的，于是 409 那条路径永远没人验。
    app.state.busy = busy
```

放在 `include_router` 之前即可。

**(b)** 新建 `tests/api/test_busy.py`——专门钉「忙的时候端点会不会拒」：

```python
"""忙的时候端点必须当场拒。**这是 `Busy` 存在的全部意义。**"""

from pathlib import Path

import pytest
from fastapi.testclient import TestClient

from kb.api.http import create_app
from kb.config import Config
from kb.llm.base import FakeLLM


@pytest.fixture
def client(tmp_path):
    vault = tmp_path / "库"
    vault.mkdir()
    (vault / ".git").mkdir()          # `settings.validate` 要的那个标记
    cfg = Config(
        llm_api_key="k", llm_base_url="http://x", llm_model="m",
        vault_path=vault, port=None,
    )
    env = tmp_path / ".env"
    env.write_text(
        "KB_LLM_API_KEY=k\nKB_LLM_BASE_URL=http://x\nKB_LLM_MODEL=m\n"
        f"KB_VAULT_PATH={vault}\n",
        encoding="utf-8",
    )
    data = tmp_path / "data"
    data.mkdir()
    return TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data,
                                 env_file=env))


@pytest.mark.parametrize("what,label", [("migrate", "迁移"), ("remove", "移除")])
def test_organize_is_rejected_while_something_else_runs(client, what, label):
    """别人在动库的时候 `/organize` 必须当场拒。

    不拒的话它会把半截笔记写进库——前一半落旧库、后一半落新库，
    而最后那个 `commit_changes` 只在新库里提交，**旧库那半永远不进 git
    却躺在磁盘上**。那是「看起来一切正常」的失败，最难发现。
    """
    busy = client.app.state.busy
    assert busy.acquire(what) is True
    try:
        resp = client.post("/organize", json={})
        assert resp.status_code == 409
        assert label in resp.json()["detail"]
    finally:
        busy.release()


@pytest.mark.parametrize("path,data", [
    ("/drop", {"ids": ["x"]}),
    ("/inbox", None),
    ("/search", None),
    ("/sweep", None),
])
def test_reads_are_not_blocked_by_busy(client, path, data):
    """**只有整理被拦，读操作照常。**

    迁移期间把 `/inbox` 也挡掉的话，用户连「库里现在有什么」都看不到，
    而那时候他正需要看一眼。读不会把库写到一半。
    """
    busy = client.app.state.busy
    assert busy.acquire("migrate") is True
    try:
        resp = client.get(path) if data is None else client.post(path, json=data)
        assert resp.status_code != 409
    finally:
        busy.release()


def test_sweep_post_is_not_blocked_by_busy(client):
    """`POST /sweep` **得单独测**——上面那张表覆盖不到它。

    `GET /sweep` 和 `POST /sweep` 是**两个不同的东西**：前者是 `router.py`
    里的巡检页，后者是 `http.py` 里真跑巡检的那个。上面那张表用
    `data is None` 决定走 GET 还是 POST，所以 `("/sweep", None)` 打的是**页面**，
    真跑巡检那条一次都走不到。

    它不占锁是刻意的（巡检整的是标签和目录，跟整理不是一回事），但它**是写操作**，
    所以不能混进「读操作照常」那组——语义不一样，混着写下次就没人看得出这条在测什么。
    """
    busy = client.app.state.busy
    assert busy.acquire("organize") is True
    try:
        assert client.post("/sweep").status_code != 409
    finally:
        busy.release()
```

**注意 `/drop` 传的是 `json`**——它收一个 `DropRequest`（`ids` 列表）。`/sweep` 是 POST，`/inbox` / `/search` 是 GET。

- [ ] **Step 7c: 跑它**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/api/test_busy.py -v
```

预期：全绿。若 `test_reads_are_not_blocked_by_busy` 红了，说明你把 `organizing()` 包到了读端点上——**撤回那一处**，读不该占锁。

- [ ] **Step 8: 跑目标测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_lifecycle.py tests/web/test_router.py tests/api/test_http.py -v
```

预期：全绿。`tests/web/test_router.py` 走的是 `create_app`，`busy` 由它造了传进 `build_router`——`build_router` 只在 `http.py` 一处被调用（全仓 grep 过），所以把 `busy` 设成**必填**关键字参数不会漏掉任何调用点。

- [ ] **Step 9: ruff + 全量 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
# 提交信息先用 Write 工具写到 C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t4.txt
# （别用 heredoc——这台机器上多行 shell 构造会触发 AbortError: Stream closed，
#  `cd A && ...` 链和 for 循环都栽过）
git -C "E:/Study_Projects/KN_Base_t4" add src/kb/core/lifecycle.py src/kb/api/http.py src/kb/web/router.py tests/core/test_lifecycle.py tests/api/test_busy.py
git -C "E:/Study_Projects/KN_Base_t4" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t4.txt"
```

```
feat: Busy 锁——半路迁移必须挡住，不然失败的样子是「看起来一切正常」

一轮整理会多次读 vault：每条草稿规划时 list_domains + find_candidates，
落盘时 write_note。迁移插在中间，一次整理就被劈成两半：

- 前一半落旧库、后一半落新库，最后那个 commit_changes 只在新库里提交，
  旧库那半永远不进 git 却躺在磁盘上，看起来正常
- append_results 写日志时库已经换了，整理日志只记了后半截
- write_note 自己的 mkdir(parents=True) 落旧路径，旧库那边长出半个骨架

Busy 是个带锁的小对象，acquire 不等待——拿不到就 409。排队等只会把一个
「正在整理」变成一串排队请求。锁在 create_app 里造一把传下去，两边各造
一把等于没锁。

/chat 也占锁：对话层能触发 organize 动作，整轮对话都要挡住迁移。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 5: `/setup/*` 端点 + 搬家与移除的逻辑

**Files:**
- Modify: `src/kb/core/lifecycle.py`（加体检 / 搬家 / 移除）
- Modify: `src/kb/api/http.py`（`unbind_vault` + 多传一个参数）
- Modify: `src/kb/web/router.py`（五个端点）
- Test: `tests/core/test_lifecycle.py`、`tests/web/test_setup.py`（新建）

---

- [ ] **Step 1: 先写 `core` 那三条逻辑的失败测试**

`tests/core/test_lifecycle.py` 这个文件 Task 4 已经建过（里面只有 `Busy` 那四条）。这一步是**改它顶部的 import，再往下追加**。

把那一行 import 换成：

```python
import stat

import pytest

from kb.core import lifecycle
from kb.core.lifecycle import (
    Busy,
    LifecycleError,
    check_path,
    migrate_vault,
    remove_vault,
    vault_ready,
)
```

（`import stat` 是给下面那个「只读对象」的用例造只读文件用的。
`from kb.core import lifecycle` 是给 monkeypatch 用的：下面有两处要换掉
`lifecycle.shutil.rmtree` 和 `lifecycle._tree_size`。）

然后往文件末尾追加：

```python
def _make_vault(root):
    """一个有三篇笔记、两个领域的库。

    ⚠️ **它的 `.git` 是个空目录，不是真仓库。** 所以它**碰不到**只读的
    git 松散对象——「迁移/移除在真库上必炸」那个坑靠它是验不出来的，
    那条另有一个用例（下面 `_make_vault_with_readonly_object`）。
    """
    (root / ".git").mkdir(parents=True)
    (root / "计算机").mkdir()
    (root / "艺术").mkdir()
    for i in range(3):
        (root / "计算机" / f"笔记{i}.md").write_text("x" * 100, encoding="utf-8")
    return root


def _make_vault_with_readonly_object(root):
    """`_make_vault` 再塞一个**只读**文件，模拟 git 的松散对象。

    git for Windows 把 `.git/objects/**` 写成**只读**（实测 `0o100444`），
    而 Windows 上 `shutil.rmtree` 碰到只读文件直接抛
    `PermissionError [WinError 5] 拒绝访问`。

    **只造 `.git` 目录是不够的**——只读位才是那个坑的全部原因，
    空目录删起来一路顺风。
    """
    _make_vault(root)
    obj = root / ".git" / "objects" / "ab" / "cdef0123"
    obj.parent.mkdir(parents=True)
    obj.write_text("x", encoding="utf-8")
    obj.chmod(stat.S_IREAD)          # 0o444，和 git 写出来的一样
    return root


# ---------- check_path ----------

def test_check_path_on_a_missing_dir(tmp_path):
    got = check_path(tmp_path / "还没有")
    assert got["exists"] is False
    assert got["looks_like_vault"] is False
    assert got["entries"] == []


def test_check_path_on_an_initialized_vault(tmp_path):
    vault = _make_vault(tmp_path / "库")
    got = check_path(vault)
    assert got["exists"] is True
    assert got["looks_like_vault"] is True
    assert got["writable"] is True
    # `check_path` 用的是 `sorted()`，按 **Unicode 码点**排，不是按拼音：
    # `艺` 是 U+827A、`计` 是 U+8BA1，所以 `艺` 在前。**看着别扭别改回去。**
    assert got["entries"] == [".git", "艺术", "计算机"]


def test_check_path_on_a_plain_dir(tmp_path):
    plain = tmp_path / "普通目录"
    plain.mkdir()
    got = check_path(plain)
    assert got["exists"] is True
    assert got["looks_like_vault"] is False


# ---------- migrate_vault ----------

def test_migrate_moves_everything_and_removes_the_source(tmp_path):
    source = _make_vault(tmp_path / "旧")
    target = tmp_path / "新"

    moved = migrate_vault(source, target)

    assert moved["files"] == 3          # `_tree_size` 只数 `is_file()`；`.git`
    assert moved["bytes"] == 300        # 与两个领域目录都是空目录，不算文件
    assert not source.exists()
    assert (target / "计算机" / "笔记0.md").is_file()


def test_migrate_accepts_an_existing_empty_target(tmp_path):
    """**「存在但是空的」是规格 5.1 明说要通过的两种目标之一**
    （`design.md:172`）。

    这条以前没有，于是漏掉了一个真会咬人的 500：`copytree` 默认
    `dirs_exist_ok=False`，撞上**已存在的目录一律抛 `FileExistsError`，
    哪怕它是空的**（本机 3.11 实测 `[WinError 183]`）。而
    `FileExistsError` 是 `OSError` 不是 `LifecycleError`，`router.py`
    那句 `except LifecycleError` 接不住 → 冒成 500，**重试还是 500**。
    用户在界面上按「检查」的提示挑一个现成的空文件夹，是最自然不过的操作。
    """
    source = _make_vault(tmp_path / "旧")
    target = tmp_path / "新"
    target.mkdir()                                   # 先建好，是空的

    moved = migrate_vault(source, target)

    assert moved["files"] == 3
    assert (target / "计算机" / "笔记0.md").is_file()
    assert not source.exists()


def test_migrate_rolls_back_when_copytree_itself_fails(tmp_path, monkeypatch):
    """**拷贝中途失败也要回滚目标**（规格 9.3，`design.md:389`）。

    原来只有「校验失败」才回滚（`except LifecycleError`），而 `copytree`
    自己炸（磁盘满、权限）抛的是 `OSError`，**不走那个分支**——目标留着
    半截，之后重试还会撞上「目标非空」400，走进死胡同。
    """
    source = _make_vault(tmp_path / "旧")
    target = tmp_path / "新"

    def boom(*_a, **_kw):
        # 先落下半截目标：证明确实回滚了，而不是「什么都没建，看着像回滚」
        (target / "半截").mkdir(parents=True, exist_ok=True)
        raise OSError("磁盘满了")

    monkeypatch.setattr(lifecycle.shutil, "copytree", boom)

    # 抛的必须是 `LifecycleError`——`router.py` 只接得住它，接不住就回 500
    with pytest.raises(LifecycleError, match="拷贝失败"):
        migrate_vault(source, target)

    assert not target.exists()                       # 目标回滚了
    assert (source / "计算机" / "笔记0.md").is_file()  # 源完好


def test_migrate_rolls_back_the_target_when_verification_fails(tmp_path, monkeypatch):
    """**源库一根汗毛不动。** 数据有两份的时候，永远让旧的那份先活着——
    新的可以再造。"""
    source = _make_vault(tmp_path / "旧")
    target = tmp_path / "新"

    real = lifecycle._tree_size

    def lying(path):
        # 第一次量源（真话），第二次量目标（谎报少一个文件）
        if path == target:
            return (1, 1)
        return real(path)

    monkeypatch.setattr(lifecycle, "_tree_size", lying)

    with pytest.raises(LifecycleError, match="对不上"):
        migrate_vault(source, target)

    assert not target.exists()                       # 目标回滚了
    assert (source / "计算机" / "笔记0.md").is_file()  # 源完好


def test_migrate_refuses_a_non_empty_target_before_copying(tmp_path):
    """非空目标必须被挡在**拷贝之前**。

    ⚠️ 这条原来写的是 `pytest.raises(OSError)`，靠「`copytree` 撞上已存在
    目录会抛 `FileExistsError`」间接验——**那个前提已经没了**：为了让
    「存在但是空的目录」走得通（规格 5.1），`copytree` 现在带
    `dirs_exist_ok=True`，非空目标它会**静默合并**。

    所以这条改成直接钉住新加的那道闸。**而且要断言 `占位` 还在**：
    非空目标一旦走进回滚分支，`_rmtree(target)` 会把目标里**原有的东西
    一起删掉**——那是数据损失，比「拷贝失败」严重得多。
    """
    source = _make_vault(tmp_path / "旧")
    target = tmp_path / "新"
    target.mkdir()
    (target / "占位").write_text("x", encoding="utf-8")

    with pytest.raises(LifecycleError, match="非空"):
        migrate_vault(source, target)

    assert (source / "计算机" / "笔记0.md").is_file()
    assert (target / "占位").is_file()               # **回滚没碰用户原有的东西**


def test_migrate_handles_a_vault_with_read_only_git_objects(tmp_path):
    """**这条是「真库上必炸」的那个回归。** 删源那一步走的是 `_rmtree`：
    只读位要先摘掉再删。退回裸 `shutil.rmtree` 这条立刻红。"""
    source = _make_vault_with_readonly_object(tmp_path / "旧")
    target = tmp_path / "新"

    moved = migrate_vault(source, target)

    assert moved["files"] == 4          # 3 篇笔记 + 1 个只读对象
    assert not source.exists()
    assert (target / ".git" / "objects" / "ab" / "cdef0123").is_file()


# ---------- remove_vault ----------

def test_remove_deletes_vault_chats_logs_and_state(tmp_path):
    vault = _make_vault(tmp_path / "库")
    data = tmp_path / "data"
    (data / "chats").mkdir(parents=True)
    (data / "chats" / "c.json").write_text("{}", encoding="utf-8")
    (data / "logs" / "kb").mkdir(parents=True)
    (data / "logs" / "kb" / "2026-01-01.log").write_text("x", encoding="utf-8")
    (data / "state.json").write_text("{}", encoding="utf-8")

    result = remove_vault(vault, data)

    assert result["vault_removed"] is True
    assert not vault.exists()
    assert not (data / "chats").exists()
    assert not (data / "logs").exists()
    assert not (data / "state.json").exists()
    assert result["failed"] == []


def test_remove_handles_a_vault_with_read_only_git_objects(tmp_path):
    """**「移除知识库」在真库上必炸的那条回归。**

    上面那条用的 `_make_vault` 造的是**空** `.git` 目录，删起来一路顺风——
    于是这个坑在测试里完全隐形：测试全绿，用户点「移除」永远失败，
    报的还是 `PermissionError [WinError 5] 拒绝访问`，看着像权限问题，
    排查方向全错。这条把只读位补上，退回裸 `shutil.rmtree` 立刻红。
    """
    vault = _make_vault_with_readonly_object(tmp_path / "库")
    data = tmp_path / "data"

    result = remove_vault(vault, data)

    assert result["vault_removed"] is True
    assert not vault.exists()
    assert result["failed"] == []


def test_remove_leaves_the_rest_of_data_alone(tmp_path):
    """**删除清单只有三项**：`data/chats`、`data/logs`、`data/state.json`。

    最要紧的是 `data/runtime/service.json` 不在里面——那是 `kb stop` 唯一的
    线索，探活失败时 `stop_service` 还会顺手把它清掉；删了就是「停不掉的
    孤儿进程」，而那个 bug 的症状是**测试全绿、开发机上服务失控**
    （`04_踩坑与经验.md` 第 21 条）。

    **这条测试得真的会咬，所以多造一个无关文件一起验。** 只断言
    `service.json` 还在的话它是**恒真**的——`remove_vault` 压根不碰
    `data/runtime`，那条断言一条路径都走不到。（「一点痕迹都不留」这个说法
    压过来的时候，最省事的改法就是 `rmtree(data_dir)`，那正是要拦的。）
    """
    vault = _make_vault(tmp_path / "库")
    data = tmp_path / "data"
    (data / "runtime").mkdir(parents=True)
    (data / "runtime" / "service.json").write_text("{}", encoding="utf-8")
    (data / "unrelated.txt").write_text("x", encoding="utf-8")

    remove_vault(vault, data)

    assert (data / "runtime" / "service.json").is_file()
    assert (data / "unrelated.txt").is_file()
    assert data.is_dir()          # `data/` 本身也得留着


def test_remove_survives_a_missing_vault_dir(tmp_path):
    """库本来就不在了 → 照删其余部分，最后说明一句。**不算错。**"""
    data = tmp_path / "data"
    (data / "chats").mkdir(parents=True)

    result = remove_vault(tmp_path / "根本没有", data)

    assert result["vault_removed"] is True
    assert result["failed"] == []
    assert any("本来就不在了" in n for n in result["notes"])


def test_remove_with_no_vault_configured(tmp_path):
    """没配过库就点移除——不该炸。"""
    result = remove_vault(None, tmp_path / "data")
    assert result["vault_removed"] is True
    assert result["failed"] == []


def test_remove_reports_a_locked_vault_instead_of_pretending(tmp_path, monkeypatch):
    """被占着删不掉时**如实汇报**，并且**不解绑**——库还在，界面就不能说
    「还没有知识库」。"""
    vault = _make_vault(tmp_path / "库")

    def refuse(*a, **k):
        raise OSError(13, "Permission denied")

    monkeypatch.setattr(lifecycle.shutil, "rmtree", refuse)

    result = remove_vault(vault, tmp_path / "data")

    assert result["vault_removed"] is False
    assert result["failed"]
    assert vault.exists()                # 名字被改回去了
```

- [ ] **Step 2: 跑，确认红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_lifecycle.py -v
```

预期：`ImportError: cannot import name 'LifecycleError'`。

- [ ] **Step 3: 往 `src/kb/core/lifecycle.py` 追加逻辑**

先在文件顶上的 import 里**加一行 `import stat`**（现有的是
`os` / `shutil` / `threading`，见 Task 4 Step 3）。`stat.S_IWRITE` 要用它。

然后在 `Busy` 之后追加：

```python
class LifecycleError(RuntimeError):
    """搬家搬不成。消息直接给人看。"""


def vault_ready(vault_path: Path | None) -> bool:
    """库真的建好了吗。**这个判据只有这一份。**

    认 **`.git` 存在**，不是「目录存在」——路径配了但那儿没库，是最容易
    骗过界面的一种状态，理由见 `settings._vault_path_problem`。

    ⚠️ **别在任何地方再写一遍 `bool(p and (p / ".git").exists())`。**
    这条判据计划里本来散在四处（`/setup/state`、`/setup/init` 的「已经
    初始化了」闸、Task 6 的 `settings_context`、Task 7 的 `_ctx`），每处
    还都配了一句「判据要一模一样」的注释——**那种注释就是重复的信号**：
    真出现分歧时，没有任何东西会站出来报错，只会出现「状态接口说已初始化、
    设置窗说没有」这种自相矛盾的画面。同一份计划里 `busy_guard`（合掉两份
    409）、`organizing()`（合掉两份去重）、`_BUSY_LABELS` 都刚收敛过，
    这条是同一类。

    **两处有意不并进来**，别顺手改：`settings._vault_path_problem` 与
    `vault_setup.init_vault`。前者判的是**用户刚填进来的候选路径**，
    要的是那句「那儿还没有知识库」的文案，不是「当前库建好没有」；
    后者问的是「这儿要不要跑 `git init`」——**那是个 git 问题，不是
    库的问题**，同一个 `.git` 只是恰好都出现在两句话里。并进来会把
    两个不同的判断塞进一个名字里，正是上面说的那种假统一。
    """
    return bool(vault_path and (vault_path / ".git").exists())


# ------------------------------------------------------------ 删树的公用件

def _rmtree(path: Path, *, ignore_errors: bool = False) -> None:
    """删一棵树，**删不掉的先摘只读位再删一次**。

    **git 的松散对象是只读的（实测 `0o100444`）**，而 Windows 上
    `shutil.rmtree` 碰到只读文件直接抛 `PermissionError [WinError 5] 拒绝访问`
    ——手工 `rm -rf .git` 在那个目录上同样删不掉。不管这一步，「移除知识库」
    与「搬家」在**真库**上必然失败（假 `.git` 目录的用例是绿的，所以这个坑
    只有拿真 git 跑才看得见），而且失败的样子像个权限问题，排查方向全错。
    实测：`os.chmod(p, stat.S_IWRITE)` 之后同一个 `rmtree` 就过了。

    **只用在库那棵树（含 `.git`）上**，`data/chats` / `data/logs` 不用：
    那里没有 git 对象，而真正会挡住的「文件正被服务打开」是**别的进程握着
    句柄**，摘只读位救不了——那种失败要的是如实汇报（见 `remove_vault`），
    不是换个删法再试。

    `ignore_errors=True` 时**照样先试摘只读位**再吞掉最终失败
    （`shutil.rmtree` 自带的 `ignore_errors` 是直接不调 `onerror` 的，
    那样回滚会留下一堆只读残留）。
    """
    def _on_error(func, target, _exc_info):
        os.chmod(target, stat.S_IWRITE | stat.S_IREAD | stat.S_IEXEC)
        func(target)

    try:
        shutil.rmtree(path, onerror=_on_error)
    except OSError:
        if not ignore_errors:
            raise


# ------------------------------------------------------------ 体检

def _writable(path: Path) -> bool:
    """能不能在这儿建东西。**父目录存在就查父目录**——用户填的多半是
    一个还不存在的路径，而那个路径的父目录才是真正要写进去的地方。"""
    probe = path
    while not probe.exists() and probe != probe.parent:
        probe = probe.parent
    return os.access(probe, os.W_OK)


def check_path(path: Path) -> dict:
    """顺手看一眼这个路径。**只回事实，不做判断。**

    这是「检查」按钮的全部实现——失败也不拦着初始化（`init_vault` 幂等，
    在一个有东西的目录上跑不会毁掉什么）。所以这里没有「行/不行」，
    只有「存在吗、能写吗、里面是不是已经有个库、有几个条目」。
    """
    entries: list[str] = []
    if path.is_dir():
        try:
            entries = sorted(p.name for p in path.iterdir())[:50]
        except OSError:
            entries = []
    return {
        "exists": path.exists(),
        "writable": _writable(path),
        "looks_like_vault": (path / ".git").exists(),
        "entries": entries,
    }


# ------------------------------------------------------------ 搬家

# **`data/` 不搬，一个字都不用动**（spec 5.4）。已核实过：`data/logs/flow/*.jsonl`
# 里记的路径**全是 vault 相对路径**（0 条绝对路径），`data/state.json` 只有巡检状态。
# 所以下面 `migrate_vault` 只碰 vault 目录，**别顺手把 `data/` 也拷过去**——
# 那是已经查过、确认不需要的，不是漏了。


def _tree_size(root: Path) -> tuple[int, int]:
    """数一棵树里的文件数与总字节数。

    **只比这两个，不逐文件哈希。** 这库按几千条设计（Q95）：727K 时哈希是
    零成本，但真到几千条、带附件时逐文件哈希会明显变慢；而**拷贝被截断几乎
    必然改变文件数和字节数**。这个取舍是有意的，别以为是漏了。
    """
    files = 0
    size = 0
    for p in root.rglob("*"):
        if p.is_file():
            files += 1
            size += p.stat().st_size
    return files, size


def migrate_vault(source: Path, target: Path) -> dict:
    """把整棵库从 `source` 搬到 `target`。成功返回 `{files, bytes}`。

    **失败时回滚目标，源库一根汗毛不动。** 这条是铁律：数据有两份的时候，
    永远让**旧的那份先活着**，新的可以再造。
    """
    # 非空目标由调用方 `_check_migration` 先挡（规格 5.1），这里再兜一道：
    # `copytree(dirs_exist_ok=True)` 会**静默合并**，而合并进一个已有的库
    # 是灾难。**这道闸必须在 `try` 之外**——它拦的是「调用方用错了」，
    # 不是「拷贝出错」，绝不能走回滚（回滚会把目标里原有的东西一起删掉）。
    if target.exists() and any(target.iterdir()):
        raise LifecycleError(f"目标非空（{target}），换一个空的目录")

    files, size = _tree_size(source)

    try:
        # ⚠️ `dirs_exist_ok=True` **不能省**。`copytree` 默认 `False`，撞上
        # **已存在的目录一律抛 `FileExistsError`，哪怕它是空的**（本机 3.11
        # 实测：`[WinError 183] 当文件已存在时，无法创建该文件`）。
        # 而「存在但是空的」是规格 5.1 明说要**通过**的两种目标之一
        # （`design.md:172`）——用户在界面上按「检查」的提示挑一个现成的
        # 空文件夹，是最自然不过的操作。不加的话那条路 500，而且**重试还是
        # 500**（`FileExistsError` 是 `OSError` 不是 `LifecycleError`，
        # `router.py` 那句 `except LifecycleError` 接不住）。
        # 合并语义在这里是安全的：上面那道闸挡住了非空目标，能走到这行的
        # target 只可能是空的或不存在。
        shutil.copytree(source, target, dirs_exist_ok=True)
        got_files, got_size = _tree_size(target)
        if (got_files, got_size) != (files, size):
            raise LifecycleError(
                f"拷完对不上：拷了 {got_files} 个文件 / {got_size} 字节，"
                f"源有 {files} 个 / {size} 字节。目标目录已清掉，源库没动。"
            )
    except (OSError, LifecycleError) as exc:
        # `ignore_errors=True`：这一步是**回滚**，删不干净也不能盖掉上面那个
        # `LifecycleError`——用户要看见的是「拷完对不上」，不是「回滚没删掉」。
        _rmtree(target, ignore_errors=True)
        if isinstance(exc, LifecycleError):
            raise
        # **`copytree` 自己炸了也要回滚**：规格 9.3 写着「拷贝中途失败 →
        # 回滚目标目录，源不动，报错」（`design.md:389`）。不回滚的话目标
        # 留着半截，用户重试会撞上「目标非空」400，**走进死胡同**。
        # 包成 `LifecycleError` 抛出去，`router.py` 才接得住、回 400 而不是 500。
        raise LifecycleError(f"拷贝失败：{exc}") from exc

    _rmtree(source)
    return {"files": files, "bytes": size}
```

**一个已知的边界**（实现时才浮出来，记着别再惊讶一次）：拷贝失败回滚时，
如果目标原本是个**已存在的空目录**，那个空目录会**连壳一起没**。空目录没有
数据损失，用户再建一个就行，所以不值得为它加一层「记住原来有没有这个目录、
回滚时补回来」——那层逻辑本身比它防的事故更重。留着这句是因为
`dirs_exist_ok=True` 让「目标存在」成了一个正常状态，而回滚会 `rmtree` 它。

```python
# ------------------------------------------------------------ 移除

def remove_vault(vault_root: Path | None, data_dir: Path) -> dict:
    """把库和它在服务侧留下的痕迹一起删掉。

    返回 `{removed, failed, notes, vault_removed}`——**逐项汇报**，因为这里
    做不到「全部成功」：Windows 上正被服务打开的文件删不掉（今天的日志就在
    其中），所以「一点痕迹都不留」物理上不成立。界面上得说实话，否则用户
    下次在 `data/logs/` 里看见文件会以为移除没生效。

    **`data/runtime/service.json` 绝不能删**：那份文件是 `kb stop` 唯一的线索，
    删了就是「停不掉的孤儿进程」——而那个 bug 的症状是测试全绿、开发机上服务
    失控（`04_踩坑与经验.md` 第 21 条）。所以下面的删除清单里**没有它**，
    这是刻意的，不是漏了。

    **`data/cache/` 也不在清单里，理由跟上面那条不一样**：里面装的是 pytest
    与 ruff 的缓存（`pyproject.toml` 把 `cache_dir` 指过去的），**跟这个 vault
    一点关系都没有**，删了只是让下次跑测试慢一点。判据是「这份文件记的**是不是
    这个库**」，不是「它在不在 `data/` 底下」——照后者去加，`data/` 下迟早
    会长出一堆不相干的东西被误伤。

    `vault_removed` 是给调用方的信号：**库还在的时候不要解绑 `.env`**，
    否则界面上显示「还没有知识库」而笔记还躺在原地。
    """
    removed: list[str] = []
    failed: list[str] = []
    notes: list[str] = []
    vault_removed = True

    if vault_root is None:
        pass
    elif not vault_root.exists():
        notes.append(f"笔记目录本来就不在了：{vault_root}")
    else:
        # **先改名再删。** 改名在同盘内是原子的、瞬间完成，所以「删了一半」
        # 那个窗口从一开始就不存在——要么库还在原地，要么它整个变成了一个
        # 待删的目录。
        #
        # **但改回去不等于毫发无伤。** `rmtree` 是**边遍历边删**的：中途
        # 失败时前面那些文件已经没了，`os.rename` 只能把**剩下的**搬回原名。
        # 所以这条路径下面是「残缺但还在」，**不是「失败可逆」**——别写成
        # 那种让人放心的假话，用户会以为库完好而不再去看一眼。
        graveyard = vault_root.with_name(
            f".{vault_root.name}.deleting-{datetime.now():%Y%m%d%H%M%S}"
        )
        try:
            os.rename(vault_root, graveyard)
        except OSError as exc:
            vault_removed = False
            failed.append(
                f"{vault_root}（{exc.strerror or exc}）——"
                "先关掉 Obsidian、停在库里的资源管理器、正在扫这个目录的杀软，再试"
            )
        else:
            try:
                _rmtree(graveyard)
                removed.append(str(vault_root))
            except OSError as exc:
                # 只能把**剩下的**搬回原名：删到一半才失败时，已经删掉的那些
                # 回不来了。所以这活儿的失败是「残缺」，不是「没动过」。
                os.rename(graveyard, vault_root)
                vault_removed = False
                failed.append(
                    f"{vault_root}（{exc.strerror or exc}）——"
                    "删到一半失败，目录已改回原名但**内容可能不全**，请自己核一遍"
                )

    for name in ("chats", "logs"):
        item = data_dir / name
        if not item.exists():
            continue
        try:
            shutil.rmtree(item)
            removed.append(str(item))
        except OSError:
            # 今天的日志文件正被服务自己打开着。**不抛错、不中断**，
            # 但**照实记下来**——`failed` 的语义是「这份没删掉」，不是
            # 「出 bug 了」，两者混起来用户会在 `data/logs/` 里看见文件
            # 却什么都没被告知。`notes` 里那句是给人看的原因。
            #
            # **光说「删不掉」不够，得说下一步干什么。** 规格 9.4 专门
            # 交代过（`design.md:408-409`）：清单里写「全部日志」时，底下的
            # 结果得说「今天那两个日志文件服务还开着，**下次重启后可以再清**」。
            # 少了后半句，用户下次在 `data/logs/` 里看见文件，会以为移除
            # 根本没生效——而实际上是操作系统的边界（文件正被本进程打开，
            # Windows 上就是删不掉），不是 bug。
            notes.append(f"{item} 里有文件正被服务打开，删不掉；下次重启后可以再清")
            failed.append(str(item))

    state = data_dir / "state.json"
    if state.is_file():
        try:
            state.unlink()
            removed.append(str(state))
        except OSError as exc:
            failed.append(f"{state}（{exc.strerror or exc}）")

    return {
        "removed": removed,
        "failed": failed,
        "notes": notes,
        "vault_removed": vault_removed,
    }
```

- [ ] **Step 4: 跑 `tests/core/test_lifecycle.py`**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/core/test_lifecycle.py -v
```

预期：全绿。

- [ ] **Step 5: 写端点层的失败测试**

新建 `tests/web/test_setup.py`：

⚠️ **文件名是 `test_setup.py`，不是 `test_lifecycle.py`——别顺手改回去。**
`tests/core/` 和 `tests/web/` 底下**都没有 `__init__.py`**（只有 `tests/` 有），
所以 pytest 是按**裸模块名**收这些文件的。两个目录里各放一个
`test_lifecycle.py` 会直接撞车，报的是：

```
import file mismatch: imported module 'test_lifecycle' has this __file__ attribute:
  ...\tests\core\test_lifecycle.py   which is not the same as the test file we want to collect:
  ...\tests\web\test_lifecycle.py
```

**一个测试都跑不了**，而且错误信息指向 `__pycache__`，很容易看成是缓存问题去删缓存。
项目里现有几十个测试文件**没有一对重名**，这是原因，不是巧合。

（这里本来有另外一个选择：给 `tests/core/` 和 `tests/web/` 各补一个
`__init__.py`。**没那么做**——那会把整个测试树的模块名从 `test_x` 改成
`tests.core.test_x`，影响面比改个文件名大得多，而这里要改的只有一处。）

```python
"""知识库生命周期的端点。走真 `init_vault` 的库，不是手搓的半骨架。"""

import pytest
from fastapi.testclient import TestClient

from kb.api.http import create_app
from kb.config import Config, load_config
from kb.core.vault_setup import init_vault
from kb.llm.base import FakeLLM


@pytest.fixture
def env(tmp_path):
    """一份配好模型、**没有 KB_VAULT_PATH** 的 `.env`——就是新用户那份。"""
    path = tmp_path / ".env"
    path.write_text(
        "KB_LLM_API_KEY=k\nKB_LLM_BASE_URL=http://x\nKB_LLM_MODEL=m\n",
        encoding="utf-8",
    )
    return path


@pytest.fixture
def app(tmp_path, env):
    """未初始化状态的服务。`data_dir` 用临时目录，别碰真的 data/。"""
    data = tmp_path / "data"
    data.mkdir()
    cfg = Config(
        llm_api_key="k", llm_base_url="http://x", llm_model="m",
        vault_path=None, port=None,
    )
    return create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env)


@pytest.fixture
def client(app):
    return TestClient(app)


@pytest.fixture
def initialized_vault(tmp_path, env):
    """一个走真 `init_vault` 的库——有 `.git`、有领域。"""
    path = tmp_path / "库"
    init_vault(path, ["计算机", "健康"])
    env.write_text(
        "KB_LLM_API_KEY=k\nKB_LLM_BASE_URL=http://x\nKB_LLM_MODEL=m\n"
        f"KB_VAULT_PATH={path}\n",
        encoding="utf-8",
    )
    return path


# ---------- 状态 ----------

def test_state_says_not_initialized(client):
    got = client.get("/setup/state").json()
    assert got["initialized"] is False
    assert got["vault_path"] == ""
    assert got["domains"] == []
    assert got["busy"] is None


def test_state_says_initialized(initialized_vault, env, tmp_path):
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    client = TestClient(
        create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env)
    )
    got = client.get("/setup/state").json()
    assert got["initialized"] is True
    assert got["vault_path"] == str(initialized_vault)
    assert got["domains"] == ["健康", "计算机"]      # `list_domains` 排序


# ---------- 未初始化时挡住投递 ----------

@pytest.mark.parametrize("call", [
    lambda c: c.post("/push", json={"content": "x"}),
    lambda c: c.post("/organize", json={}),
    lambda c: c.get("/inbox"),
    lambda c: c.get("/search", params={"q": "x"}),
])
def test_vault_endpoints_409_before_init(client, call):
    """**端点必须自己拦**：`kb.bat push` 走 CLI、接入指南里的会话层 AI 走
    `/push`，它们都绕过界面，界面置灰拦不住它们。"""
    resp = call(client)
    assert resp.status_code == 409
    assert "初始化知识库" in resp.json()["detail"]


def test_pages_still_open_before_init(client):
    """页面**不能** 409——首屏那张引导卡得有个落脚的地方。"""
    for path in ("/", "/journal", "/settings", "/new"):
        assert client.get(path).status_code == 200, path


# ---------- 初始化 ----------

def test_init_creates_the_vault_and_writes_env(client, env, tmp_path):
    target = tmp_path / "新库"
    resp = client.post("/setup/init", json={
        "vault_path": str(target),
        "domains": ["计算机", "健康"],
        "values": {
            "KB_LLM_MODEL": "m",
            "KB_LLM_BASE_URL": "http://x",
            "KB_LLM_API_KEY": "k",
        },
    })
    assert resp.status_code == 200, resp.text
    assert (target / ".git").is_dir()
    assert (target / "计算机").is_dir()
    assert (target / "健康").is_dir()
    assert not (target / "艺术").exists()
    assert f"KB_VAULT_PATH={target}" in env.read_text(encoding="utf-8")
    assert client.get("/setup/state").json()["initialized"] is True


def test_init_rejects_zero_domains(client, tmp_path):
    """一个领域都没有的话，投进来的每条都会掉进「待归类」。**理由要说清。**"""
    resp = client.post("/setup/init", json={
        "vault_path": str(tmp_path / "库"), "domains": [],
        "values": {"KB_LLM_MODEL": "m", "KB_LLM_BASE_URL": "x",
                   "KB_LLM_API_KEY": "k"},
    })
    assert resp.status_code == 400
    assert "待归类" in resp.json()["detail"]


def test_init_rejects_an_unknown_domain(client, tmp_path):
    """领域名不能随便起——候选清单之外的直接丢掉，丢掉后一个不剩就 400。"""
    resp = client.post("/setup/init", json={
        "vault_path": str(tmp_path / "库"), "domains": ["我自己编的"],
        "values": {"KB_LLM_MODEL": "m", "KB_LLM_BASE_URL": "x",
                   "KB_LLM_API_KEY": "k"},
    })
    assert resp.status_code == 400


def test_init_rejects_a_missing_model_config_before_creating_anything(
        tmp_path):
    """**模型没配齐就一个目录都别建。**

    `settings.validate` 只查「提交上来的键」，缺的键它不管；真正兜底的是
    `apply_settings` 重载之后那句复查（`api/http.py:362`）——可那时候库已经
    建好、`.env` 里的 `KB_VAULT_PATH` 也写下去了，留下「有库、没模型」的
    半截状态。本进程内还能靠「抛在换内存之前」歪打正着救回来，服务一重启
    就只剩「移除知识库」或手改 `.env` 两条路。所以检查必须在建库之前。

    `.env` 得是**连模型都没配**的那种，所以不用 `env` / `client` 夹具——
    它们那份 `.env` 三件套是齐的，走不到这条分支。
    """
    path = tmp_path / ".env"
    path.write_text("# 什么都还没填\n", encoding="utf-8")   # 刚解压出来那份
    data = tmp_path / "data"
    data.mkdir()
    cfg = Config(llm_api_key="", llm_base_url="", llm_model="",
                 vault_path=None, port=None)
    client = TestClient(
        create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=path)
    )

    target = tmp_path / "库"
    resp = client.post("/setup/init", json={
        "vault_path": str(target), "domains": ["计算机"], "values": {},
    })
    assert resp.status_code == 400
    assert "模型名" in resp.json()["detail"]
    assert not target.exists()                              # 一个目录都没建
    assert "KB_VAULT_PATH" not in path.read_text(encoding="utf-8")


def test_init_is_rejected_when_already_initialized(client, initialized_vault,
                                                   env, tmp_path):
    """已初始化时 400，文案指向两条正路——不是「先移除」一句打发。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))
    resp = c.post("/setup/init", json={
        "vault_path": str(tmp_path / "另一个"), "domains": ["计算机"],
        "values": {"KB_LLM_MODEL": "m", "KB_LLM_BASE_URL": "x",
                   "KB_LLM_API_KEY": "k"},
    })
    assert resp.status_code == 400
    assert "迁移到别处" in resp.json()["detail"]


# ---------- 迁移 ----------

def test_migrate_moves_and_repoints(initialized_vault, env, tmp_path):
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))
    (initialized_vault / "计算机" / "笔记.md").write_text("x", encoding="utf-8")

    target = tmp_path / "搬过去"
    resp = c.post("/setup/migrate", json={"target": str(target)})
    assert resp.status_code == 200, resp.text
    assert resp.json()["moved"]["files"] >= 1
    assert (target / "计算机" / "笔记.md").is_file()
    assert not initialized_vault.exists()
    assert f"KB_VAULT_PATH={target}" in env.read_text(encoding="utf-8")
    assert c.get("/setup/state").json()["vault_path"] == str(target)


def test_migrate_refuses_a_non_empty_target(initialized_vault, env, tmp_path):
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    target = tmp_path / "有东西"
    target.mkdir()
    (target / "占位").write_text("x", encoding="utf-8")

    resp = c.post("/setup/migrate", json={"target": str(target)})
    assert resp.status_code == 400
    assert "非空" in resp.json()["detail"]
    assert initialized_vault.exists()          # 源没动


def test_migrate_refuses_a_target_inside_the_source(initialized_vault, env, tmp_path):
    """`D:\\a` 搬到 `D:\\a\\b` 会把库搬进自己肚子里。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    resp = c.post("/setup/migrate",
                  json={"target": str(initialized_vault / "子目录")})
    assert resp.status_code == 400
    assert initialized_vault.exists()


def test_migrate_409_while_busy(initialized_vault, env, tmp_path):
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    app = create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env)
    c = TestClient(app)

    assert app.state.busy.acquire("organize") is True
    try:
        resp = c.post("/setup/migrate", json={"target": str(tmp_path / "新")})
        assert resp.status_code == 409
        assert "整理" in resp.json()["detail"]
    finally:
        app.state.busy.release()


# ---------- 移除 ----------

def test_remove_deletes_everything_and_unbinds(initialized_vault, env, tmp_path):
    data = tmp_path / "data"
    (data / "chats").mkdir(parents=True)
    (data / "chats" / "c.json").write_text("{}", encoding="utf-8")
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    resp = c.post("/setup/remove")
    assert resp.status_code == 200, resp.text
    assert not initialized_vault.exists()
    assert not (data / "chats").exists()

    got = c.get("/setup/state").json()
    assert got["initialized"] is False
    assert got["vault_path"] == ""


def test_remove_keeps_service_json(initialized_vault, env, tmp_path):
    """**端点这一层也不许顺手把 `data/` 整个端掉。**

    删的只有 `chats` / `logs` / `state.json` 三处。`data/runtime/service.json`
    是 `kb stop` 唯一的线索，删了就是「停不掉的孤儿进程」——那个 bug 的症状是
    **测试全绿、开发机上服务失控**（`04_踩坑与经验.md` 第 21 条）。

    同样造一个无关文件一起验：`remove_vault` 根本不碰 `data/runtime`，
    只断言 `service.json` 还在的话，删不删都绿，等于没测。
    """
    data = tmp_path / "data"
    (data / "runtime").mkdir(parents=True)
    (data / "runtime" / "service.json").write_text("{}", encoding="utf-8")
    (data / "unrelated.txt").write_text("x", encoding="utf-8")
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    c.post("/setup/remove")

    assert (data / "runtime" / "service.json").is_file()
    assert (data / "unrelated.txt").is_file()


def test_remove_then_init_again_keeps_the_model_config(initialized_vault, env,
                                                       tmp_path):
    """**移除后能重新初始化，模型配置保留**——那是给模型的钥匙，跟库没关系。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    c.post("/setup/remove")
    text = env.read_text(encoding="utf-8")
    assert "KB_LLM_API_KEY=k" in text          # 钥匙还在
    assert "KB_VAULT_PATH=" in text            # 只是解绑了

    again = tmp_path / "再来一个"
    resp = c.post("/setup/init", json={
        "vault_path": str(again), "domains": ["计算机"],
        "values": {},                          # 不重填模型三件套
    })
    assert resp.status_code == 200, resp.text
    assert (again / ".git").is_dir()
    assert c.get("/setup/state").json()["initialized"] is True


# ---------- 路径体检 ----------

def test_check_reports_a_missing_path(client, tmp_path):
    got = client.get("/setup/check",
                     params={"path": str(tmp_path / "还没有")}).json()
    assert got["exists"] is False
    assert got["looks_like_vault"] is False


def test_check_reports_an_existing_vault(client, initialized_vault):
    got = client.get("/setup/check",
                     params={"path": str(initialized_vault)}).json()
    assert got["exists"] is True
    assert got["looks_like_vault"] is True
    assert got["writable"] is True


def test_check_with_an_empty_path_is_not_an_error(client):
    """空输入不该 422——用户刚清空输入框就会打一次。"""
    assert client.get("/setup/check", params={"path": ""}).status_code == 200
```

- [ ] **Step 6: 跑，确认红**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py -v
```

预期：`404`（端点还没写）。

- [ ] **Step 7: `src/kb/api/http.py`——加 `unbind_vault` 并传下去**

在 `apply_settings` 之后插入：

```python
    def unbind_vault() -> None:
        """把 `KB_VAULT_PATH` 写空并重载。**移除专用，不走 `settings.validate`。**

        那条「空值 = 400」的校验管的是**设置窗**：留着笔记和日志却显示成
        「还没有知识库」，正是本设计要消灭的状态。移除不一样——它是
        **先删干净、再解绑**，所以这里直接写文件。

        必填项那条复查也不做：移除只动 `KB_VAULT_PATH` 一个键，
        模型三件套一个字都没碰。
        """
        settings.write_env(env_path, {"KB_VAULT_PATH": ""})
        state["cfg"] = reload_config(env_path)
        cache["llm"] = None
```

`include_router` 那段补上第二个参数：

```python
    app.include_router(build_router(
        get_cfg, data_dir, _chat_organize_fn, get_llm,
        apply_settings, quit_fn or _default_quit, env_path, make_llm,
        busy=busy,
        unbind_vault=unbind_vault,
    ))
```

- [ ] **Step 7b: `busy_guard` 收一个标签**

`busy_guard` 现在把 `"organize"` 写死在里头，而本任务要占的名目是
`"migrate"` 和 `"remove"`。按现在这个签名，`setup_migrate` /
`setup_remove` 就只能**再手写两份** `busy.acquire(...)` + 409 那段——**Task 4
刚刚逐字抄的那两份合掉，这里又抄出三份**，而且这次连 status code 和
`f"正在{busy.label}，等它跑完再试"` 都是新抄的。改 409 文案时漏掉哪一份，
症状还是那个「两条路给出两种答案」。

所以先给 `busy_guard` 加一个参数（`src/kb/api/http.py:97`）：

```python
def busy_guard(busy: Busy, what: str = "organize") -> Callable[[], AbstractContextManager[None]]:
    """造一个「占住 `busy`；拿不到就当场 409」的上下文管理器。

    **`api/http.py` 与 `web/router.py` 共用这一份。** 逐字抄两遍的话，
    改 409 文案、加 `Retry-After`、改状态码都得记得改两处——迟早漏一处，
    而漏掉的正是你没点开的那条路。跟 `_commit_sweep` 里那两条 git 调用
    是同一条理由：**平台坑与协议细节只该有一处**。

    **不是 `with busy` 那种阻塞式**——拿不到锁要立刻回 409，
    而不是让请求排在这儿等一个可能几分钟的整理跑完。

    `what` 是**自己**要占的名目（`"organize"` / `"migrate"` / `"remove"`，
    三条都在 `_BUSY_LABELS` 里）；`busy.label` 给的是**别人**正在干的事，
    那句 409 要说的是后者。默认值让两个老调用点
    （`api/http.py:352`、`web/router.py:221`）**一个字都不用改**。
    """
    @contextmanager
    def organizing():
        if not busy.acquire(what):
            raise HTTPException(
                status_code=409, detail=f"正在{busy.label}，等它跑完再试"
            )
        try:
            yield
        finally:
            busy.release()

    return organizing
```

`web/router.py` 的 import 那行**已经有 `busy_guard` 了**
（`from kb.api.http import busy_guard, push_and_organize, run_sweep, vault_guard`），
不用再加。

- [ ] **Step 8: `src/kb/web/router.py`——加五个端点**

**(a)** import 区补（**这一份就是 8(d) 那一份，两处别抄岔**）：

```python
from kb.core.lifecycle import (
    Busy,
    LifecycleError,
    check_path,
    migrate_vault,
    remove_vault,
    vault_ready,
)
from kb.core.vault import list_domains, list_drafts, list_notes
# `DEFAULT_DOMAINS` 这一步就得带上。它 Task 5 里用不到，**Task 6 用**——
# 设置窗那条龙拿它预勾复选框（`"default_domains": DEFAULT_DOMAINS`）。
# 漏了的后果不是「到时候再说」：`settings_context` 是**请求时**执行的，
# 第一次有人开 `/settings` 就 `NameError` → 500，`tests/web/test_router.py`
# 里十来处 `client.get("/settings")` 加上本任务自己那条
# `test_pages_still_open_before_init` 当场一起红。
from kb.core.vault_setup import DEFAULT_DOMAINS, DOMAIN_CANDIDATES, init_vault
```

（`list_domains` / `list_notes` / `list_drafts` 如果已经 import 了就跳过。`_counts`
里**同时**用了 `list_notes` 和 `list_drafts`，漏一个就是 `NameError`。）

**(a-2)** 两个请求体模型放**模块级**，紧挨着现成的 `SettingsBody`（约 54 行）：

```python
class SettingsBody(BaseModel):          # ← 现成的那个，别动
    values: dict[str, str] = {}


# **这两个必须住在模块级，不能挪进 `build_router` 函数体。**
# 本文件开头有 `from __future__ import annotations`（12 行），于是所有注解
# 都变成字符串；FastAPI 的 `get_typed_signature()` 是拿**函数所在模块的全局**
# 去 eval 这个注解的——`SetupBody` 要是成了函数体里的局部名，在那儿查不到，
# `create_app()` 会**当场 `NameError`**。
# `SettingsBody` 和 `api/http.py` 的 `PushRequest` / `DropRequest` 都在模块级，
# 跟它们是同一个规矩，别「顺手」挪回去。
class SetupBody(BaseModel):
    vault_path: str = ""
    domains: list[str] = []
    values: dict[str, str] = {}


class MigrateBody(BaseModel):
    target: str = ""
```

**(b)** `build_router` 签名再加一个关键字参数：

```python
    *,
    busy: Busy,
    unbind_vault: Callable[[], None],
```

docstring 补一句：

```python
    `unbind_vault` 是移除专用的一条路（直接写空 `KB_VAULT_PATH` 再重载），
    **故意绕开 `apply_settings`**——那条路会走 `settings.validate`，
    而那里「空值 = 400」。
```

**(c)** 在 `return router` **之前**插入五个端点：

```python
    # ---------------------------------------------------------- 知识库生命周期
    #
    # `SetupBody` / `MigrateBody` 在**模块级**（见 (a-2)）——写在这个函数体里
    # 的话 FastAPI 解析不到注解，`create_app()` 会当场 NameError。

    def _counts(vault_path: Path | None) -> dict:
        """移除确认页要的「将要删掉多少」——数量让人看一眼就知道删的是不是
        他以为的那个库。一句「确定要删除吗」在删 3 篇和删 3000 篇时长得
        一模一样。"""
        notes = 0
        drafts = 0
        if vault_path is not None and vault_path.exists():
            notes = len(list_notes(vault_path))
            drafts = len(list_drafts(vault_path))
        chats = 0
        chats_dir = data_dir / "chats"
        if chats_dir.is_dir():
            chats = len(list(chats_dir.glob("*.json")))
        return {"notes": notes, "drafts": drafts, "chats": chats}

    @router.get("/setup/state")
    def setup_state() -> dict:
        """四个状态里的「未初始化 / 已初始化」，加「谁正忙着」。

        判据是 **`.git` 存在**，不是「目录存在」——理由见
        `settings._vault_path_problem`。**判据本身在 `lifecycle.vault_ready`**，
        这里别再手写一遍。
        """
        vault_path = get_cfg().vault_path
        return {
            "initialized": vault_ready(vault_path),
            "vault_path": str(vault_path) if vault_path else "",
            "domains": list_domains(vault_path) if vault_path else [],
            "counts": _counts(vault_path),
            "busy": busy.what,
        }

    @router.get("/setup/check")
    def setup_check(path: str = "") -> dict:
        """顺手验一下用户填的路径——**失败也不拦着初始化**。

        它只回事实，不做判断：存在吗、能写吗、里面是不是已经有个库、有几个条目。
        """
        text = path.strip()
        if not text:
            return {"exists": False, "writable": False,
                    "looks_like_vault": False, "entries": []}
        return check_path(Path(text))

    @router.post("/setup/init")
    def setup_init(body: SetupBody) -> dict:
        """一条龙：**建骨架 → 写配置 → 重载**。

        **这个顺序不能反**，理由见下面那段注释：反过来的话
        `settings.validate` 会因为「目标还没有 `.git`」把用户自己的初始化
        请求拒掉，而那句文案还是「想搬过去用『迁移到别处』」。

        **只在「未初始化」时允许**（已初始化时 400）。它**不加 busy 锁**——
        那个状态下没有别的事在跑，加了是空转。
        """
        cfg = get_cfg()
        if vault_ready(cfg.vault_path):
            raise HTTPException(
                status_code=400,
                detail="已经有知识库了。要改配置去对应的设置，要搬家用「迁移到别处」",
            )

        target = body.vault_path.strip()
        if not target:
            raise HTTPException(status_code=400, detail="知识库目录不能留空")

        # 领域名不能随便起：候选清单之外的直接丢掉
        domains = [d for d in dict.fromkeys(body.domains) if d in DOMAIN_CANDIDATES]
        if not domains:
            raise HTTPException(
                status_code=400,
                detail="至少勾一个领域——一个都没有的话，投进来的每一条都会掉进「待归类」",
            )

        # **模型三件套要在这里查，必须在建库之前。**
        #
        # `settings.validate` 只检查「提交上来的键」，**缺的键它不管**；真正兜底的
        # 是 `apply_settings` 重载之后那句复查（`api/http.py:362`）。可那时候库
        # 已经建好、`.env` 里的 `KB_VAULT_PATH` 也已经写下去了——留下一个
        # 「有库、没模型」的半截状态。
        #
        # 本进程内它歪打正着还能救：`apply_settings` 是在换内存**之前**抛的
        # （`http.py:362` 早于 374 行的 `state["cfg"] = new_cfg`），所以
        # `get_cfg()` 拿到的还是旧的、`vault_path` 仍是 `None`，上面那道
        # 「已初始化」的闸门关不上，用户补上那几项再点一次能过。
        # **但那只是运气**——服务一重启，`reload_config` 就从 `.env` 里读到刚
        # 写下的 `KB_VAULT_PATH`，闸门关上，用户只剩「移除知识库」或手改 `.env`
        # 两条路。所以这个检查既不能省，也不能挪到建库之后。
        #
        # 留空的键**沿用现在的配置**（上面 2714 行那个 `cfg`，别再读一遍
        # `.env`）。判据要和兄弟端点 `/settings/test`（`router.py:481-486`）
        # 用同一套：**两边都是 `get_cfg()` 兜底**。不一致的话，用户会看见
        # 「测试连接：通了」紧接着「初始化：还差 API Key」——同一个
        # 「留空 = 不改」的语义，两个端点给出两种答案，而他还得自己猜哪个对。
        known = {
            "KB_LLM_MODEL": cfg.llm_model,
            "KB_LLM_BASE_URL": cfg.llm_base_url,
            "KB_LLM_API_KEY": cfg.llm_api_key,
        }
        known.update({k: v for k, v in body.values.items() if v})
        missing = [
            label
            for key, label in (
                ("KB_LLM_MODEL", "模型名"),
                ("KB_LLM_BASE_URL", "API 地址"),
                ("KB_LLM_API_KEY", "API Key"),
            )
            if not known.get(key)
        ]
        if missing:
            raise HTTPException(
                status_code=400,
                detail=(
                    "还差 " + "、".join(missing) + "，模型没配齐之前不建库。"
                    "（留空的那些，`.env` 里本来有就沿用；这次是那儿也没有。）"
                ),
            )

        # **先建库、再写配置。反过来是死锁。**
        #
        # `settings.validate` 现在要求 `KB_VAULT_PATH` 指向一个含 `.git` 的目录
        # （Task 3 加的路径校验），而这一刻那个库当然还不存在——先写配置的话，
        # 用户会被自己的初始化请求拒掉，文案还是「想搬过去用『迁移到别处』」。
        # 建在前就没这个问题：写配置那一刻库已经在了。
        #
        # 反过来的风险（库建好了、写配置失败）靠上面那道检查兜着：模型三件套
        # 已经确认齐了，`apply_settings` 到这一步不会因为「必填项是空的」而抛。
        # 剩下的失败（磁盘满、目标只读）由 `except OSError` 接。
        try:
            actions = init_vault(Path(target), domains)
        except OSError as exc:
            raise HTTPException(
                status_code=400, detail=f"建不了 {target}：{exc}"
            ) from exc

        values = dict(body.values)
        values["KB_VAULT_PATH"] = target
        try:
            apply_settings(values)
        except settings.SettingsError as exc:
            raise HTTPException(status_code=400, detail=str(exc)) from exc

        return {"actions": actions, "vault_path": target, "domains": domains}

    def _check_migration(source: Path, target: Path) -> None:
        """迁移的前置检查。不过就抛 400，一句一条。"""
        if not target.is_absolute():
            raise HTTPException(status_code=400, detail="目标要填绝对路径")
        if not source.exists():
            raise HTTPException(
                status_code=400,
                detail=f"当前库不在了：{source}。这本身就不正常，先查一下",
            )
        if target == source:
            raise HTTPException(status_code=400, detail="目标就是当前库，不用搬")
        if source in target.parents:
            raise HTTPException(
                status_code=400,
                detail=f"{target} 在当前库里面——搬进去会把库搬进自己肚子里",
            )
        if target.exists() and any(target.iterdir()):
            # **「非空」= 有任何一个条目**，不区分是不是库。迁移做完就把目标
            # 当成新库了，里面原有的任何东西都会混进笔记树——连一个
            # `.DS_Store` 都算。
            raise HTTPException(
                status_code=400,
                detail=f"目标非空（有 {len(list(target.iterdir()))} 个条目）。换一个空的目录",
            )

    @router.post("/setup/migrate")
    def setup_migrate(body: MigrateBody) -> dict:
        """整目录搬家：拷 → 校验 → 删源 → 改指向。

        **同步做**（和 `/new` 一个路子）。库这个量级拷贝是秒级，不值得为它
        再开一套进度协议；期间 busy 是 `"migrate"`，前端拿它做轮询。
        """
        # ⚠️ **下面整块比原稿少四格缩进**——头尾换成了 `with busy_guard(...)`，
        # 中间一字不改。409 的文案与状态码只有 `busy_guard` 那一份。
        #
        # ⚠️ **末尾那个 `()` 不能省。** `busy_guard(busy, what)` 返回的是
        # **工厂**（内层那个 `@contextmanager` 的 `organizing` 函数），不是
        # 上下文管理器本身——现成的用法就是 `organizing = busy_guard(busy)`
        # 之后 `with organizing():`。漏了 `()` 报的是
        # `TypeError: 'function' object does not support the context manager protocol`，
        # 而且只在**跑到这个端点**时才炸，导入期一切正常。
        with busy_guard(busy, "migrate")():
            source = vault()
            target = Path(body.target.strip())
            _check_migration(source, target)
            try:
                moved = migrate_vault(source, target)
            except LifecycleError as exc:
                raise HTTPException(status_code=400, detail=str(exc)) from exc
            # **这里必须和 `setup_init` 一样兜住 `SettingsError`，而且更要兜。**
            # 走到这一行时库已经搬完、源已经删了；`apply_settings` 要是以 500
            # 抛出去，`.env` 仍指着那个不存在的旧路径——服务显示「还没有知识库」，
            # 笔记却躺在 `target` 里，用户只能手工改 `.env` 才走得出来。
            # 校验没法提前做：`validate` 要求目标已经有 `.git`，而迁移前它必然
            # 是个空目录，提前校验会把合法的迁移目标全部拒掉。所以顺序是死的，
            # 只能把失败说清楚——把该填什么直接写进消息里。
            try:
                apply_settings({"KB_VAULT_PATH": str(target)})
            except settings.SettingsError as exc:
                raise HTTPException(
                    status_code=400,
                    detail=(
                        f"库已经搬到 {target} 了，但写 `.env` 没过校验（{exc}）。"
                        f"请把 `.env` 里的 KB_VAULT_PATH 手工改成 {target}"
                    ),
                ) from exc
            return {"moved": moved, "vault_path": str(target)}

    @router.post("/setup/remove")
    def setup_remove() -> dict:
        """移除知识库：**全部删除，一点痕迹不留**——用户的原话。

        逐项汇报是因为这里做不到「全部成功」：Windows 上正被服务打开的文件
        （今天的日志）删不掉，那是操作系统的边界，不是设计妥协。
        """
        # 同 `setup_migrate`：整块少四格缩进，409 只有 `busy_guard` 那一份，
        # 末尾那个 `()`（工厂调用）也别漏。
        with busy_guard(busy, "remove")():
            vault_root = get_cfg().vault_path
            result = remove_vault(vault_root, data_dir)
            if result["vault_removed"]:
                unbind_vault()
            else:
                # **库还在就别解绑。** 否则界面上显示「还没有知识库」而笔记
                # 还躺在原地——正是本设计要消灭的那种状态。
                result["notes"].append(
                    "笔记目录没删掉，所以还指着它。处理完再点一次「移除」"
                )
            return result
```

**(d)** `LifecycleError` / `list_notes` / `list_drafts` 要 import——**这份和 (a)
是同一份**，改一处就得两处一起改，别让它们岔开。**三行，一行都不能少**：

```python
from kb.core.lifecycle import (
    Busy,
    LifecycleError,
    check_path,
    migrate_vault,
    remove_vault,
    vault_ready,
)
from kb.core.vault import list_domains, list_drafts, list_notes
from kb.core.vault_setup import DEFAULT_DOMAINS, DOMAIN_CANDIDATES, init_vault
```

⚠️ **第三行看着像 Task 6 的东西，但它必须在这里就位。** `settings_context`
是**请求时**执行的：Task 6 用到 `DEFAULT_DOMAINS` 时这个文件里要是没这个名字，
第一次开 `/settings` 就是 500（`NameError`），不是等谁点某个按钮才炸。
展开的理由写在 (a) 那边。

- [ ] **Step 9: 跑目标测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py tests/core/test_lifecycle.py -v
```

预期：全绿。若 `test_init_creates_the_vault_and_writes_env` 报「建不了……」，检查 `tmp_path` 下建 `.git` 的权限；若 `_check_migration` 报 `iterdir` 空目录错误，确认 `target.exists()` 那个短路写对了。

- [ ] **Step 10: ruff + 全量 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
git -C "E:/Study_Projects/KN_Base_t5" add src/kb/core/lifecycle.py src/kb/api/http.py src/kb/web/router.py tests/core/test_lifecycle.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base_t5" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t5.txt"
```

```
feat: /setup/* 端点——初始化、路径体检、迁移、移除

逻辑在 core/lifecycle.py（core 不 import web/api），端点只是协议转换。

迁移：
- 拒绝非空目标，「非空」= 有任何一个条目，连 .DS_Store 都算——迁移做完
  就把目标当成新库了，里面原有的任何东西都会混进笔记树
- 拒绝目标是源自己 / 源的子目录（D:\a 搬到 D:\a\b 会把库搬进自己肚子）
- 拷完只比文件数 + 总字节数，不逐文件哈希：这库按几千条设计，727K 时
  哈希是零成本，几千条带附件时会明显变慢，而拷贝被截断几乎必然改变
  文件数和字节数。取舍是有意的
- 失败回滚目标，源一根汗毛不动——数据有两份时永远让旧的那份先活着

移除：
- 先改名再删。改名同盘内是原子的，所以「删了一半」那个窗口从一开始就
  不存在。**但失败改回去不等于毫发无伤**——rmtree 是边遍历边删的，中途
  失败时前面那些文件已经没了，只能把剩下的搬回原名。所以失败是「残缺但
  还在」，不是「没动过」，文案照这个说
- data/runtime/service.json 绝不在删除清单里：那是 kb stop 唯一的线索，
  删了就是停不掉的孤儿进程，而那个 bug 症状是测试全绿、开发机上服务失控
- 删不掉 vault 时**不解绑 .env**，否则界面说「还没有知识库」而笔记还在
- 今天的日志正被服务打开着，Windows 上删不掉——如实汇报，不假装干净

初始化：
- 只在未初始化时允许，已初始化时 400 指向两条正路
- 领域名必须来自候选清单，勾选后一个不剩就 400，文案说清「不然每条都
  会掉进待归类」
- 不加 busy 锁：那个状态下没有别的事在跑

busy_guard 收一个 what 参数（默认 organize，老调用点不动）。迁移与移除
自己要占的名目是 migrate / remove，本来要再手写两份 acquire + 409——那正是
Task 4 刚合掉的那两份逐字抄，且这次连状态码与文案都是新抄的。改 409 文案时
漏掉哪一份，症状还是「两条路给出两种答案」。

删 vault 那棵树走 _rmtree（先摘只读位再删，删不掉再试一次），不是裸
shutil.rmtree：**git 的松散对象是只读的**（实测 0o100444），Windows 上
rmtree 碰到只读文件直接抛 PermissionError [WinError 5] 拒绝访问，手工
rm -rf .git 同样删不掉。不管这一步，「移除知识库」与「搬家」在**真库**上
必然失败——而假 .git 目录的用例是绿的，失败的样子又像个权限问题，
排查方向全错。这条是拿真 git 仓库实测出来的，见两个
`*_with_readonly_object` 回归用例。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 6: 设置窗——「知识库」分组的一屏两态

**界面基准是两张 mockup：** `.superpowers/brainstorm/1668-1790038075/content/wizard.html`（一条龙三段 + 初始化后那张）、`first-run.html`（右半张：已初始化态）。

**Files:**
- Modify: `src/kb/web/templates/_settings.html`
- Modify: `src/kb/web/templates/base.html`
- Modify: `src/kb/web/static/style.css`

---

- [ ] **Step 1: `settings_context` 多给模板七样东西**

`src/kb/web/router.py` 的 `settings_context`：把最后那行 `return {"groups": groups}` 换成下面这一整块。

⚠️ **别按行号找**——Task 5 在 `settings_context` 上面加了约 34 行（`/setup/*` 那批），
行号已经从写计划时的位置整体下移了。**按函数名和那句 `return {"groups": groups}` 定位**。

```python
    # 「知识库」那一组要**一屏两态**，模板得知道现在是哪一态。
    # ⚠️ 局部变量**不要再叫 `vault_ready`**——那会遮住 `lifecycle.vault_ready`
    # 这个函数，同一个名字在这里指两样东西。判据本身在它那儿，只有那一份。
    vault_path = cfg.vault_path
    ready = vault_ready(vault_path)

    return {
        "groups": groups,
        "vault_ready": ready,
        "vault_path": str(vault_path) if vault_path else "",
        # 已初始化态要显示磁盘上住了哪些领域。**从磁盘读，不从配置读**——
        # 领域的唯一真源始终是磁盘（Q95 那条）。
        "domains": list_domains(vault_path) if vault_path else [],
        # 一条龙的段①要预填模型名与地址。**密钥不预填**（掩码都不给）：
        # 「留空 = 不改」是它的语义，预填成掩码等于让人一保存把 key 覆盖掉。
        "llm_model": values.get("KB_LLM_MODEL", ""),
        "llm_base_url": values.get("KB_LLM_BASE_URL", ""),
        # 段③那九个复选框。清单是常量、不进 `.env`——勾选的作用只是建出目录，
        # 建完就不再被读第二次（见 `DOMAIN_CANDIDATES` 那段注释）。
        "domain_candidates": DOMAIN_CANDIDATES,
        "default_domains": DEFAULT_DOMAINS,
    }
```

`DOMAIN_CANDIDATES` / `DEFAULT_DOMAINS` 在 Task 5 的 Step 8(a) 已经 import 进这个文件；`list_domains` 同理。

- [ ] **Step 1b: 顺手删掉 `_value` 里那段死代码**

同一文件 `_value` 里现在有这么一段（**同样别按行号找**，按这段代码本身定位）：

```python
        if f.kind == "readonly":
            if f.key == "KB_VAULT_PATH":
                return str(cfg.vault_path)
            if f.key == "KB_PORT":
                return str(cfg.port) if cfg.port else "自动"
```

`KB_VAULT_PATH` 在 Task 3 已经从 `readonly` 改成 `text`，那个分支**永远走不到**了（而且「知识库」那一组从 Task 6 Step 2 起也不再走字段循环，`_value` 根本不会为它被调用）。删掉它，并把 `KB_PORT` 那条升上来：

```python
        if f.kind == "readonly" and f.key == "KB_PORT":
            return str(cfg.port) if cfg.port else "自动"
```

`KB_PORT` 用 `cfg.port` 而不是 `.env` 字面值的理由不变（`.env` 里没写它的人会看到空白，尽管跑起来是个自动找的空闲端口）。

**同一函数开头那段 docstring（第 74-91 行）也要改掉**：它写着「`.env` 里没写 `KB_VAULT_PATH` / `KB_PORT` 的人（默认值就是给这种 `.env` 准备的），那两栏会是空白——尽管跑起来用的是 `DEFAULT_VAULT_PATH`」。`DEFAULT_VAULT_PATH` 在 Task 1 已经不存在了。改成：

```
        只读栏不能按字面值显示：`.env` 里没写 `KB_PORT` 的人（默认值就是给
        这种 `.env` 准备的），那一栏会是**空白**——尽管跑起来是个自动找的
        空闲端口。一栏空白看起来像坏了，而这一页的任务正是让人看清楚现在在
        用的是什么。
```

- [ ] **Step 2: 改 `_settings.html`——「知识库」那一组换成一屏两态**

把 `_settings.html` **第 23-59 行整块**（`{% for g in groups %}` 到它对应的 `{% endfor %}`，含 `section.pane` 和里面的字段循环）换成下面这样。**关键是「知识库」这一组不走通用字段循环，它整个换成自己的块**：

⚠️ **别只换 26-57 行那半截。** 只换里层的 `{% for f in g.fields %}` 的话，外层那句 `{% for g in groups %}` 和 `section.pane` 会**原样留着**，再套上贴出来的这一整块——Jinja 语法平衡、不报错，**只是每一组会被渲染两遍**。这种错不会红，只会让界面上东西多一倍。

```html
    {% for g in groups %}
      <section class="pane" data-group="{{ loop.index0 }}"
               {{ 'hidden' if not loop.first }}>
        {# 「知识库」是唯一一组自己画的：它要**一屏两态**（未初始化 = 一条龙，
           已初始化 = 状态 + 三个动作）。通用字段循环表达不了这个，
           硬塞进去会把模板变成一堆 if。 #}
        {# **`'知识库'` 这三个字要和 `settings.py:63` 的 `Group("知识库", …)`
           逐字对得上。** 对不上时不会报错，只是这一组悄悄退回通用字段循环
           （一个只读的路径框），而 Step 7 的
           `test_settings_shows_the_wizard_before_init` 找不到 `id="wizard"`
           会红——这就是那条测试守着的东西。别顺手给分组改名。 #}
        {% if g.name == '知识库' %}
          {% include "_vault_pane.html" %}
        {% else %}
          {% for f in g.fields %}
            {# …原有那段一字不动… #}
          {% endfor %}
        {% endif %}
      </section>
    {% endfor %}
```

- [ ] **Step 3: 新建 `src/kb/web/templates/_vault_pane.html`**

```html
{# 「知识库」分组的一屏两态。由 `_settings.html` include 进来。 #}

{% if vault_ready %}
  {# ---------- 已初始化 ---------- #}
  <div class="vault-card">
    <p class="vault-state"><span class="dot on" aria-hidden="true"></span>已初始化</p>
    <p class="vault-path">{{ vault_path }}</p>
    <p class="hint">领域：{{ domains | join('　·　') if domains else '还没有领域——去库里建一个一级文件夹' }}</p>
    <div class="vault-actions">
      <button type="button" class="secondary" onclick="openMigrate()">迁移到别处</button>
      <button type="button" class="secondary" onclick="openVaultFolder()">打开目录</button>
      {# `secondary danger` 两个 class，**不是光一个 `danger`**——
         形状靠 `button.secondary`，`button.danger` 只换颜色。理由见
         style.css 里那两条规则上面的注释。 #}
      <button type="button" class="secondary danger"
              onclick="openRemove()">移除知识库…</button>
    </div>
  </div>
  <p class="hint">
    要改模型配置或日志级别，去左边对应的那一栏。
    要加领域，在资源管理器里给库建个一级文件夹就行——服务下次就认。
  </p>
{% else %}
  {# ---------- 未初始化：一条龙 ---------- #}

  {# **这一块不是 `<form>`，是 `<div>`。别改回去。**

     它整块住在 `_settings.html` 的 `<form class="settings-panes"
     id="settings-form">` 里面（那个 form 从模板第 21 行开到第 70 行，
     `{% for g in groups %}` 整个在它肚子里）。而 HTML 规范里 `form` 的
     内容模型明确排除 `form` 后代；解析器在「表单指针非 null」时碰到
     `<form>` 开始标签是**直接丢弃**的。这里是 `openSettings()` 里
     `box.innerHTML = await r.text()` 塞进来的，`box` 是 `#settings-modal`
     （`base.html` 里 body 下的 div，祖先链上没有 form），所以指针初始为
     null → 外层那个建出来、指针被设上 → **内层这个一声不响地没了**。

     丢掉的后果一个都不报错：`getElementById('wizard-form')` 是 `null`
     （`testWizardConnection` 第一行就 TypeError，「测试连接」永远显示失败）；
     `onsubmit` 随标签一起没了 → 点「初始化」走的是外层 `#settings-form` 的
     `saveSettings`，POST 到 `/settings`——要么 400，要么弹「已保存 N 项」
     **而库根本没建**。正是这个项目最恨的那种「看起来一切正常」。

     所以：值一律**按 id 读**，输入框一律**不给 `name`**。不给 `name` 是
     沿用本文件已有的那条约定（只读字段也不给 `name`：不进 `FormData`，
     就不会被 `saveSettings` 顺带提交）——顺带还解决了一件事：这些字段叫
     `KB_LLM_MODEL` 之类，跟「模型配置」那一组的字段**同名**，留着 `name`
     的话 `Object.fromEntries(new FormData(form))` 会取到重复 name 的最后一个。 #}
  {# ⚠️ **回车必须拦掉。** 这条龙住在 `#settings-form` 肚子里（`_settings.html:21`
     开到 70 行），而那个 form 里有一颗 `<button type="submit">保存</button>`
     （第 68 行）。在段①/段② 任意输入框里按回车，浏览器会走**隐式提交**去点
     那颗「保存」→ `saveSettings` → POST `/settings`。用户填完路径顺手一回车，
     看到的会是「没有要改的」（`saveSettings` 按 `data-init` 过滤，通常什么都
     不提交）**而库根本没建**——正是上面那段注释里最怕的那个形状。

     拦掉而不是改成「回车 = 初始化」：回车时用户多半还停在第一段，
     那会儿替他建库是越俎代庖。 #}
  <div class="wizard" id="wizard"
       onkeydown="if (event.key === 'Enter') { event.preventDefault(); }">

    <div class="wizard-step">
      <span class="wizard-num" aria-hidden="true">1</span>
      <div class="wizard-body">
        <p class="wizard-title">模型配置</p>
        <p class="hint">整理笔记要调模型，先把钥匙给它。</p>
        <div class="field-row">
          <div class="field">
            <label for="w-KB_LLM_MODEL">模型名</label>
            <input id="w-KB_LLM_MODEL" value="{{ llm_model }}">
          </div>
          <div class="field">
            <label for="w-KB_LLM_BASE_URL">API 地址</label>
            <input id="w-KB_LLM_BASE_URL" value="{{ llm_base_url }}">
          </div>
        </div>
        <div class="field">
          <label for="w-KB_LLM_API_KEY">API Key</label>
          <div class="field-row">
            <input id="w-KB_LLM_API_KEY" type="password"
                   value="" placeholder="留空表示不改" autocomplete="off">
            <button type="button" class="secondary"
                    onclick="testWizardConnection(this)">测试连接</button>
          </div>
        </div>
      </div>
    </div>

    <div class="wizard-step">
      <span class="wizard-num" aria-hidden="true">2</span>
      <div class="wizard-body">
        <p class="wizard-title">知识库目录</p>
        <p class="hint">笔记存哪儿。它会是一个独立的 git 仓库。</p>
        <div class="field-row">
          <input id="w-vault-path" class="mono"
                 placeholder="D:\我的知识库" autocomplete="off">
          <button type="button" class="secondary"
                  onclick="checkPath(this)">检查</button>
        </div>
        <p class="hint" id="w-check-msg" hidden></p>
      </div>
    </div>

    <div class="wizard-step">
      <span class="wizard-num" aria-hidden="true">3</span>
      <div class="wizard-body">
        <p class="wizard-title">初始领域</p>
        <p class="hint">
          领域就是知识库的顶层分类。勾中的会建成一级目录。<br>
          <b>至少勾一个</b>——一个都没有的话，投进来的每一条都会变成「待归类」。
        </p>
        {# 同样不给 `name`——`runInit` 按 `#wizard .domain-box input:checked` 收。 #}
        <div class="domain-grid">
          {% for d in domain_candidates %}
            <label class="domain-box">
              <input type="checkbox" value="{{ d }}"
                     {{ 'checked' if d in default_domains }}>
              <span>{{ d }}</span>
            </label>
          {% endfor %}
        </div>
      </div>
    </div>

    <p class="settings-msg" id="wizard-msg" hidden></p>

    <div class="settings-actions">
      <span class="hint wizard-foot">
        初始化会建目录、写 .gitignore、初始化 git 仓库。幂等——重复点不会毁掉已有的东西。
      </span>
      {# 没有 form 了，所以是 `type="button"` + `onclick`，不是 `type="submit"`。
         写成 submit 的话它会去提交外层的 `#settings-form`。 #}
      <button type="button" class="primary" id="wizard-go"
              onclick="runInit(this)">初始化</button>
    </div>
  </div>
{% endif %}
```

模板里用到的 `llm_model` / `llm_base_url` / `domains` / `domain_candidates` / `default_domains` / `vault_ready` / `vault_path` **全部来自 Step 1 那个返回值**，没有一个是模板现算的。（`_settings.html` 是 include 进来的，跟 `settings_context` 共享同一个上下文。）

**⚠️ 已初始化那张卡上的三个按钮，本任务结束时是「死」的**——`openMigrate()` / `openVaultFolder()` / `openRemove()` 要到 **Task 7 Step 3** 才定义，这会儿点了不会有任何反应（浏览器控制台里是 `… is not defined`）。**这是有意的**：Task 6 只负责「一屏两态」画得对不对，交互归 Task 7。别提前把 Task 7 的 JS 抄过来，也别以为自己漏写了什么。本任务 Step 8 那一眼重点看的是**未初始化态那条龙**（它这一段是完整可用的）。

- [ ] **Step 4: 加 `base.html` 里的 JS**

在 `testConnection` 函数之后插入：

```javascript
    // ---- 一条龙初始化 ----
    //
    // **整块按 id 读，一个字段都不从 `#settings-form` 里取。**
    // ① 一条龙那块压根不是 `<form>`——它是嵌在 `#settings-form` 里面的一个
    //    div（原因写在 `_vault_pane.html` 那段注释里，别改回去）；
    // ② `saveSettings` 按 `data-init` 过滤没动过的字段，那是为「改配置」设计的，
    //    而一条龙每次都要把三段全交上去（不然模型名会被当成「没改」漏掉）。
    // 共用 `.pane` 那套样式，各自的 handler 各写各的。

    function _wizardMsg(text, ok) {
      const el = document.getElementById('wizard-msg');
      el.textContent = text;
      el.classList.toggle('bad', !ok);
      el.hidden = false;
    }

    // ⚠️ **别把 `testConnection` 再抄一份。**
    // 计划早先的稿子就是抄的：那条龙版本跟 `base.html:280` 的设置窗版本
    // 逐字相同——三分支、`_detail(data, '未知原因')`、「只有请求本身没走通
    // 才值得报状态码」那段理由、`finally` 里恢复按钮，一处不差，只差
    // 「值从哪来」和「写进哪个元素」。同一份文件里 `+ '（HTTP ' + r.status + '）'`
    // 这个写法一度有四处。这个项目刚为同一类重复做过三次收敛
    // （`busy_guard` 合掉两份 409、`organizing()` 合掉两份去重、
    // Task 10 又要合 `_BUSY_LABELS`），这条是第四次。
    //
    // 做法：把「试一次、写结果」抽出来，两个调用点只留「值从哪来」。
    // **`testConnection` 的对外签名一个字都不变**（`_settings.html:67` 那句
    // `onclick="testConnection(this)"` 是模板里的字面量，改它就得改那一行），
    // 所以是加一层薄壳，不是改调用方。

    async function _testConnection(btn, values, msg) {
      btn.disabled = true;
      msg('测试中…', true);
      try {
        const r = await fetch('/settings/test', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({values: values}),
        });
        const data = await r.json().catch(function () { return {}; });
        if (r.ok && data.ok) {
          msg('通了 · ' + data.seconds + ' 秒', true);
        } else if (r.ok) {
          // **HTTP 通了、模型没通**（模型名不存在之类）。这时状态码是 200，
          // 缀在后面是噪音——只有请求本身没走通（422 那种）才值得报状态码。
          msg('不通：' + _detail(data, '未知原因'), false);
        } else {
          // **带上状态码**：不带的话 422 会显示成「未知原因」，排查时找不到北
          msg('不通：' + _detail(data, '未知原因') + '（HTTP ' + r.status + '）', false);
        }
      } catch (e) {
        msg('测试失败：' + e, false);
      } finally {
        // **恢复按钮必须走 `finally`**：原来只包了 `r.json()`，`fetch` 本身
        // 抛错时这行根本执行不到，按钮就此永久禁用，页面上还没有任何说明。
        btn.disabled = false;
      }
    }

    function testWizardConnection(btn) {
      const values = {};
      ['KB_LLM_MODEL', 'KB_LLM_BASE_URL', 'KB_LLM_API_KEY'].forEach(function (k) {
        values[k] = document.getElementById('w-' + k).value;
      });
      _testConnection(btn, values, _wizardMsg);
    }

    async function checkPath(btn) {
      const path = document.getElementById('w-vault-path').value;
      const msg = document.getElementById('w-check-msg');
      if (!path.trim()) {
        // **空输入在这儿拦住，别去问后端。** 后端拿 `Path('')` 当**当前目录**：
        // `exists` 为真、`entries` 是服务目录那一大堆，回过来的话跟用户手里
        // 那个空框对不上（「目录已存在，里面有 37 个条目」——哪个目录？）。
        // 端点那边照样回 200，`test_check_with_an_empty_path_is_not_an_error`
        // 钉的是「不 422」，不是「这条路径有话说」。
        msg.textContent = '还没填路径。填一个目录的绝对路径再点「检查」';
        msg.classList.add('bad');
        msg.hidden = false;
        return;
      }
      btn.disabled = true;
      try {
        const r = await fetch('/setup/check?path=' + encodeURIComponent(path));
        const d = await r.json();
        const bits = [];
        if (!d.exists) bits.push('这个目录还不存在，初始化时会建出来');
        else bits.push('目录已存在，里面有 ' + d.entries.length + ' 个条目');
        if (d.looks_like_vault) bits.push('它已经是一个知识库了——会直接用它，已有的东西不动');
        if (!d.writable) bits.push('这个位置写不进去，换一个地方');
        msg.textContent = bits.join('。');
        msg.classList.toggle('bad', !d.writable);
        msg.hidden = false;
      } catch (e) {
        msg.textContent = '检查失败：' + e;
        msg.classList.add('bad');
        msg.hidden = false;
      } finally {
        btn.disabled = false;
      }
    }

    // **Enter 不能把外层那个设置表单交上去。**
    // 一条龙整块住在 `#settings-form` 里面（所以它自己不是 form，见
    // `_vault_pane.html` 那段注释）。而 form 的隐式提交规矩是：在文本框里按
    // Enter，浏览器会拿**这个 form 里第一个 submit 按钮**（`_settings.html`
    // 底部那个「保存」）交一次。于是「在路径框里敲回车，想检查一下」变成
    // 「把设置存了」——还附一句「已保存」。一条龙这几个字段**一个 `name`
    // 都没有**，压根不会被提交，所以存的是个谁也没改的东西。
    //
    // 拦法：**委托在 `document` 上，不是绑在 `#wizard` 上。** `#wizard` 是
    // `/settings` 那个片段取回来之后才进 DOM 的（`base.html:181`），脚本
    // 解析时它还不存在——绑在它上面会 `null.addEventListener` 直接炸。
    // 在处理函数里现找，才等得到它。
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Enter') return;
      const wizard = document.getElementById('wizard');
      if (!wizard || !wizard.contains(e.target)) return;   // 不关一条龙的事
      if (e.target.tagName === 'TEXTAREA') return;         // 文本域里回车是换行
      e.preventDefault();
      // 路径框：Enter = 点旁边的「检查」，跟按钮是同一件事
      if (e.target.id === 'w-vault-path') {
        const btn = e.target.closest('.field-row').querySelector('button');
        if (btn) checkPath(btn);
      }
    });

    // `btn` 就是「初始化」那个按钮自己（`onclick="runInit(this)"`）。
    async function runInit(btn) {
      const wizard = document.getElementById('wizard');
      const domains = Array.from(wizard.querySelectorAll('.domain-box input:checked'))
        .map(function (el) { return el.value; });
      if (!domains.length) {
        // 前端拦一道只是礼貌，后端也会 400——理由写在按钮旁边那句里
        _wizardMsg('至少勾一个领域——一个都没有的话，投进来的每一条都会掉进「待归类」', false);
        return;
      }
      const values = {};
      ['KB_LLM_MODEL', 'KB_LLM_BASE_URL', 'KB_LLM_API_KEY'].forEach(function (k) {
        const v = document.getElementById('w-' + k).value;
        if (v) values[k] = v;
      });
      btn.disabled = true;
      _wizardMsg('正在建库…', true);
      try {
        const r = await fetch('/setup/init', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({
            vault_path: document.getElementById('w-vault-path').value,
            domains: domains,
            values: values,
          }),
        });
        const data = await r.json().catch(function () { return {}; });
        if (!r.ok) {
          _wizardMsg(_detail(data, '初始化失败') + '（HTTP ' + r.status + '）', false);
          return;
        }
        // 建成了：**把设置窗整个重载一遍**。同一屏换成「已初始化」态，
        // 不做局部刷新——两态的形状差得远，重载比补丁简单也更不容易错。
        //
        // ⚠️ **这句消息的写法有讲究，别顺手改回 `_wizardMsg`。**
        // `openSettings()` 会把 `#settings-modal` 的 `innerHTML` **整个换掉**
        // （`base.html:174` 先换成「载入中…」、`base.html:181` 再换成真片段），
        // 所以在它前后直接写 `_wizardMsg` 都不成立：
        //   ① 写在它**之前** → 那句跟着旧 DOM 一起被扔掉，用户根本看不见；
        //   ② 写在它**之后** → `#wizard-msg` 这个东西已经不存在了，它属于
        //      「未初始化」那一态，重载后那一屏换成「已初始化」了。
        // 落到 `_msg` 上：它写的是 `_settings.html:61` 的 `#settings-msg`，
        // 那是**两态共用**的底部消息位（在 `.pane` 外面、`.settings-actions`
        // 上面），重载后照样在。`data.actions` 里是「建了几个领域目录、
        // 播了几条种子笔记」——**用户要看的正是这个**（`design.md:159`），
        // 丢了就等于「点完按钮什么都没说」。
        const done = '建好了：' + (data.actions || []).join('、');
        await openSettings();
        showGroupByName('知识库');
        _msg(done, true);
      } catch (e) {
        _wizardMsg('初始化失败：' + e, false);
      } finally {
        btn.disabled = false;
      }
    }

    function showGroupByName(name) {
      const modal = document.getElementById('settings-modal');
      const nav = Array.from(modal.querySelectorAll('.nav-item'))
        .find(function (b) { return b.textContent.trim() === name; });
      if (nav) showGroup(nav);
    }
```

- [ ] **Step 5: `base.html` 加「首屏引导卡跳到那条龙」**

`showGroup` 是现成的（第 197 行），引导卡只要在 `openSettings()` 之后调 `showGroupByName('知识库')`。引导卡本身在 Task 7 加，这里只把函数备好（上一步已经有了）。

- [ ] **Step 6: `style.css` 加一条龙的样式**

追加到 `style.css` 末尾（**色值一律走变量层，不能写死十六进制**——`scripts/derive_skins.py` 生成的六套皮肤靠变量换色，写死就只在一套皮肤上对）：

```css
/* ---- 知识库那条龙（设置窗「知识库」分组） ---- */

.wizard-step {
  display: flex;
  gap: 11px;
  margin-bottom: 16px;
}

.wizard-num {
  flex: none;
  width: 18px; height: 18px;
  border-radius: 50%;
  background: var(--accent);
  color: var(--on-accent);
  font-size: 10px; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
}

.wizard-body { flex: 1; min-width: 0; }

.wizard-title {
  color: var(--ink);
  font-size: 11.5px; font-weight: 600;
  margin: 0 0 4px;
}

.field-row { display: flex; gap: 8px; align-items: flex-start; }
.field-row > .field { flex: 1; min-width: 0; }
.field-row > button { flex: none; white-space: nowrap; }

.domain-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8px;
}

/* ⚠️ **选择器必须带 `.settings-panes` 前缀，不能只写 `.domain-box`。**
   这几个格子渲染在 `.settings-panes` 里，那里已经有两条同族规则：

     `.settings-panes label`  (0,1,1)  → `display: block; margin-bottom: 4px; color: var(--muted)`
     `.settings-panes input`  (0,1,1)  → `width: 100%; padding: 7px 10px; border: 1px solid; …`

   光写 `.domain-box` 是 (0,1,0)，**两条都压不过**：`display: flex` 被
   `label` 那条的 `display: block` 吃掉，格子的排版回到竖排；而复选框会被
   `input` 那条撑成 `width: 100%` 再加上内边距和边框，**变成一个占满整格的
   大框**。加上前缀是 (0,2,0) / (0,2,1)，才轮得到下面这些生效。 */
.settings-panes .domain-box {
  display: flex; align-items: center; gap: 8px;
  border: 1px solid var(--line);
  border-radius: 5px;
  padding: 8px 10px;
  font-size: 10.5px;
  color: var(--ink);
  cursor: pointer;
}
/* `:has` 让整格跟着复选框变——点框和点字是一回事，但视觉上得有主次 */
.settings-panes .domain-box:has(input:checked) {
  border-color: var(--accent);
  background: var(--accent-soft);
}
/* 复选框要把上面 `.settings-panes input` 撑出来的那几样**逐条收回去**，
   少收一样就看得出来（比如漏了 `width` 就是一个占满整格的方框）。 */
.settings-panes .domain-box input {
  width: auto; padding: 0; border: 0; background: none;
  margin: 0; accent-color: var(--accent);
}

.vault-card {
  border: 1px solid var(--line);
  border-radius: 6px;
  padding: 12px 14px;
}

.vault-state {
  display: flex; align-items: center; gap: 7px;
  color: var(--ink); font-size: 11px; font-weight: 600;
  margin: 0 0 9px;
}

.vault-state .dot {
  width: 7px; height: 7px; border-radius: 50%;
  background: var(--muted);
}
.vault-state .dot.on { background: var(--accent); }

.vault-path {
  color: var(--muted);
  font-family: monospace;
  font-size: 10px;
  margin: 0 0 4px;
  overflow-wrap: anywhere;   /* 长路径不许把卡片撑破 */
}

.vault-actions { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 12px; }

/* 「移除知识库…」——**次级按钮的形状 + 危险色**。

   形状（padding / font / cursor / 圆角 / 胶囊）全靠 `button.secondary`
   那一份，这里只换颜色。**所以 HTML 里写的是 `class="secondary danger"`，
   不是光一个 `danger`。** 光写 `danger` 的话站内没有任何规则给它 padding
   与字体，浏览器默认外观直接露出来——本文件 1124 行把这个毛病记过两次
   （「光板 `<button>`，站内没有任何规则命中」）。
   危险色不另写一套形状，理由还是那句：各写一份就是两处真相。 */
button.secondary.danger {
  border-color: var(--danger);
  color: var(--danger);
}
/* ⚠️ hover 用 `--danger-hover` + `--on-danger`，**不是 `--accent-soft`**。
   `--accent-soft` 是淡蓝：一颗红边红字的「移除知识库…」悬停变淡蓝底，
   跟侧栏「退出」那颗（`style.css:962-963`，`.side-btn.quit:hover` 用的
   就是 `--danger-hover`）不是一套，看着像两拨人做的。
   两个变量都现成在变量清单里（`--danger-hover` 在 `style.css:67`），
   不用新增。 */
button.secondary.danger:hover {
  background: var(--danger-hover);
  border-color: var(--danger);
  color: var(--on-danger);
}

/* ⚠️ **这条不加的话，「这个位置写不进去」会是一行普通灰字。**
   `checkPath` 里是 `msg.classList.toggle('bad', !d.writable)`，而 `msg` 是
   `<p class="hint" id="w-check-msg">`（Step 3 段②）。全站只有一条 `.bad` 规则
   ——`style.css:1081` 的 `.settings-msg.bad`，作用域不覆盖 `.hint`；裸 `.hint`
   规则也没有（只有 `.settings-panes .hint` 与 `.modal.notice .hint` 两条
   带作用域的）。结果就是**「路径不可写」和「检查通过」长得一模一样**，
   而这条提示的全部意义就是让用户分清这两种。 */
.hint.bad { color: var(--danger); }

.wizard-foot { flex: 1; }
```

**还要改一处已有的规则**（`style.css` 里紧跟在 `button.primary` 之后那条，约 933 行）：

```css
button.primary:disabled,
button.secondary:disabled { opacity: .6; cursor: default; }
```

**把 `button.danger:disabled` 加进这个选择器列表**，别另写一条：

```css
button.primary:disabled,
button.secondary:disabled,
button.danger:disabled { opacity: .6; cursor: default; }
```

Task 7 那个移除确认模态靠 `disabled` 拦住没勾「我知道，删了找不回来」的人（`onchange` 里 `disabled = !this.checked`）。不补这条的话按钮**看着照样能点**、光标还是小手——而 `button.secondary` 把 `cursor: pointer` 写死了。那条规则自己的注释就写着「**两颗都写，别只顾 `.primary`**」，现在有第三颗了。

（`button.danger:disabled` 的特异度是 0-2-1，压得过 `button.secondary` 的 0-1-1，所以补进列表里就够，不用提升特异度。）

用到的变量名**已核对过，都在这份清单里**（`style.css` 里现存的）：`--accent`、`--on-accent`、`--accent-soft`、`--danger`、`--line`、`--ink`、`--muted`。

**一个都不新增。** 要加新变量，得同时改 `src/kb/web/skins.py`（权威清单）和重跑 `scripts/derive_skins.py`——六套皮肤都从那儿推出来，漏一套那个颜色在别的皮肤上就是错的。为了一条龙去动皮肤推导，不划算。想核对现有名字：

```bash
grep -o -- '--[a-z0-9-]*' src/kb/web/static/style.css | sort -u
```

- [ ] **Step 7: 加一条渲染测试，钉住「一屏两态」**

Task 3 把 `test_readonly_fields_show_the_effective_values` 里 vault 那半断言删掉了（那边契约变了，确实该没）——**留下一个覆盖缺口：没有任何测试证明设置窗真的渲染出了两种状态**。这里补上。

追加到 `tests/web/test_setup.py`（Task 5 建的，`env` / `app` / `client` / `initialized_vault` 那几个夹具现成的）：

```python
# ---------- 设置窗的一屏两态 ----------

def test_settings_shows_the_wizard_before_init(client):
    """未初始化：画的是那条龙，不是「已初始化」那张卡。"""
    html = client.get("/settings").text
    assert '<div class="wizard" id="wizard">' in html
    assert 'id="w-KB_LLM_MODEL"' in html          # 段①
    assert 'class="domain-box"' in html           # 段③的复选框
    assert "已初始化" not in html


def test_the_wizard_is_not_a_nested_form(client):
    """**整页只有 `#settings-form` 一个 `<form>`。**

    一条龙整块住在 `#settings-form` 里面（那个 form 从 `_settings.html`
    第 21 行开到 70 行）。它自己要是也写成 `<form>`，浏览器解析
    `box.innerHTML = await r.text()` 时会**一声不响地把内层那个丢掉**——
    `form` 的内容模型排除 `form` 后代，解析器碰到表单指针非 null 时的
    `<form>` 开始标签是直接忽略的。丢掉之后：`getElementById('wizard-form')`
    是 null、「测试连接」永远 TypeError、`onsubmit` 跟着标签一起没、
    点「初始化」实际提交的是外层 `saveSettings`——**库根本没建，界面上
    还可能弹一句「已保存 N 项」**。

    ⚠️ **这条测试的能力边界要说清**：`TestClient` 拿到的是**服务端渲染的
    字符串**，浏览器丢不丢 form 它一无所知。所以它只能从源头钉住
    「我们没写出嵌套 form」——挡不住有人把一条龙整体挪进另一个 form 里，
    但挡得住最常见的那两种改法（把 div 改回 form、给外层再套一层 form）。
    """
    html = client.get("/settings").text
    assert html.count("<form") == 1
    assert 'id="settings-form"' in html


def test_the_wizard_swallows_enter(client):
    """**回车不许触发外层表单的隐式提交。**

    这条龙住在 `#settings-form` 肚子里，而那个 form 里有一颗
    `<button type="submit">保存</button>`（`_settings.html:68`）。不拦的话，
    在段①/段② 的输入框里按回车 → 浏览器隐式提交 → `saveSettings` →
    POST `/settings`。用户填完路径顺手一回车，看到「没有要改的」**而库没建**。

    跟前一条（嵌套 form）是**同一个故障形状的两条路**，所以两条都要钉。
    ⚠️ 同一条能力边界：这条只能证明我们**写出了** `onkeydown`，
    证明不了浏览器真的执行了它。
    """
    html = client.get("/settings").text
    assert 'onkeydown="if (event.key === \'Enter\')' in html


def test_settings_shows_the_card_after_init(initialized_vault, env, tmp_path):
    """已初始化：状态 + 路径 + 三个动作，初始化那条龙收起来。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    html = c.get("/settings").text
    assert "已初始化" in html
    assert str(initialized_vault) in html
    assert "迁移到别处" in html
    assert "移除知识库" in html
    assert 'id="wizard"' not in html


def test_settings_lists_the_domains_from_disk(initialized_vault, env, tmp_path):
    """领域从**磁盘**读，不从配置读——建个文件夹就该在界面上出现。"""
    (initialized_vault / "新领域").mkdir()
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    html = c.get("/settings").text
    assert "计算机" in html
    assert "健康" in html
    assert "新领域" in html              # 人工建的也得认
```

**最后一条尤其值得留着**：它钉的是「领域的唯一真源是磁盘」这条设计（Q95）。哪天有人图省事改成从 `.env` 或某个常量读，这条会红。

- [ ] **Step 7b: 跑它**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py -v
```

预期：全绿。若 `test_settings_shows_the_wizard_before_init` 报 `assert '已初始化' not in html` 失败，多半是别处的文案里恰好含这三个字——把断言收紧成 `'class="vault-card"' not in html`（那张已初始化卡的判据）。

- [ ] **Step 8: 起服务，亲眼看一眼**

```bash
PYTHONIOENCODING=utf-8 ./kb.bat web
```

看三件事：
1. 设置窗「知识库」那一组现在画的是**一条龙三段**（不是那个只读的路径框）；
2. 「检查」按钮点了有反应，填一个不存在的路径会说「初始化时会建出来」；
3. 勾选框点了整格变色。

- [ ] **Step 9: 跑测试 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
git -C "E:/Study_Projects/KN_Base_t6" add src/kb/web/templates/_vault_pane.html src/kb/web/templates/_settings.html src/kb/web/templates/base.html src/kb/web/static/style.css src/kb/web/router.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base_t6" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t6.txt"
```

```
feat: 设置窗「知识库」分组换成一屏两态——未初始化是一条龙

未初始化：三段一屏（模型配置 / 知识库目录 / 初始领域），底部一个
「初始化」收尾。已初始化：状态 + 路径 + 领域 + 三个动作（迁移 / 打开目录 /
移除），初始化按钮消失。

几个刻意的选择：

- **一条龙不是 `<form>`，是个 `<div>`。** 它整块住在 `#settings-form` 里面
  （`_settings.html` 第 21 行开到 70 行），自己再写一层 `<form>` 是非法嵌套——
  `form` 的内容模型排除 `form` 后代，解析器碰到表单指针非 null 时的 `<form>`
  开始标签直接忽略，**一声不响**。叠加上面那些 `name` 还会跟外层通用字段
  循环撞车（`Object.fromEntries(new FormData(form))` 只取到最后一个）。
  现在的做法是不给 `name`（沿用本文件「只读字段不给 name」那条约定），
  `runInit` 按 id 逐个收
- **也不共用外层那个 form。** `#settings-form` 走 `saveSettings()`，按 data-init
  过滤掉没动过的字段——那是为「改配置」设计的，而一条龙每次都要把三段全交
  上去（不然模型名会被当成「没改」漏掉）。两条路各走各的
- ② 就是个文本框，用户自己填绝对路径。不做目录选择器——路径是他自己电脑上
  的，他打得出来。旁边的「检查」顺手验一下，失败也不拦着初始化
- 领域清单来自 DOMAIN_CANDIDATES 常量，不进 .env。勾选的作用只是建出目录，
  建完就不再被读第二次；要加领域 = 建文件夹
- 密钥不预填（掩码都不给），留空 = 不改是它的语义
- 初始化成功后**整个设置窗重载一遍**：两态形状差得远，重载比局部刷新简单

新样式全走变量层，色值一个都没写死——六套皮肤靠 skins.py 推出来的变量换色。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 7: 首屏引导卡 + 移除确认模态

**界面基准：** `first-run.html` 左半张（引导卡）。

> ⚠️ **`wizard.html` 下面那张「移除之后再回来」的卡，本计划不实现——只取它的文案。**
> 那张 mockup 画的是「设置窗里一张卡 + 一颗『初始化知识库』按钮」，但设计稿
> 4.1 后来把这件事定成了**一屏两态**：未初始化态**就是**那条龙本身，首屏引导卡的
> 按钮直接 `showGroupByName('知识库')` 滚到它，**不是两套界面**（`design.md:102-110`）。
> 卡片 → 卡片 → 龙 是三层，跟 4.1 冲突，所以卡片被 4.1 取代了。
> `design.md:280` 那句「那张 mockup 里的**文案**就是最终文案」照办——
> 落点在 Task 6 那条龙的第①段（见那边 Step 3 的注），不在这里。
> **设计稿 6.4 第 3 条要一并改掉**（Task 8 的文档收尾负责），否则下一个人
> 翻设计稿照着做，又会长出那张被取代的卡。

**Files:**
- Modify: `src/kb/web/templates/chat.html`
- Modify: `src/kb/web/templates/base.html`
- Modify: `src/kb/web/static/style.css`
- Modify: `src/kb/web/router.py`（Step 1 给 `_ctx` 加 `vault_ready`）
- Test: `tests/web/test_setup.py`（Step 4b 追加，别新建文件）

---

- [ ] **Step 1: `_ctx` 给模板带一个「有没有库」**

`src/kb/web/router.py` 的 `_ctx` 返回值里加一个键。判据用
`lifecycle.vault_ready`（Task 5 Step 3 落在 `core/lifecycle.py` 里的那一份，
**全仓唯一**——`/setup/state`、`/setup/init` 的闸、Task 6 的 `settings_context`
都调它）：

```python
        # 首屏那张引导卡看它。也能顺便给侧栏那两处「未初始化就置灰」用
        # （Task 7 Step 2b）。
        "vault_ready": vault_ready(vault_path),
```

- [ ] **Step 2: `chat.html` 顶部加引导卡**

在 `{% block content %}` 里、现有内容**之前**插入：

```html
{% if not vault_ready %}
  {# 首屏引导卡。**按钮跳的是设置窗那条龙，不是另开一个向导**——
     用户的原话是「初始化后以后就显示已初始化，要改就去对应的设置改」，
     所以两处必须是同一屏。 #}
  <div class="first-run">
    <h2>还没有知识库</h2>
    <p>KN_Base 把笔记存成一个独立目录（vault）。先告诉它两件事，就能建起来。</p>
    <button type="button" class="primary" onclick="startInit()">初始化知识库</button>
  </div>
  <p class="hint first-run-hint">配好之前，投递和整理这几个入口用不了</p>
{% endif %}
```

- [ ] **Step 2b: 未初始化时把「投递 / 整理」两个入口置灰**

**这一步之前整份计划里没有**——`置灰` 全文只出现在 Task 2 的一句注释
（「不能只靠界面置灰」）和收尾自查那句打了 ✓ 的话里，**功能一行都没写**。

需求表第 1 条是用户看过 mockup 定的（`design.md:32`）：
「未初始化时首屏一张引导卡 + 「初始化知识库」按钮，**投递/整理入口置灰**」。
`vault_ready` 上一步已经进了 `_ctx`，现成可用。

⚠️ **这只是界面礼貌，拦人靠的是端点**（`design.md:317-319`）：`kb.bat push` 走 CLI、
接入指南里的会话层 AI 走 `/push`，界面置灰一个都拦不住。Task 2 的 `vault_guard`
才是那道门。**所以这里只碰模板和 CSS，不加任何新的拦截逻辑。**

改 `base.html` 侧栏两处。「记一条」（第 28 行）改成：

```html
      <a href="/new" class="side-action {{ 'active' if active == 'new' }}{{ ' is-disabled' if not vault_ready }}"
         {% if not vault_ready %}aria-disabled="true" title="还没有知识库，先初始化"
         onclick="return false"{% endif %}>
```

「巡检」（第 68 行）同理，在 `class="side-btn ..."` 后面加
`{{ ' is-disabled' if not vault_ready }}`，后面再加同一组
`{% if not vault_ready %}aria-disabled="true" title="还没有知识库，先初始化" onclick="return false"{% endif %}`。

**为什么是这两个**：`/new` 的 POST 是投递，没库干不了活（`router.py:298`，函数体
第一句就是 `vault()`）；`/sweep` 动的是库里的标签和目录。其余四项——ai对话 /
整理日志 / 工作日志 / 运行日志——未初始化时照样有用，**尤其 ai对话：首屏那张引导卡
就住在它上面**，置灰了卡片就没地方落。`tests/api/test_busy.py` 的
`test_pages_still_open_before_init` 钉着 `/`、`/journal`、`/settings`、`/new`
四页都得回 200，正是这个意思。

**用 `onclick="return false"` 而不是 CSS 的 `pointer-events: none`**：
后者一旦生效，元素就不再接收鼠标事件，`title` 那句「为什么点不动」
**永远悬不出来**——置灰了却不告诉人为什么，等于没说。

CSS 追加到 `style.css` 末尾（生成区之后，安全的——理由见 Task 6 Step 6 那条注）：

```css
/* 未初始化时置灰的入口。**只是界面礼貌**——真拦截在端点（`vault_guard`），
   因为 CLI 和接入指南里的会话层 AI 都绕过界面（`design.md:317-319`）。 */
.side-btn.is-disabled,
.side-action.is-disabled {
  opacity: .45;
  cursor: not-allowed;
}
/* 悬停不给「可点」的反馈，不然置灰是骗人的 */
.side-btn.is-disabled:hover,
.side-action.is-disabled:hover {
  background: none;
  color: inherit;
  text-decoration: none;
}
.side-btn.is-disabled:hover .icon,
.side-action.is-disabled:hover .icon { color: var(--faint); }
```

- [ ] **Step 2c: 给置灰钉两条测试**

追加到 `tests/web/test_setup.py`：

```python
def test_sidebar_greys_out_the_push_entries_before_init(client):
    """需求表第 1 条：未初始化时投递/整理入口置灰。

    **只是界面礼貌**——CLI 和接入指南都绕开界面，真拦截在 Task 2 的
    `vault_guard`（`tests/api/test_busy.py` 那边钉着）。
    这里只钉模板真的渲染出了那个状态。
    """
    html = client.get("/").text
    assert html.count('is-disabled') == 2          # 记一条 + 巡检，一个不多
    assert 'aria-disabled="true"' in html


def test_sidebar_is_not_greyed_after_init(initialized_vault, env, tmp_path):
    """建好之后再进来，两个入口得能点。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    assert "is-disabled" not in c.get("/").text
```

⚠️ `count == 2` 而不是 `>= 2`：**置灰错的面板比不置灰更糟**——
把 ai对话 或日志那几页也灰掉，首屏那张引导卡就没了落脚处
（`test_pages_still_open_before_init` 会红）。这条数就是在防「顺手全灰了」。

- [ ] **Step 3: `base.html` 加 `startInit()` 和移除确认模态**

**(a)** 在 `showGroupByName` 之后加：

```javascript
    async function startInit() {
      await openSettings();
      showGroupByName('知识库');
    }
```

**(b)** 在 `quit-notice` 那个 overlay 之后加移除确认模态：

```html
  {# 移除确认。**不用浏览器原生 confirm()**（`quitService` 那个做法）：
     原生 confirm 不能放清单、不能放勾选框，而且跟其余界面的样式是两套。
     用的是 `quit-notice` 那套 `.modal.notice`。 #}
  <div class="overlay" id="remove-overlay" hidden>
    <div class="modal notice">
      <h2>移除知识库</h2>
      <p id="remove-counts" class="remove-counts"></p>
      <p class="hint">这一步不能撤销，也不进回收站。</p>
      <label class="remove-confirm">
        <input type="checkbox" id="remove-ok" onchange="document.getElementById('remove-go').disabled = !this.checked">
        <span>我知道，删了找不回来</span>
      </label>
      <div class="modal-actions">
        <button type="button" class="secondary"
                onclick="document.getElementById('remove-overlay').hidden = true">取消</button>
        {# 同样 `secondary danger` 两个 class——见设置窗那张卡上的注。 #}
        <button type="button" class="secondary danger" id="remove-go" disabled
                onclick="confirmRemove()">移除</button>
      </div>
    </div>
  </div>
```

**(c)** 加三个函数：

```javascript
    async function openRemove() {
      const overlay = document.getElementById('remove-overlay');
      const counts = document.getElementById('remove-counts');
      document.getElementById('remove-ok').checked = false;
      document.getElementById('remove-go').disabled = true;
      counts.textContent = '正在数…';
      overlay.hidden = false;
      try {
        const d = await (await fetch('/setup/state')).json();
        // **先给数量、再给警告。** 数量让人看一眼就知道删的是不是他以为的
        // 那个库——一句「确定要删除吗」在删 3 篇和删 3000 篇时长得一模一样。
        counts.textContent =
          d.counts.notes + ' 篇笔记 · ' + d.counts.drafts + ' 条草稿 · ' +
          d.counts.chats + ' 段会话 · 全部整理日志';
      } catch (e) {
        counts.textContent = '数不出来（' + e + '），但删的东西是一样的';
      }
    }

    async function confirmRemove() {
      const btn = document.getElementById('remove-go');
      btn.disabled = true;
      try {
        const r = await fetch('/setup/remove', {method: 'POST'});
        const data = await r.json().catch(function () { return {}; });
        if (!r.ok) {
          alert('移除失败：' + _detail(data, '未知原因') + '（HTTP ' + r.status + '）');
          return;
        }
        // **删不干净的地方要如实说。** Windows 上正被服务打开的日志文件
        // 删不掉——那是操作系统的边界，不是设计妥协。不说这一句，用户下次
        // 在 data/logs/ 里看见文件会以为移除没生效。
        const notes = data.notes || [];
        if (notes.length) alert('移除完成，但有几句要说明：\n\n' + notes.join('\n'));
        location.reload();
      } catch (e) {
        alert('移除失败：' + e);
      } finally {
        btn.disabled = false;
      }
    }

    async function openVaultFolder() {
      // 服务是本地进程，浏览器打不开资源管理器——**如实说**，别做一个
      // 点了没反应的按钮。路径本身在设置窗里就显示着，可以复制。
      alert('在资源管理器里打开这个路径即可（路径就在上面，可以复制）。');
    }
```

**⚠️ `openVaultFolder` 这个空壳是诚实的降级，不是偷懒。** 浏览器沙箱里没法调 `explorer.exe`；要做就得后端加一个端点去 `subprocess.Popen(['explorer', path])`——那是**在用户机器上执行命令**，得单独讨论。本计划**不做**，只留一个说清楚的话术。若用户回头要真打开，另开任务。

**(d)** 加 `openMigrate()`：

```javascript
    async function openMigrate() {
      const target = prompt('搬到哪里？填一个绝对路径，而且那个目录必须是空的（不存在也行）。');
      if (!target) return;
      try {
        const r = await fetch('/setup/migrate', {
          method: 'POST',
          headers: {'Content-Type': 'application/json'},
          body: JSON.stringify({target: target}),
        });
        const data = await r.json().catch(function () { return {}; });
        if (!r.ok) {
          alert('没搬成：' + _detail(data, '未知原因') + '（HTTP ' + r.status + '）');
          return;
        }
        alert('搬好了：' + data.moved.files + ' 个文件 / ' +
              Math.round(data.moved.bytes / 1024) + ' KB。新位置：' + data.vault_path);
        location.reload();
      } catch (e) {
        alert('没搬成：' + e);
      }
    }
```

- [ ] **Step 4: `style.css` 加引导卡与模态的样式**

```css
/* ---- 首屏引导卡（未初始化时） ---- */

.first-run {
  border: 1px solid var(--accent);
  background: var(--accent-soft);
  border-radius: 8px;
  padding: 16px 18px;
  margin-bottom: 14px;
}
.first-run h2 {
  color: var(--ink); font-size: 13px; font-weight: 600;
  margin: 0 0 8px;
}
.first-run p {
  color: var(--ink); font-size: 11px; line-height: 1.6;
  margin: 0 0 14px;
}
/* 这行 `p` 在 `chat.html` 里，**不在 `.settings-panes` 也不在 `.modal.notice`
   里面**——上面那两条 `.hint` 规则一条都命中不到它。只给 `margin-top` 的话，
   颜色和字号会落到浏览器默认值（黑字 16px），跟页面其它小字不是一套。
   所以这里给全，写法照 `.settings-panes .hint` 那条。 */
.first-run-hint { margin-top: 10px; color: var(--faint); font-size: 11.5px; }

/* ---- 移除确认 ---- */

.remove-counts {
  color: var(--ink); font-size: 12px; font-weight: 600;
  margin: 0 0 6px;
}
.remove-confirm {
  display: flex; align-items: center; gap: 8px;
  font-size: 11px; cursor: pointer;
  margin: 14px 0;
}
.remove-confirm input { margin: 0; accent-color: var(--danger); }
.modal-actions { display: flex; gap: 8px; justify-content: flex-end; }
```

- [ ] **Step 4b: 追加两条页面断言测试**

追加到 `tests/web/test_setup.py` 末尾（**别新建文件**——夹具都在那边）：

```python
# ---------- 首屏引导卡 ----------

def test_chat_shows_the_first_run_card_before_init(client):
    """未初始化时首屏那张卡在，按钮跳的是设置窗那条龙。

    **不是另开一个向导**——用户的原话是「初始化后以后就显示已初始化，
    要改就去对应的设置改」，所以两处必须是同一屏。`startInit()` 的
    存在就是在钉这一条：它做的是 `openSettings()` + `showGroupByName('知识库')`。
    """
    html = client.get("/").text                      # chat.html 挂在 `/`
    assert 'class="first-run"' in html
    assert "还没有知识库" in html
    assert "初始化知识库" in html
    assert "startInit()" in html


def test_chat_hides_the_first_run_card_after_init(initialized_vault, env,
                                                  tmp_path):
    """建好之后卡片收起来——**留着的话首屏永远在说「还没有知识库」**。"""
    data = tmp_path / "data"
    data.mkdir(exist_ok=True)
    cfg = load_config(env)
    c = TestClient(create_app(cfg, llm=FakeLLM([]), data_dir=data, env_file=env))

    html = c.get("/").text
    assert 'class="first-run"' not in html


def test_the_remove_modal_is_not_a_native_confirm(client):
    """移除确认是个真模态，**不是浏览器原生 `confirm()`**。

    `quitService` 那个原生 confirm 放不下清单和勾选框，样式跟其余界面
    也是两套。这条钉三件事：用的是 `.modal.notice` 那套、清单容器在、
    「移除」**默认禁用**（勾选框没勾之前点不动）。
    """
    # ⚠️ **取 `/`，不是 `/settings`。** 移除模态加在 `base.html` 里，
    # 而 `/settings` 返回的是 `_settings.html` 那个**片段**（`router.py:430`），
    # 没有 `{% extends %}`，整页骨架一个都不带。
    # 现成有一条测试专门钉这件事：`tests/web/test_router.py::test_settings_fragment_has_no_full_page`。
    html = client.get("/").text
    assert 'id="remove-overlay"' in html
    assert 'id="remove-counts"' in html              # 「先给数量、再给警告」
    assert 'id="remove-ok"' in html                  # 原生 confirm 放不下勾选框
    assert 'id="remove-go" disabled' in html         # 没勾之前点不动

    # `class="modal notice"` 单断言是**恒真**的：`base.html:160` 那个
    # 「服务已停」通知本来就带这对 class，哪页都命中。必须限定在移除模态
    # 自己那块里，否则这条断言永远不会红，也就永远没在测东西。
    pos = html.index('id="remove-overlay"')
    assert 'class="modal notice"' in html[pos:]      # 跟「服务已停」同一套
```

- [ ] **Step 4c: 跑它**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/web/test_setup.py -v
```

预期：全绿。（这一条**跑不了**就说明 Step 1 的 `vault_ready` 没接上——
`_ctx` 里没这个键时 Jinja 的 `{% if not vault_ready %}` 会当成 undefined，
`not undefined` 是 true，卡片**永远显示**，`test_chat_hides_...` 当场红。）

- [ ] **Step 5: 起服务，走一遍完整流程**

先**备份 vault**（这一轮会真删东西）：

```bash
cp -r "E:/KB_Library" "C:/Users/wuyeu/AppData/Local/Temp/kb_library_snapshot_2026-09-22b"
```

然后：

```bash
PYTHONIOENCODING=utf-8 ./kb.bat stop && PYTHONIOENCODING=utf-8 ./kb.bat web
```

走一遍：**清空 `.env` 的 `KB_VAULT_PATH` → 重启 → 看到引导卡 → 点按钮跳到设置那条龙 → 填一个临时路径 → 勾两个领域 → 初始化 → 变成已初始化态 → 投一条 → 整理 → 迁移到另一个临时路径 → 确认库跟过去了 → 移除 → 确认清单数量对得上 → 确认 `data/runtime/service.json` 还在**。

- [ ] **Step 6: 跑测试 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
git -C "E:/Study_Projects/KN_Base_t7" add src/kb/web/templates/chat.html src/kb/web/templates/base.html src/kb/web/static/style.css src/kb/web/router.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base_t7" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t7.txt"
```

```
feat: 首屏引导卡 + 移除确认模态

引导卡上的按钮跳的就是设置窗那条龙（startInit = openSettings +
showGroupByName('知识库')）——**不是两套界面**，用户的原话是「初始化后
以后就显示已初始化，要改就去对应的设置改」。

移除确认用 .modal.notice（跟「服务已停」那个通知同一套），不用浏览器
原生 confirm：原生 confirm 不能放清单、不能放勾选框，样式也是两套。

**先给数量、再给警告**：清单写着「34 篇笔记 · 2 条草稿 · 7 段会话」，
数量让人看一眼就知道删的是不是他以为的那个库——一句「确定要删除吗」
在删 3 篇和删 3000 篇时长得一模一样。

「打开目录」只给了句提示，没做真打开：浏览器沙箱里调不了 explorer.exe，
要做就得后端加个端点去 Popen——那是在用户机器上执行命令，得单独讨论。
留个点了有反应的诚实降级，不做点了没反应的假按钮。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 8: 文档收尾

**Files:**
- Modify: `docs/02_需求.md`（第 118 行那条需求）
- Modify: `docs/03_问题记录.md`（新增 Q）
- Modify: `docs/04_踩坑与经验.md`（新增一条）
- Modify: `docs/01_架构.md`（第十四节——**先核实**「真缺口」里有没有相关条目，没有就不动）
- Modify: `README.md`（第一次用那一节 + 配置表）
- Modify: `.env.example`

---

- [ ] **Step 1: `docs/02_需求.md:118`——把「vault 路径只读」改掉**

**先看清楚那是哪一行：它不是表格行，是第 117 行那条列表项的续行。** 原文是：

```
  （它们只在服务启动那一刻用一次）。端口与 vault 路径**只读**（换错了整个服务当场废、改了书签作废）
```

它属于 `### 2026-09-17 补充：设置与退出` 下面那条「保存即生效」。那一整条改成：

```
- **保存即生效**——模型三件套与日志级别当场换。**巡检间隔与日志保留要下次启动服务**
  （它们只在服务启动那一刻用一次）。**端口仍然只读**（改了地址就变，书签作废）；
  **vault 路径可改**（2026-09-22 放开）。改错了整个服务当场废——**所以约束从「不给改」
  换成了「改的时候必须是个已初始化的库」**：目标没有 `.git` 就 400，空值也 400（解绑
  要走「移除知识库」）。要搬家用「迁移到别处」，那是显式动作，会拷完校验再删源
```

**「换错了整个服务当场废」这句必须留着**，否则下次有人翻到这一行会以为我们忘了当初的教训。

- [ ] **Step 2: `docs/03_问题记录.md`——按那个文件的格式加一条 Q**

编号接在现有最大号后面。**先看一眼当前最大号**——那个文件的 Q 标题是**三个 `#`、
数字后面带点号**（`### Q107. …`），`^## Q` 这种写法**一条都匹配不到**：

```bash
grep -n '^### Q' docs/03_问题记录.md | tail -3
```

现在最大号是 `Q107`（`### Q107. 日志页的「一条一张卡片」，一条是指什么？`），所以新的是
`Q108`。**标题照现有格式写**，别少写一个 `#`、也别漏了数字后面那个点。内容：

```markdown
### Q108. 知识库的生命周期：初始化 / 迁移 / 移除

**问题**（用户的原话）：如果给别人使用，那有几个问题——知识库路径要能填；
刚开始使用时会自动生成领域级的文件夹吗；初始化按钮，需填好模型配置、知识库路径，
然后初始化知识库文件夹；不想要知识库之后怎么处理。后续补：不想要的话就是全部删除，
一点痕迹都不留的那种，另外知识库目录可迁移也实现一下；领域名让用户选，在设置窗口里
加一个初始化按钮，直接一条龙。

**结论**：设计文档在 `docs/superpowers/specs/2026-09-22-知识库生命周期-design.md`。
几条值得单独记的：

- **`config.py` 的 `DEFAULT_VAULT_PATH` 是颗雷，已拆。** `KB_VAULT_PATH` 留空会
  回落到 `Path(r"E:\KB_Library")`——作者本机的路径。别人的服务会安安静静写进
  作者的库，而且看起来一切正常（`list_drafts` / `list_domains` 在目录不存在时
  都返回 `[]`）。**「未初始化」这个状态要能被表示，跟这个回落直接冲突**，
  所以空值现在表示「没有」（`Path | None`），不是「用默认的」。
- **「有没有库」不存标志位**，由磁盘 + `.env` 推出来：`cfg.vault_path is None`
  或路径下没有 `.git` = 未初始化。判据用 `.git` 而不是「目录存在」——用后者的话，
  用户填一个现成的空文件夹就会被当成已初始化，投递进去后 `atomic_write` 自己
  `mkdir(parents=True)`，**在一个不是库的地方长出半个库**：收件箱有草稿、领域是空的、
  每条都掉进待归类。
- **领域清单不进 `.env`，设置里也没有这一栏。** 领域的唯一真源始终是磁盘
  （`list_domains` 直接读目录），初始化时勾选的作用只是**建出目录**，建完就不再
  被读第二次。要加领域 = 建文件夹——这也正是「名称不能随便起」能成立的原因
  （勾选保证起点干净，人工建目录留着当后路）。
- **`init_vault` 从 `scripts/` 搬到 `core/vault_setup.py`。** 服务要调它，而 `src/`
  与 `scripts/` 之间没有桥（`pythonpath` 只有 `src`）。服务进程「恰好」能 import
  是**巧合不是设计**——`spawn_service` 用 `-m kb.api.http` 且 `cwd=PROJECT_ROOT`，
  `python -m` 把 cwd 放进了 `sys.path[0]`，换个人从别的 cwd 起就断。
- **迁移拒绝非空目标，半路迁移必须挡。** 后者失败的样子是「看起来一切正常」：
  一轮整理会多次读库，迁移插中间就把它劈成两半，而最后那个 `commit_changes`
  只在新库里提交——**旧库那半永远不进 git 却躺在磁盘上**。挡住它的机制就是个
  带锁的 `Busy`，很便宜。
- **「一点痕迹都不留」物理上做不到**：`data/logs/` 今天那两个文件正被服务自己
  打开着，Windows 上删不掉。界面上如实说明，不假装清干净了。
- **`data/runtime/service.json` 绝不能删**——那是 `kb stop` 唯一的线索，删了就是
  「停不掉的孤儿进程」，而那个 bug 的症状是测试全绿、开发机上服务失控。**有两条测试
  真咬着这条**：`tests/core/test_lifecycle.py::test_remove_leaves_the_rest_of_data_alone`
  与 `tests/web/test_setup.py::test_remove_keeps_service_json`。它们不止断言
  `service.json` 还在，还各造了一个无关文件一起验——因为 `remove_vault` 压根不碰
  `data/runtime`，只断言 `service.json` 的话是**恒真**的，守不住任何东西。
  删除清单只有 `data/chats`、`data/logs`、`data/state.json` 三项。

**同时改了一条需求**：`02_需求.md` 里「vault 路径只读」那条放开了，理由写在那条
需求里（靠的是「目标必须是已初始化的库」这条机械校验 + 迁移这个显式动作）。
```

- [ ] **Step 3: `docs/04_踩坑与经验.md`——加一条**

**编号写 `35`，别按「接在末尾顺延」推。** 那个文件的编号**在文内不单调**：
最后一节是 `### 32.`（624 行），而 `### 33.` / `### 34.` 在更前面（381 / 450 行）——
照「末尾 +1」会推出 33 这个**已经被占用的错号**。按当前最大号 `34` 顺延才是 35：

```markdown
### 35. 「静默回落到一个写死的路径」和「文档里写着的能力」是同一类

`config.py` 的 `DEFAULT_VAULT_PATH = Path(r"E:\KB_Library")` 配上
`vault_path=Path(vault_raw) if vault_raw else DEFAULT_VAULT_PATH`：**`.env` 里留空，
服务就写进作者本机的库。**

它的可怕之处不在于错，在于**错得没有声音**：

- 服务起得来，不报错
- 每个端点都返回 200
- `list_drafts` / `list_domains` 在目录不存在时都静默返回 `[]`，所以界面上
  就是「空库」，跟「新库」长得一模一样
- 作者的机器上一切正常——**只有别人那儿是坏的**

这跟第 30 条「文档里写着的能力」是同一种病：**代码里有两条路，一条对一条错，
而错的那条在开发机上永远走不到。** 判据也很像——「在我这儿好好的」不构成证据，
得问「一个没有 `E:\KB_Library` 的人跑起来会怎样」。

**落地的教训**：默认值不是中立的。一个指向具体机器路径的「默认值」，等于
把开发机的环境假设焊进了产品。**要表示「没有」，就得有一个真正的「没有」**——
现在 `vault_path` 是 `Path | None`，空就是 `None`，`require_vault` 会当场抛。
```

- [ ] **Step 4: `README.md`——重写「第一次用」那一节**

把第 76-116 行那节（「第一次用：两件事必须先定」）整节换成：

```markdown
### 第一次用：开网页，点「初始化知识库」

```bash
cp .env.example .env        # Windows cmd: copy .env.example .env
```

`.env` 里**只需要填模型的三件套**：

```
KB_LLM_API_KEY=sk-...
KB_LLM_BASE_URL=https://api.deepseek.com
KB_LLM_MODEL=deepseek-flash
```

后两项 `.env.example` 里已经给了能用的值，换供应商才需要改。空着不会静默降级——
**除了 `kb stop`，每条 `kb` 命令都会当场停下**并说清缺哪一项。

然后双击 **`web.bat`**。**知识库还没建的时候，首屏会有一张引导卡**，点它上面的
「初始化知识库」会打开设置窗里那条龙，一屏三段填完：

| | |
|---|---|
| **① 模型配置** | 模型名 / API 地址 / API Key。旁边有「测试连接」，先测再存 |
| **② 知识库目录** | 填一个绝对路径。**目录不存在没关系，初始化时会建**。旁边的「检查」顺手验一下 |
| **③ 初始领域** | 九个候选里勾，**至少勾一个**。勾中的会建成一级目录 |

点「初始化」就齐了。它会建目录、写 `.gitignore`、把那个目录初始化成 git 仓库。
**幂等**——在一个已经有东西的目录上点它也不会毁掉什么。

之后那块面板就变成「已初始化」状态，上面挂着三个动作：**迁移到别处 / 打开目录 /
移除知识库**。

> **要加领域，不用改配置**：在资源管理器里给库建个一级文件夹就行，服务下次就认。
> 领域的唯一真源始终是磁盘。

<details>
<summary>不想用网页，走命令行也行</summary>

填好 `.env` 里的 `KB_VAULT_PATH`（**它没有默认值了**——不填就是「还没有知识库」，
每条命令都会停下告诉你去初始化），然后：

```bash
python scripts/init_vault.py <你的路径>
```

那个脚本是 vault 结构的唯一真源（实现住在 `src/kb/core/vault_setup.py`）——
想调目录就改它重跑，别手工建。**注意它只建目录，不写 `.env`**：配置一直是服务的活。
</details>

> vault 是**另一个 git 仓库，刻意不配远端**：里头是你的私人笔记，别顺手推到 GitHub。
> （本仓库的 `data/` 同理，整棵不进 git。）
```

⚠️ **上面最后那个 blockquote 是从原文第 113-114 行**原样**搬过来的，一个字都别改。**
它是这一节里唯一一处讲「别把私人笔记推上去」的地方——2026-09-15 真发生过一次误推，
已撤回。整节替换的 diff 很容易顺手把它带走，带走之后没人会注意到，而代价是把私人
笔记公开发布。搬进新节里，位置在结尾即可。

- [ ] **Step 5: `README.md` 配置表那一行**

第 179 行：

```
| `KB_VAULT_PATH` | | vault 路径，默认 `E:\KB_Library`（**作者本机的路径**，自己用得改成自己的，或照上面那把骨架建出来） |
```

改成：

```
| `KB_VAULT_PATH` | | vault 路径。**没有默认值**——留空就是「还没有知识库」，网页上会显示成未初始化。正常不用手填，点「初始化知识库」时它会自己写进来 |
```

同表下面那句「**只读** | vault 路径、端口」（第 192 行）也要改：

```
| **只读** | 端口。改了地址就变，收藏的网页地址就废了 |
```

**还有一处，跟刚改完的第 189 行直接打架**：第 182 行（配置表**正上方**那句）
现在写着「**第一次用先看上面的「第一次用」一节**——key 和 vault 目录是必填的」。
新节里 vault 目录已经不是手填项了，这句话和 189 行**隔七行自相矛盾**。改成：

```
配置放工程根目录的 `.env`（**不入库**），照 `.env.example` 填一份。**第一次用先看上面的「第一次用」一节**——模型的 key 是必填的；vault 目录不用手工填，点「初始化知识库」时会自己写进来。
```

（换节时容易只盯着被替换的那 40 行，忘了这节之外还有句子在描述同一件事。
**改文档要顺带搜一遍同一个说法还在哪儿出现过。**）

**「当场生效」那一行不用动。** `README.md:190` 现在逐字就是
`| **当场生效** | 模型三件套、日志级别、界面皮肤 |`——vault 路径本来也不该进这一栏
（它有自己的按钮组，那三个动作不是「一个字段」）。

（**别为了「有改动」去动它**：改完顺手扫一眼，确认这行没被碰坏就够了。）

- [ ] **Step 6: `.env.example`**

**第 6、7 两行一起换掉**（第 6 行是那条错的注释「留空则用默认值 E:\KB_Library」，第 7 行是值 `KB_VAULT_PATH=E:\KB_Library`——**两行都得动，只删注释的话那行还留着作者本机的路径**）：

```
# 知识库目录。**没有默认值**——留空就是「还没有知识库」，服务会拒绝投递并
# 引导你去网页上初始化。正常不用手填：点「初始化知识库」时它会自己写进来。
KB_VAULT_PATH=
```

- [ ] **Step 7: `docs/01_架构.md` 第十四节——**先核实，多半是个空操作**

先打开那一节看一眼。**表名叫「真缺口」，不叫「还缺什么」**——「还缺什么」只是第 7 行
导语里的说法，正文里没有这个名字。那一节（`十四、尚未定 / 未设计`）的结构是：

- `### 真缺口`（**第 672 行**）——现在只有三行：①流程链上「跳过」和「没走到」分不清
  ②可移植打包 ③Codex / 通用版的接入说明。**没有一条跟知识库路径可配 / 初始化 /
  迁移 / 移除有关。**
- 往下是**加粗正文** `**已经定过的**（不再列为缺口）：`（**第 680 行**）——
  ⚠️ **它不是标题**，别去找 `###`，找不到的。再往下 `### 明确不做（用户说先不管）`
  才是标题（第 687 行）

所以这一步多半**无事可做**。按这个规矩走：

- 那一节里**确实有**跟「知识库路径可配 / 初始化 / 迁移 / 移除」相关的条目 → 删掉
  （**作废的条目直接删，不留存根**——这是本项目对文档的规矩）
- **没有就跳过。** 别硬凑一条进去，也别为了「有东西可删」去动无关的行

（`01_架构.md` 只写「是什么」，本次这条不产生新的架构表述，正文其余部分一律不动。）

- [ ] **Step 8: 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
git -C "E:/Study_Projects/KN_Base_t8" add docs README.md .env.example
git -C "E:/Study_Projects/KN_Base_t8" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t8.txt"
```

```
docs: 知识库生命周期的文档收尾

- 02_需求.md：vault 路径从「只读」改成「可改」，**理由一起写进去**——
  当初设只读是本机被改废过的教训，现在敢放开靠的是「目标必须是已初始化的库」
  这条机械校验 + 迁移这个显式动作。不写清楚，下次翻到那行会以为我们忘了
- 03_问题记录.md：新增 Q108，记四个决定 + DEFAULT_VAULT_PATH 这颗雷 +
  搬 init_vault 的理由 + 「一点痕迹都不留」为什么物理上做不到
- 04_踩坑与经验.md：新增一条——「静默回落到一个写死的路径」和「文档里写着
  的能力」是同一类病：代码里有两条路，一条对一条错，而错的那条在开发机上
  永远走不到
- README：第一次用那一节重写，手工填 .env 降级成 <details> 里的备选；
  配置表里 KB_VAULT_PATH 不再是「默认 E:\KB_Library」
- .env.example：「留空则用默认值」那句是错的，删掉

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## Task 9: 写死的路径收尾——问题记录与接入指南

**为什么单独开一条**：Task 8 改的是「给人读的文档」，这条改的是**另外两处**——设计状态的唯一真源（`03_问题记录.md`）和给会话层 AI 读的指令（`SKILL.md`）。它们都还把 `E:\KB_Library` 当成既定事实，而本次改动正好推翻了这一点。

**Files:**
- Modify: `docs/03_问题记录.md`（第 241 行，Q32 那张「配置项（初稿）」表）
- Modify: `docs/接入指南/kn-record/SKILL.md`（**源**，三处）
- Modify: `README.md`（第 5 行）
- **不在本仓库**：`C:/Users/wuyeu/.claude/skills/kn-record/SKILL.md`（装好的那份，Step 3 同步）

---

- [ ] **Step 1: `docs/03_问题记录.md` 第 241 行——那张表还在说「有默认值」**

Q32（`.env` 放哪儿）里那张「**配置项（初稿）**」表，第 241 行逐字是：

```
| `KB_VAULT_PATH` | 否 | vault 路径，默认 `E:\KB_Library` |
```

改成：

```
| `KB_VAULT_PATH` | 否 | vault 路径。**没有默认值**（2026-09-22 拆掉了回落到 `E:\KB_Library` 那颗雷，见 Q108）——留空就是「还没有知识库」，网页上点「初始化知识库」时自己写进来 |
```

**只动这一格，别顺手重排整张表。** 那三条漏掉的键（`KB_LOG_LEVEL` / `KB_SWEEP_INTERVAL` / `KB_LOG_KEEP_DAYS`）和 `KB_SKIN` 是后来才加的，补进「初稿」表反而是伪造历史。

**但要在表下面加一句**，说清这张表的地位——它是 2026-09-15 的初稿，**当前生效的清单在 `.env.example`**：

```
（**这张表是 2026-09-15 的初稿**，后面陆续加的键没往这儿补。
**当前生效的清单看 `.env.example`**，人话版看 `README.md` 那张配置表。）
```

理由：那一条自己第 231 行就写着「`.env.example` 必须入库，列出全部配置项」——真源本来就在那儿，两张表并存只会漂移（`04_踩坑与经验.md` 第 24 条「两份约定会漂移」）。

- [ ] **Step 2: `docs/接入指南/kn-record/SKILL.md`——三处写死的 vault 路径**

**注意区分**：这个文件里还有十几处 `"E:/Study_Projects/KN_Base/kb.bat"`，**那些一律不动**。它们是刻意的（`docs/接入指南/README.md:49` 明说「指南里一律写完整路径」），而且「换个机器怎么装」已经作为真缺口记在 `01_架构.md` 第十四节。改它们会把这条真缺口提前糊上，反而看不出还缺什么。

**要改的只有三处**——它们不是在写命令，是在**指认哪个目录是库**：

| 行 | 原文 | 改成 |
|---|---|---|
| 169 | ``只走 `kb drop`，绝不手改 `E:\KB_Library` 里的文件`` | ``只走 `kb drop`，绝不手改**知识库（vault）**里的文件`` |
| 199 | ``也**不用**再去 Read `E:\KB_Library` 里的文件，结果里已经全了。`` | ``也**不用**再去 Read 知识库里的文件，结果里已经全了。`` |
| 214 | ``**别改 `E:\KB_Library` 里的任何文件。** 那是用户在 Obsidian 里的地盘。`` | ``**别改知识库（vault）里的任何文件。** 那是用户在 Obsidian 里的地盘。`` |

**改的是「说不说得出那个路径」，不是规矩本身**——三处的规矩都成立，只是路径一写死，库搬过家的人拿到的就是错的目录。

- [ ] **Step 3: 同步装好的那份**

源改了，装好的那份不会自己变：

```bash
bash -c 'cp "E:/Study_Projects/KN_Base/docs/接入指南/kn-record/SKILL.md" "C:/Users/wuyeu/.claude/skills/kn-record/SKILL.md"'
```

**用 `bash -c` 包一层**——本机直接 `cp` 会报 `AbortError: Stream closed`。拷完自己看一眼两份是不是一样（`diff` 一下，或比对 `E:\KB_Library` 在两个文件里的出现次数，源应该是 0 次）。

这份**不在仓库里**，不进任何提交，也不用回报到 diff 里——但**漏了它，本地跑起来的会话 AI 读到的还是旧指令**。

- [ ] **Step 4: `README.md` 第 5 行**

逐字是：

```
知识存在 `E:\KB_Library`（一个 Obsidian vault），代码在本仓库。两者物理分离，各自独立版本管理。
```

改成：

```
知识存在一个独立的 Obsidian vault 里——**放在哪由你定**，网页上点「初始化知识库」时告诉它（本文这套跑在 `E:\KB_Library`）。代码在本仓库。两者物理分离，各自独立版本管理。
```

**第 49 行那张 ASCII 图里的 `vault（E:\KB_Library）` 不动。** 它在固定宽度的框里，改字就要重排整个框，而第 5 行已经把「放在哪由你定」说清楚了。**这是刻意的，别以为是漏了。**

- [ ] **Step 5: 提交**

```bash
git -C "E:/Study_Projects/KN_Base_t8" add docs README.md
git -C "E:/Study_Projects/KN_Base_t8" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t9.txt"
```

```
docs: 写死的 vault 路径收尾——问题记录与接入指南

Task 8 改的是给人读的文档，这一批改另外两处：

`03_问题记录.md` 的 Q32 里那张「配置项（初稿）」表还写着
`KB_VAULT_PATH` 默认 `E:\KB_Library`——**这个文件是本项目设计状态的
唯一真源**，留着一句已被推翻的话比留着一段过时的 prose 严重。
改掉那一格，并在表下注明它的地位（2026-09-15 初稿，当前清单看
`.env.example`）。没有顺手补那几条后加的键：给「初稿」补内容是伪造历史。

`接入指南/kn-record/SKILL.md` 有三处把 `E:\KB_Library` 当成既定事实
写进给会话层 AI 的指令里——「别改 `E:\KB_Library` 里的文件」。规矩本身
没错，错在写死了路径：库搬过家的人，读到的是**错的目录**。改成不带
路径的说法。

**同一文件里那十几处 `"E:/Study_Projects/KN_Base/kb.bat"` 一律没动**：
它们是刻意写全的（`接入指南/README.md:49` 有说明），而「换机器怎么装」
已经作为真缺口记在 `01_架构.md` 第十四节——提前糊上会让人看不出还缺什么。

装的第二份（`~/.claude/skills/kn-record/SKILL.md`）已同步，那份不在仓库里。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

---

## 收尾自查（全部任务做完后）

- [ ] **全量测试 + ruff 干净**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
```

- [ ] **`git log --oneline main | head -10`** 看一遍这一串提交是不是按 Task 1→8 排的

- [ ] **真跑一遍完整链路**（Task 7 Step 5 那一串），确认九条定案逐条对得上：
  1. 未初始化时首屏有引导卡 + 投递/整理入口置灰 ✓
  2. 初始化是设置窗里的一条龙，一屏三段 ✓
  3. 领域从预设清单里勾，至少一个 ✓
  4. 初始化后面板显示「已初始化」+ 路径 + 领域 + 三个动作 ✓
  5. 迁移拒绝非空目标 ✓
  6. 正在整理时不给迁 ✓
  7. 移除 = 全部删除（**并如实说明今天那两个日志删不掉**）✓
  8. 移除后能重新初始化，模型配置保留 ✓
  9. 首屏按钮和设置里那条龙**是同一屏** ✓

- [ ] **skill 两份同步**（本任务没改 skill，但顺手确认一次没漂移）：

```bash
diff "E:/Study_Projects/KN_Base/docs/接入指南/kn-record/SKILL.md" \
     "C:/Users/wuyeu/.claude/skills/kn-record/SKILL.md"
```

- [ ] **`git status` 确认 `data/` 下没有任何东西被 stage**——仓库是 public 的，运行日志与会话历史进去就是公开发布。

- [ ] **不要 `git push`**，除非用户明确要求。vault 那个仓库（`E:\KB_Library`）**刻意没有远端**，别顺手给它配一个。

---

## Task 10: `/sweep` 也挂上 `Busy` 锁

**Files:**
- Modify: `src/kb/core/lifecycle.py`（`_BUSY_LABELS` 加一条；`Busy` 出一个文案的单一来源）
- Modify: `src/kb/api/http.py`（`busy_guard` 改用那个来源；`_sweep_in_background` 占锁；`main()` 传 `busy`）
- Modify: `src/kb/web/router.py`（`_run_sweep_or_report` 占锁）
- Test: `tests/api/test_busy.py`、`tests/web/test_setup.py`

---

**为什么补这一条**（Task 4 的复查里两个独立 agent 各发现一次；2026-09-22 用户拍板「也挡」）：

Task 4 给 `/organize`、`/push`、`/setup/migrate`、`/setup/remove` 都挂了
`busy_guard`，**唯独漏了巡检**。而 `run_sweep`（`api/http.py:271`）自己那行
docstring 写的就是「**它走的是和整理草稿同一条链**，只是输入换成了整个库的
标签与目录」——同样改 vault 里的笔记、同样落一个 git commit。所以
「迁移搬到一半，巡检插进来」和「整理插进来」是**同一个损坏形状**，
**失败的样子也同样是「看起来一切正常」**。

还有一处比它更实在：`_sweep_in_background` 是**服务进程里的一个 daemon 线程**，
和请求处理是**并发**的。它占着锁，用户在那个窗口里点「整理」才会被挡住；
不占的话，启动后那一轮巡检跑到一半就可能被一轮整理劈开。

- [ ] **Step 1: 先让 `Busy` 出一个文案的单一来源**

`busy_guard` 里那句 `f"正在{busy.label}，等它跑完再试"` 现在只有一处，但本任务
要在**两个地方**说同一句话（409 那条路，和巡检页那份报告）。再抄一遍就是
Task 4 刚合掉的那种重复。

`src/kb/core/lifecycle.py` 的 `_BUSY_LABELS` 加一条：

```python
_BUSY_LABELS = {
    "organize": "整理", "migrate": "迁移", "remove": "移除", "sweep": "巡检",
}
```

在 `Busy.label` 之后加：

```python
    @property
    def refusal(self) -> str:
        """拿不到锁时那句话。**只有这一份。**

        409 那条路（`busy_guard`）和巡检页那份报告都要说同一句话；两边各写
        一遍的话，改了「整理」忘了「巡检」——用户看到两种说法，还得自己
        猜是不是两回事。
        """
        return f"正在{self.label}，等它跑完再试"
```

- [ ] **Step 2: `busy_guard` 改用它**

`src/kb/api/http.py`，`organizing()` 里那三行：

```python
        if not busy.acquire(what):
            raise HTTPException(
                status_code=409, detail=f"正在{busy.label}，等它跑完再试"
            )
```

换成：

```python
        if not busy.acquire(what):
            raise HTTPException(status_code=409, detail=busy.refusal)
```

- [ ] **Step 3: 后台巡检占住锁**

`src/kb/api/http.py:308`，整个函数换成：

```python
def _sweep_in_background(
    vault_root: Path, data_dir: Path, llm: LLM, busy: Busy
) -> None:
    """后台跑巡检。**绝不抛异常**——后台线程里抛了没人接。

    `llm` 由 `main()` 用现成的 `cfg` 建好传进来，**这里不重新 `load_config()`**：
    `load_dotenv` 写的是进程级 `os.environ`，同一进程内只该调用一次
    （见 `config.load_config` 的 docstring），而 `main()` 已经调过了。

    **要占住 `busy`，从拿到锁一直占到跑完。** 这个线程和服务在同一个进程里，
    跟请求处理是**并发**的：它占着，用户在这个窗口里点「整理」才会被 409
    挡住；不占的话，一轮巡检跑到一半被一轮整理劈开，就是「一半旧库一半
    新库」那个形状——而它**看起来一切正常**（`run_sweep` 的 docstring
    自己写着「它走的是和整理草稿同一条链」）。
    """
    # 拿不到（启动那一瞬间恰好有请求在跑，可能性极小）就**连坑都不占**：
    # `mark_run` 是先用掉的「这次算跑过了」，占了就要再等一个
    # `KB_SWEEP_INTERVAL` 才会重试，等于白丢一次巡检。
    if not busy.acquire("sweep"):
        return
    try:
        # 先占坑：万一跑挂了也不会每次重启都重跑。
        # 用 mark_run 而不是 save_report——后者会留一份 read: false 的报告，
        # 侧栏会把它当正式报告显示（还带两个回复按钮）。
        sweep_state.mark_run(data_dir)
        try:
            run_sweep(vault_root, data_dir, llm)
        except Exception:      # noqa: BLE001
            logging.getLogger("kb.sweep").exception("巡检失败")
            sweep_state.save_report(data_dir, {"summary": "这次巡检没跑成，看运行日志"})
    finally:
        busy.release()
```

⚠️ **`mark_run` 挪进了 `try`**（原来在 `busy.acquire` 那行之前）。它留在锁外的话，
拿不到锁也会先把坑占了——而拿不到锁恰恰是**最不该占坑**的那种情况。

`main()` 里那个线程调用（约 706 行）多传一个参数：

```python
        threading.Thread(
            target=_sweep_in_background,
            args=(cfg.vault_path, DATA_DIR, build_llm(cfg), app.state.busy),
            daemon=True,
        ).start()
```

（`app` 就是上面 691 行 `create_app(cfg)` 那个，`app.state.busy` 现成的。）

- [ ] **Step 4: `/sweep/run` 占锁，被挡时落报告**

`src/kb/web/router.py:348`，整个 `_run_sweep_or_report` 换成：

```python
    def _run_sweep_or_report() -> RedirectResponse:
        """跑一次巡检，失败就落一份没读的报告——**人在这儿等着，不能甩 500**。

        写成报告的语义和后台那条失败路径一致（`_sweep_in_background` 也这么落），
        巡检页上看得见原因。**没有知识库也走这条路**：`/sweep` 是个页面表单，
        回 409 JSON 很难看，而且用户看不出下一步该干什么。

        **正在整理/迁移/移除时不跑，但也不回 409**——理由同上，这还是个页面
        表单，用户是在巡检页上点了个按钮。挡它的理由和 Task 4 挡整理一样：
        `run_sweep` 走的是和整理草稿同一条链，插进一轮迁移里会写成
        「一半旧库一半新库」，而那个样子是**看起来一切正常**。
        """
        if not busy.acquire("sweep"):
            sweep_state.save_report(
                data_dir,
                {
                    "summary": f"这次巡检没跑成：{busy.refusal}",
                    "tag_merges": [],
                    "dir_merges": [],
                },
            )
            return RedirectResponse("/sweep", status_code=303)
        try:
            vault_path = get_cfg().vault_path
            if vault_path is None:
                sweep_state.save_report(
                    data_dir,
                    {
                        "summary": f"这次巡检没跑成：{NO_VAULT_MESSAGE}",
                        "tag_merges": [],
                        "dir_merges": [],
                    },
                )
                return RedirectResponse("/sweep", status_code=303)

            try:
                run_sweep(vault_path, data_dir, get_llm())
            except sweep.SweepError as exc:
                sweep_state.save_report(
                    data_dir,
                    {
                        "summary": f"这次巡检没跑成：{exc}",
                        "tag_merges": [],
                        "dir_merges": [],
                    },
                )
            return RedirectResponse("/sweep", status_code=303)
        finally:
            busy.release()
```

⚠️ **被挡时这条路会顺带把 `last_sweep` 推到现在**（`save_report` 把这两件事一起
做了），所以下一次「距上次超过 `KB_SWEEP_INTERVAL` 才自动跑」会被往后推。
**这是故意的**，和隔壁「没有知识库」那条路一致：用户按了按钮、系统给了报告、
这次就算来过了；真要重跑，按钮还在。别顺手去掉这个副作用——去掉的话，
`/sweep` 页上那份报告和 `last_sweep` 就对不上了。

- [ ] **Step 5: 测试**

**(a)** `tests/api/test_busy.py` —— 给现成那两条参数化用例各加一组。
它们本来就在验「别人在动库时这些入口必须当场拒」，巡检是同类：

第 32 行和第 50 行那两处 `@pytest.mark.parametrize` 的列表都改成：

```python
@pytest.mark.parametrize(
    "what,label", [("migrate", "迁移"), ("remove", "移除"), ("sweep", "巡检")]
)
```

（**是改现成的，不是新加测试**——`("sweep", "巡检")` 进列表之后，`/organize`、
网页对话表单、`/chat`、`/new` 四条路自动都覆盖到了。）

**(b)** 同一个文件末尾追加：

```python
def test_a_background_sweep_stands_down_while_something_else_runs(tmp_path):
    """后台巡检**要占住锁**，占不到就不跑、也不占坑。

    它是服务进程里的一个 daemon 线程，跟请求处理是**并发**的——不占锁的话，
    启动后那一轮巡检跑到一半就可能被一轮整理劈开，而那个形状是
    「看起来一切正常」。
    """
    from kb.api.http import _sweep_in_background
    from kb.core import sweep_state

    data = tmp_path / "data"
    data.mkdir()
    busy = Busy()
    assert busy.acquire("organize") is True
    try:
        _sweep_in_background(tmp_path / "库", data, FakeLLM([]), busy)
    finally:
        busy.release()

    # **连坑都没占**：`mark_run` 是先用掉的「这次算跑过了」，
    # 占了就要再等一个 `KB_SWEEP_INTERVAL` 才会重试，等于白丢一次巡检。
    state = sweep_state.load_state(data)
    assert state.get("last_sweep") is None
    assert "report" not in state
```

顶部 import 补 `from kb.core.lifecycle import Busy`。
（`Busy` 在 `test_busy.py` 里现在是**通过 `client.app.state.busy` 拿的**，
从没直接 import 过——这条新用例要自己造一把，得补上。）

**(c)** `tests/web/test_setup.py` 末尾追加：

```python
def test_sweep_run_reports_instead_of_409_while_busy(client, tmp_path):
    """`/sweep/run` 被挡时**落一份报告，不是 409**。

    它是个页面表单（`/sweep` 页上那个按钮），回 409 JSON 用户看到一坨
    英文，也不知道下一步该干什么。落成报告，巡检页上直接显示原因——
    跟「没有知识库」那条路同一个形状。

    那句原因和 409 的 detail **是同一句话**（`Busy.refusal`），所以这里
    也断言「整理」两个字：两边各写一份的话，改了一边漏了另一边，
    用户就会看到两种说法。
    """
    from kb.core import sweep_state

    busy = client.app.state.busy
    assert busy.acquire("organize") is True
    try:
        resp = client.post("/sweep/run", follow_redirects=False)
        assert resp.status_code == 303                   # 重定向回 /sweep，不是 409
    finally:
        busy.release()

    report = sweep_state.load_state(tmp_path / "data")["report"]
    assert "整理" in report["summary"]
    assert report["read"] is False
```

⚠️ **`tmp_path / "data"` 就是 `app` 夹具用的那个目录**（`app` 建的就是
`tmp_path / "data"`），同一个用例里的 `tmp_path` 是同一个——`env` / `app` /
`client` 这几个夹具都挂在它上面。别去读真的 `data/`。

- [ ] **Step 6: 跑目标测试**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m pytest tests/api/test_busy.py tests/web/test_setup.py -v
```

- [ ] **Step 7: ruff + 全量 + 提交**

```bash
"D:/Conda_base/envs/kn_base/python.exe" -m ruff check src tests scripts
"D:/Conda_base/envs/kn_base/python.exe" -m pytest -v
git -C "E:/Study_Projects/KN_Base_t10" add src/kb/core/lifecycle.py src/kb/api/http.py src/kb/web/router.py tests/api/test_busy.py tests/web/test_setup.py
git -C "E:/Study_Projects/KN_Base_t10" commit -F "C:/Users/wuyeu/AppData/Local/Temp/kb_lifecycle_t10.txt"
```

```
feat: /sweep 也挂上 Busy 锁——它和整理是同一条链

Task 4 给 /organize、/push、/setup/migrate、/setup/remove 都挂上了锁，
唯独漏了巡检。而 run_sweep 的 docstring 自己写着「它走的是和整理草稿
同一条链」——同样改 vault、同样落一个 git commit，插进一轮迁移里是同
一个损坏形状，失败的样子同样是「看起来一切正常」。

后台那条 _sweep_in_background 更实在：它是服务进程里的 daemon 线程，
跟请求处理并发，不占锁的话启动后那轮巡检跑到一半就可能被一轮整理劈开。
mark_run 也一并挪进锁内——留在锁外的话，拿不到锁反而先把「这次算跑过
了」的坑占上，等于白丢一次巡检。

/sweep/run 被挡时不回 409：它是个页面表单，409 JSON 在巡检页上很难看。
改成落一份报告，和「没有知识库」那条路同一个形状。那句原因与 409 的
detail 共用 Busy.refusal，只有一份。

被挡时仍会推 last_sweep（save_report 把两件事一起做了），所以下一次自动
巡检会往后推一个间隔。这是故意的：用户按了按钮、系统给了报告，这次就算
来过了；要重跑按钮还在。

Co-Authored-By: Claude Code <noreply@anthropic.com>
```

- [ ] **Step 8: 问题记录：删掉那条 🔴，并把结论记进「已经解决的」**

**先想清楚一件事**：`docs/03_问题记录.md` 是**设计状态的唯一真源**，
而**这份计划不是**。结论变了只改那里，别只在计划里留一句就完事——
下一个翻设计状态的人根本不会来读计划。

**① 删。** Task 10 一落地，待解决问题第 6 条（`/sweep/run` 不在 Busy 锁的
覆盖里）就不成立了。按项目规矩「**作废的条目直接删掉**，不留存根」——
**整行删掉**，并把上面那句「**就这六条。**」改回「**就这五条。**」。
**别改成一个划掉的存根。**

**② 记。** 上面那步只是把「没定」抹掉，**没说定成了什么**。在
`docs/03_问题记录.md` 的「**✅ 已经解决的（归档）**」表里加一行，
格式照那张表现成的 `| 问题 | 怎么解决的 |`：

```markdown
| **`/sweep` 不在 Busy 锁的覆盖里** | **挡。** 用户 2026-09-22 拍板（给了「也挡 / 不挡但记下风险」两条路，选了前者）。三条理由：① `run_sweep` 自己那行 docstring 就写着「它走的是和整理草稿同一条链」——同样改 vault 里的笔记、同样落一个 git commit，和整理是**同一个损坏形状**；② 巡检和整理共用同一把 `Busy`，**挡它没有额外代价**；③ 真撞上的样子是「看起来一切正常」，正是这把锁存在的理由。<br>落地：后台那条 `_sweep_in_background` 拿不到锁就**不跑、也不 `mark_run`**（不能拿「没跑成」冒充「跑过了」，否则下一轮 `due()` 会以为刚巡检完）；`/sweep/run` 被挡时**不破例回 409**——它是页面表单，409 的 JSON 很难看——而是落一份「这次巡检没跑成」的报告，跟别的失败走同一条路（`router.py` 原有的设计，没动）。 |
```

位置放在 `**巡检间隔「6 天」是拍的**` 那一行附近（同属巡检话题，挨着好找）。

---

## 收尾检查（Task 1–10 全做完之后）

原 Task 8 末尾那份清单照跑，另外加两条：

- [ ] `README.md` 与 `.env.example` 里如果有「哪些操作会互斥」的说法，跟着补上巡检。
- [ ] `git log --oneline main` 看一遍这一串提交是不是按 Task 1→10 排的，且**没有任何一条带了 `data/` 下的东西**。
