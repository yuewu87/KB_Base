# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**主要用户是作者本人**——一个在多个项目间切换、每个项目都开着 AI 会话的人。他不是在「记笔记」，是在**把会话里刚得出结论的那个瞬间截下来**：那条结论现在不记，过两天再想找就找不到了。

**次要用户是想克隆这个库的人。** README 是按公开项目写的（安装步骤、`.env.example`、默认路径都标了「作者本机」），所以**陌生人第一次跑起来的体验是产品的一部分**，不是附赠品。

## Product Purpose

把「随口一句话」变成「以后查得到的一条知识」，而**不需要用户在任何时刻做分类决定**。

成功的样子：投递时只交正文，不填类型、不填标签、不选目录；要查的时候一句「查一下 X」就能拿回自己当时的结论，并且知道那是库里记过的。

## Positioning

**检索靠标签，不靠文件夹。**

文件夹的物理位置只能有一个，但一条知识天然属于多个上下文——「IE 缩放导致点击坐标算歪」属于浏览器自动化、属于坐标换算、属于 RPA 项目，放哪个文件夹都说得通，放哪个都会在别的场景里想不起来。

所以文件夹只决定「物理放哪」，标签决定「属于哪些主题」。**一篇能挂多个标签，不能放多个文件夹。** 推论是：文件夹放错了也不影响找得到，于是「AI 偶尔分错类」不再是致命问题，也就不需要逼人逐条审核。

这一点同类工具不好抄：它们多数把文件夹 / 分类当成检索的主索引，改这一条等于推翻自己的数据模型。

## Operating Context

两个**平级**入口，能力完全一样，只是走 shell 还是走浏览器：

- **会话层**（Claude Code 等）：只投递、发请求。日常主入口。
- **Web UI**：**每天都开着的常用入口**——投递、对话、看整理日志、看工作日志、巡检、改设置。

知识本体在 Obsidian vault（独立仓库、刻意无远端）。人只用 Obsidian 做三件事：看、查、随手改。主要的读和写都是会话里的 AI。

整理切成四段：规划（LLM）→ 校验（机器）→ 审核（只在新建分类时调 LLM）→ 落盘（不调 LLM）。

## Capabilities and Constraints

**约束（都不能绕过）：**

- **本体是纯 markdown**——没有数据库、没有隐藏格式，记事本能打开。Obsidian 只是它的一个视图，卸载了文件还在，git 历史还在。
- **vault 是另一个仓库，刻意不配远端**——里头是私人笔记。
- **本仓库是 public 的**，运行数据整棵排除在 git 之外（`data/` 里有会话历史，进去就是公开发布）。
- **机械约束归代码，判断题归模型**——能落成一行 `if` 的规则，不写进提示词或文档。
- 界面语言中文。

**平台：** `web`，**只面向桌面浏览器**。主区加一条右栏的现有布局按桌面定，不为窄屏做适配。

## Brand Commitments

- 名称：KN_Base。

**用户提出的一条绑定视觉约束**（只记录，方向展开不在 init 范围内）：界面要提供**六套皮肤**——四套来自用户指定的四组撞色、一套保留现有蓝色、一套是现有皮肤的白换黑；并加入「绝区零」风格的视觉元素，但**不得出现该游戏的任何文字或品牌元素**。布局结构不变，只改配色和元素设计。

## Evidence on Hand

- 设计文档：`docs/01_架构.md`（目标架构）、`docs/02_需求.md`（要什么）、`docs/03_问题记录.md`（设计决策的唯一真源，只写「为什么」）、`docs/04_踩坑与经验.md`。
- 会话层接入指南：`docs/接入指南/kn-record/SKILL.md`——**装成 skill 用的，不是给人读的说明**。
- 模板：`templates/笔记/`（机器读，套进笔记）、`templates/投递/`（人读，帮他把话说清）。
- 代码：`src/kb/{core,llm,api,web}/`；测试全绿，LLM 全部 mock。

**不要编造：** 用户量、性能基准、外部用户评价。

## Product Principles

1. **不让人做分类决定。** 投递只交正文；类型、标签、目录、文件名都是整理阶段的事。
2. **文件夹放错不是事故。** 检索的主索引是标签，所以容忍分类错误，不为它自建审核流程。
3. **两个入口一个内核。** 会话层和 Web UI 能力完全一样；以后接别的入口是加一层，不动内核。
4. **纯文本优先。** 能用记事本打开、能进 git 历史，比任何便利功能都重要。
5. **机械的归代码，判断的归模型。** 写规则前先问：这条能不能落成一行 `if`？
