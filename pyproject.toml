[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
# 缓存统一收进 data/——根目录上只留「代码目录 + 配置 + data/」
cache_dir = "data/cache/pytest"

[tool.ruff]
line-length = 100
target-version = "py311"
cache-dir = "data/cache/ruff"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
