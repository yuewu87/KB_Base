# 下界约束 + 关键包破坏性大版本上界（Q57）
# 精确版本锁文件 requirements.lock.txt 是 pip freeze 的本机产物，不入库

fastapi>=0.141
# Web 表单（`Form(...)`）要它——starlette 1.6 起表单解析是这个库提供的
python-multipart>=0.0.9
uvicorn>=0.53
jinja2>=3.1
python-dotenv>=1.0
openai>=3.14,<4
python-frontmatter>=1.3
httpx>=0.28,<1
pytest>=9,<10
ruff>=0.9
